import type { NpcCommunityPackageFolder } from '../../npc_package_schema';

export const COMMUNITY_NPC_PACKAGE_FOLDERS: readonly NpcCommunityPackageFolder[] = [
  // Reviewed community folders are added here with explicit JSON imports.
] as const;
