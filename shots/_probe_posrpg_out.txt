=== PlayerRanged / PlayerMelee definitions + key uses ===
src/game/combat.h:315:// The camera holder's firearm state. A sibling of PlayerMelee rather than an
src/game/combat.h:319:// a magazine of shells is not a magazine of 9mm. Attached lazily, like PlayerMelee.
src/game/combat.h:320:struct PlayerRanged {
src/game/combat.h:331:struct PlayerMelee {
src/game/needs.h:229:// `player_melee_step` attaches `PlayerMelee` lazily — so no spawn, elevator or
src/app/main.cpp:1622:    // [[maybe_unused]]: superseded by PlayerRanged::shots (read straight from the
src/app/main.cpp:3763:                    // The PlayerMelee component dies with the body; the tally of
src/app/main.cpp:3776:                    reg.emplace_or_replace<game::PlayerMelee>(
src/app/main.cpp:3777:                        player, game::PlayerMelee{0, kills});
src/app/main.cpp:3909:                if (const auto* pm = reg.try_get<game::PlayerMelee>(player))
src/app/main.cpp:3952:                        const auto* prs = reg.try_get<game::PlayerRanged>(player);
src/game/combat.cpp:1006:                // Only a player carries PlayerRanged, so this quietly does nothing for
src/game/combat.cpp:1009:                    if (auto* pr = reg.try_get<PlayerRanged>(h.source)) ++pr->hits;
src/game/combat.cpp:1010:                    // And a KILL is a kill however it was made. `PlayerMelee::kills` is
src/game/combat.cpp:1016:                        if (auto* pm = reg.try_get<PlayerMelee>(h.source)) ++pm->kills;
src/game/combat.cpp:1051:    // Lazily attached, like PlayerMelee: possessing a new body after death must not
src/game/combat.cpp:1053:    PlayerRanged& pr = reg.get_or_emplace<PlayerRanged>(shooter);
src/game/combat.cpp:1199:    if (!reg.all_of<PlayerMelee>(self)) reg.emplace<PlayerMelee>(self);
src/game/combat.cpp:1200:    PlayerMelee& pm = reg.get<PlayerMelee>(self);
src/game/elevator.cpp:4:#include "game/combat.h"   // PlayerRanged, PlayerMelee — live on the body, not the pool
src/game/elevator.cpp:46:    const bool hadRanged = reg.all_of<PlayerRanged>(player);
src/game/elevator.cpp:47:    PlayerRanged ranged{};
src/game/elevator.cpp:48:    if (hadRanged) ranged = reg.get<PlayerRanged>(player);
src/game/elevator.cpp:49:    const bool hadMelee = reg.all_of<PlayerMelee>(player);
src/game/elevator.cpp:50:    PlayerMelee melee{};
src/game/elevator.cpp:51:    if (hadMelee) melee = reg.get<PlayerMelee>(player);
src/game/elevator.cpp:53:    // RpgStats is the same class of defect as PlayerRanged (RPG1). embody_as_player
src/game/elevator.cpp:83:    // PlayerRanged on a body that never fired (lazy attach stays lazy).
src/game/elevator.cpp:84:    if (hadRanged) reg.emplace_or_replace<PlayerRanged>(ne, ranged);
src/game/elevator.cpp:85:    if (hadMelee) reg.emplace_or_replace<PlayerMelee>(ne, melee);
src/game/needs.cpp:254:    // Lazy roll, the way `player_melee_step` attaches PlayerMelee lazily: no spawn
tests/game_test.cpp:737:    CHECK(!reg.all_of<PlayerRanged>(p));
tests/game_test.cpp:738:    CHECK(!reg.all_of<PlayerMelee>(p));
tests/game_test.cpp:740:        PlayerRanged pr{};
tests/game_test.cpp:747:        reg.emplace<PlayerRanged>(p, pr);
tests/game_test.cpp:748:        reg.emplace<PlayerMelee>(p, PlayerMelee{/*cooldownMs=*/0, /*kills=*/99});
tests/game_test.cpp:753:    // attr points / current psi are wiped unless captured like PlayerRanged.
tests/game_test.cpp:792:    CHECK(reg.all_of<PlayerRanged>(p));
tests/game_test.cpp:793:    CHECK(reg.get<PlayerRanged>(p).magCount == 12);
tests/game_test.cpp:794:    CHECK(reg.get<PlayerRanged>(p).weapon == static_cast<ItemId>(7));
tests/game_test.cpp:795:    CHECK(reg.get<PlayerRanged>(p).shots == 7);
tests/game_test.cpp:796:    CHECK(reg.get<PlayerRanged>(p).hits == 3);
tests/game_test.cpp:797:    CHECK(reg.all_of<PlayerMelee>(p));
tests/game_test.cpp:798:    CHECK(reg.get<PlayerMelee>(p).kills == 99);
tests/game_test.cpp:819:    CHECK(reg.all_of<PlayerRanged>(p));
tests/game_test.cpp:820:    CHECK(reg.get<PlayerRanged>(p).magCount == 12);
tests/game_test.cpp:821:    CHECK(reg.all_of<PlayerMelee>(p));
tests/game_test.cpp:822:    CHECK(reg.get<PlayerMelee>(p).kills == 99);
tests/game_test.cpp:834:    CHECK(reg.all_of<PlayerRanged>(p));
tests/game_test.cpp:835:    CHECK(reg.get<PlayerRanged>(p).magCount == 12);
tests/game_test.cpp:836:    CHECK(reg.get<PlayerRanged>(p).weapon == static_cast<ItemId>(7));
tests/game_test.cpp:837:    CHECK(reg.get<PlayerRanged>(p).shots == 7);
tests/game_test.cpp:838:    CHECK(reg.get<PlayerRanged>(p).hits == 3);
tests/game_test.cpp:839:    CHECK(reg.all_of<PlayerMelee>(p));
tests/game_test.cpp:840:    CHECK(reg.get<PlayerMelee>(p).kills == 99);
tests/game_test.cpp:2769:    const PlayerRanged* pr = reg.try_get<PlayerRanged>(shooter);
tests/game_test.cpp:2790:    const std::uint16_t magBefore = reg.get<PlayerRanged>(shooter).magCount;
tests/game_test.cpp:2794:    CHECK(reg.get<PlayerRanged>(shooter).reloadMs > 0);   // still not ready
tests/game_test.cpp:2798:    CHECK(reg.get<PlayerRanged>(shooter).reloadMs == 0);
tests/game_test.cpp:2799:    CHECK(reg.get<PlayerRanged>(shooter).magCount == magBefore - 1);
tests/game_test.cpp:2800:    CHECK(reg.get<PlayerRanged>(shooter).cooldownMs == def.cooldownMs);
tests/game_test.cpp:2828:    CHECK(reg.get<PlayerRanged>(shooter).hits == 1);
tests/suite_audit.inl:29://                          PlayerMelee::kills — the counter the HUD prints and the one
tests/suite_audit.inl:389:// `++pm.kills`) and `projectile_step` credited a HIT to PlayerRanged::hits
tests/suite_audit.inl:390:// (combat.cpp:827) but credited a KILL to nothing — PlayerRanged has no kill field and
tests/suite_audit.inl:391:// PlayerMelee was not touched. The HUD prints PlayerMelee::kills and carries it across
tests/suite_audit.inl:396:// `if (auto* pm = reg.try_get<PlayerMelee>(h.source)) ++pm->kills;` — the shot is
tests/suite_audit.inl:399:// Asserted on PlayerMelee::kills because that is the counter the HUD reads and the one
tests/suite_audit.inl:422:    reg.emplace<PlayerMelee>(shooter, PlayerMelee{0, 0});
tests/suite_audit.inl:423:    reg.emplace<PlayerRanged>(shooter, PlayerRanged{});
tests/suite_audit.inl:450:    CHECK(reg.get<PlayerRanged>(shooter).hits == 1);         // the hit is credited
tests/suite_audit.inl:451:    CHECK(reg.get<PlayerMelee>(shooter).kills == 1);         // and now so is the KILL
tests/suite_noise.inl:366:            r.reg.emplace_or_replace<PlayerRanged>(
tests/suite_noise.inl:367:                r.player, PlayerRanged{0, 0, def->magazine, gun, 0, 0});
tests/suite_rpg.inl:521:// numbers land on MobRef::hp and PlayerMelee/PlayerRanged::cooldownMs.
tests/suite_rpg.inl:568:        CHECK(reg.get<PlayerMelee>(self).cooldownMs == fist.cooldownMs);
tests/suite_rpg.inl:626:        CHECK(reg.get<PlayerMelee>(self).cooldownMs ==
tests/suite_rpg.inl:670:        reg.emplace<PlayerRanged>(
tests/suite_rpg.inl:671:            shooter, PlayerRanged{0, 0, def->magazine, gun, 0, 0});
tests/suite_rpg.inl:675:        CHECK(reg.get<PlayerRanged>(shooter).cooldownMs == def->cooldownMs);
tests/suite_rpg.inl:721:        reg.emplace<PlayerRanged>(
tests/suite_rpg.inl:722:            shooter, PlayerRanged{0, 0, def->magazine, gun, 0, 0});
tests/suite_rpg.inl:733:        CHECK(reg.get<PlayerRanged>(shooter).cooldownMs ==
tests/suite_vendorammo.inl:23:// requires `PlayerRanged::magCount` to come back non-zero, which is the same code the
tests/suite_vendorammo.inl:277:        const PlayerRanged* pr = reg.try_get<PlayerRanged>(shooter);

=== elevator.cpp body-swap window (Rpg/Ranged/Melee) ===
-- @4 --
    2|
    3|#include "ecs/components.h"
>   4|#include "game/combat.h"   // PlayerRanged, PlayerMelee — live on the body, not the pool
    5|#include "game/embody.h"
    6|#include "game/rpg.h"      // RpgStats — XP/attrs/psi live on the body, not the pool
-- @6 --
    4|#include "game/combat.h"   // PlayerRanged, PlayerMelee — live on the body, not the pool
    5|#include "game/embody.h"
>   6|#include "game/rpg.h"      // RpgStats — XP/attrs/psi live on the body, not the pool
    7|
    8|namespace giga::game {
-- @46 --
   44|    // Melee kills tally has the same lifetime; main.cpp already re-stamps it on
   45|    // death-possession, but a ride is not a death and never hit that path.
>  46|    const bool hadRanged = reg.all_of<PlayerRanged>(player);
   47|    PlayerRanged ranged{};
   48|    if (hadRanged) ranged = reg.get<PlayerRanged>(player);
-- @47 --
   45|    // death-possession, but a ride is not a death and never hit that path.
   46|    const bool hadRanged = reg.all_of<PlayerRanged>(player);
>  47|    PlayerRanged ranged{};
   48|    if (hadRanged) ranged = reg.get<PlayerRanged>(player);
   49|    const bool hadMelee = reg.all_of<PlayerMelee>(player);
-- @48 --
   46|    const bool hadRanged = reg.all_of<PlayerRanged>(player);
   47|    PlayerRanged ranged{};
>  48|    if (hadRanged) ranged = reg.get<PlayerRanged>(player);
   49|    const bool hadMelee = reg.all_of<PlayerMelee>(player);
   50|    PlayerMelee melee{};
-- @49 --
   47|    PlayerRanged ranged{};
   48|    if (hadRanged) ranged = reg.get<PlayerRanged>(player);
>  49|    const bool hadMelee = reg.all_of<PlayerMelee>(player);
   50|    PlayerMelee melee{};
   51|    if (hadMelee) melee = reg.get<PlayerMelee>(player);
-- @50 --
   48|    if (hadRanged) ranged = reg.get<PlayerRanged>(player);
   49|    const bool hadMelee = reg.all_of<PlayerMelee>(player);
>  50|    PlayerMelee melee{};
   51|    if (hadMelee) melee = reg.get<PlayerMelee>(player);
   52|
-- @51 --
   49|    const bool hadMelee = reg.all_of<PlayerMelee>(player);
   50|    PlayerMelee melee{};
>  51|    if (hadMelee) melee = reg.get<PlayerMelee>(player);
   52|
   53|    // RpgStats is the same class of defect as PlayerRanged (RPG1). embody_as_player
-- @53 --
   51|    if (hadMelee) melee = reg.get<PlayerMelee>(player);
   52|
>  53|    // RpgStats is the same class of defect as PlayerRanged (RPG1). embody_as_player
   54|    // always re-rolls via random_rpg(level, id) — deterministic base sheet for the
   55|    // record, but mid-run XP, spent attr points, and current psi are wiped. Death
-- @58 --
   56|    // possession already captures/restores the sheet in main.cpp (carriedRpg);
   57|    // a ride is not a death and never hit that path. Capture before fold_back.
>  58|    const bool hadRpg = reg.all_of<RpgStats>(player);
   59|    RpgStats rpg{};
   60|    if (hadRpg) rpg = reg.get<RpgStats>(player);
-- @59 --
   57|    // a ride is not a death and never hit that path. Capture before fold_back.
   58|    const bool hadRpg = reg.all_of<RpgStats>(player);
>  59|    RpgStats rpg{};
   60|    if (hadRpg) rpg = reg.get<RpgStats>(player);
   61|
-- @60 --
   58|    const bool hadRpg = reg.all_of<RpgStats>(player);
   59|    RpgStats rpg{};
>  60|    if (hadRpg) rpg = reg.get<RpgStats>(player);
   61|
   62|    // Fold back on the departed floor (writes the record's macro cell from the
-- @83 --
   81|
   82|    // Restore combat state only if it was present — do not invent a fresh
>  83|    // PlayerRanged on a body that never fired (lazy attach stays lazy).
   84|    if (hadRanged) reg.emplace_or_replace<PlayerRanged>(ne, ranged);
   85|    if (hadMelee) reg.emplace_or_replace<PlayerMelee>(ne, melee);
-- @84 --
   82|    // Restore combat state only if it was present — do not invent a fresh
   83|    // PlayerRanged on a body that never fired (lazy attach stays lazy).
>  84|    if (hadRanged) reg.emplace_or_replace<PlayerRanged>(ne, ranged);
   85|    if (hadMelee) reg.emplace_or_replace<PlayerMelee>(ne, melee);
   86|    // Character sheet: overwrite the random_rpg roll from embody_as_player with
-- @85 --
   83|    // PlayerRanged on a body that never fired (lazy attach stays lazy).
   84|    if (hadRanged) reg.emplace_or_replace<PlayerRanged>(ne, ranged);
>  85|    if (hadMelee) reg.emplace_or_replace<PlayerMelee>(ne, melee);
   86|    // Character sheet: overwrite the random_rpg roll from embody_as_player with
   87|    // the pre-ride progression. embody always attaches RpgStats, so this is
-- @87 --
   85|    if (hadMelee) reg.emplace_or_replace<PlayerMelee>(ne, melee);
   86|    // Character sheet: overwrite the random_rpg roll from embody_as_player with
>  87|    // the pre-ride progression. embody always attaches RpgStats, so this is
   88|    // emplace_or_replace rather than a lazy-absent guard (unlike combat).
   89|    if (hadRpg) reg.emplace_or_replace<RpgStats>(ne, rpg);
-- @88 --
   86|    // Character sheet: overwrite the random_rpg roll from embody_as_player with
   87|    // the pre-ride progression. embody always attaches RpgStats, so this is
>  88|    // emplace_or_replace rather than a lazy-absent guard (unlike combat).
   89|    if (hadRpg) reg.emplace_or_replace<RpgStats>(ne, rpg);
   90|
-- @89 --
   87|    // the pre-ride progression. embody always attaches RpgStats, so this is
   88|    // emplace_or_replace rather than a lazy-absent guard (unlike combat).
>  89|    if (hadRpg) reg.emplace_or_replace<RpgStats>(ne, rpg);
   90|
   91|    r.player = ne;

=== embody.cpp RpgStats attach ===
-- @4 --
    3|#include "game/faction.h"
>   4|#include "game/rpg.h"      // RpgStats, random_rpg
    5|// NOTE: #include "game/ai.h" (AiBrain) goes back here when the utility AI is
    6|// adapted to main mob_table -- see tools/branch_port_pending/README.md
    7|
-- @95 --
   94|    // immediately afterwards, and the body is freshly built here in either case.
>  95|    reg.emplace_or_replace<RpgStats>(e, random_rpg(pool.level(id), id));
   96|
   97|    pool.set_player(id, true);
   98|    return e;

=== main.cpp death + possess windows full ===

---- main.cpp ~1677 ----
 1663|    std::int32_t questPaid = 0;       // roubles paid by finished quests
 1664|    std::int32_t sold = 0;            // roubles taken for the haul
 1665|    std::int32_t spent = 0;           // roubles spent on supplies
 1666|    // Who buys on this floor. The dominant faction sets the sell rate, which gives the
 1667|    // faction matrix a second live consumer and makes the territory rumour worth
 1668|    // acting on rather than being colour. [vendor.h]
 1669|    game::VendorKind vendorKind = game::VendorKind::Citizen;
 1670|    std::uint32_t deaths = 0;
 1671|    std::uint32_t kills = 0;       // carried across possession
 1672|    // The character sheet, carried across possession for the same reason `kills`
 1673|    // is: a death takes the body, not the person's progression. Seeded level 1 so
 1674|    // the very first embodiment (which happens below, before any death) has
 1675|    // something valid to fall back to; embody_as_player overwrites it with the
 1676|    // record's own rolled build.
 1677|    game::RpgStats carriedRpg = game::fresh_rpg(1);
 1678|    bool attackHeld = false;
 1679|    // Carve scratch + result, reused across ops so a carve allocates nothing
 1680|    // after warmup ([world/destruct.h]).
 1681|    CarveScratch carveScratch;
 1682|    CarveResult carveResult;
 1683|    // Combat → geometry seam ([combat.h]): bullets/melee propose, sim disposes
 1684|    // below behind the same doors.frozen gate as the console carve row.
 1685|    game::CarveProposalQueue combatCarves;
 1686|
 1687|    bool healWanted = false;
 1688|    bool eatWanted = false;       // G, consumed by one sim step
 1689|    bool drinkWanted = false;     // T, consumed by one sim step
 1690|    bool sellWanted = false;      // B, consumed by one sim step
 1691|    bool buyWanted = false;       // R, consumed by one sim step       // set by H, consumed by one sim step
 1692|    bool craftWanted = false;     // C, consumed by one sim step
 1693|    bool scrapWanted = false;     // X, consumed by one sim step
 1694|    bool showCraftingWindow = false;
 1695|    bool showVendorWindow = false;
 1696|    // Run state, not world state, so it lives beside the ledger. CraftingState is a
 1697|    // 96-byte POD ([craft.h]) — nothing to own, nothing to free. craft_init zeroes the

---- main.cpp ~1838 ----
 1824|        runState.player.maxHp = pool.max_hp(nr->id);
 1825|        // The SIGNED floor, explicitly: NpcPool::floor() is the seeding label
 1826|        // and LayerId is a recycled storage slot. [save.h]
 1827|        runState.player.floorNumber = currentFloor;
 1828|        runState.player.cx = static_cast<std::uint8_t>(
 1829|            wrap_macro(static_cast<int>(sp.x / kCellSize)));
 1830|        runState.player.cy = static_cast<std::uint8_t>(
 1831|            wrap_macro(static_cast<int>(sp.y / kCellSize)));
 1832|        runState.player.cz = static_cast<std::uint8_t>(
 1833|            wrap_macro(static_cast<int>(sp.z / kCellSize)));
 1834|        // Version 7: character sheet + crafting bank. Prefer the live entity
 1835|        // component; fall back to the death-surviving carried snapshot so a
 1836|        // mid-death F5 still banks progression. [save.h] SAVRPG
 1837|        if (const game::RpgStats* rs = reg.try_get<game::RpgStats>(player))
 1838|            runState.rpg = *rs;
 1839|        else
 1840|            runState.rpg = carriedRpg;
 1841|        runState.craft = crafting;
 1842|        // REFRESH, not append and not clear. [save.h]
 1843|        game::refresh_opened_containers(reg, pl, currentFloor, runState.opened);
 1844|        // v6: the macro world travels whole — pool table, macro clock, faction
 1845|        // matrix. The society you come back to is the one you left. [save.h]
 1846|        pool.save_rows(runState.poolBlob);
 1847|        macroSim.save_state(runState.macroBlob);
 1848|        runState.factions = factionRel;
 1849|        write_floor_file(stack.layer(pl), currentFloor);
 1850|        char runPath[128];
 1851|        run_save_path(runPath, sizeof runPath);
 1852|        return write_run(runState, runPath);
 1853|    };
 1854|
 1855|    // One ride, either shape: the elevator keys ([ / ]) pass a DIRECTION, the
 1856|    // console teleport passes an ABSOLUTE registered floor. Everything after
 1857|    // the streamer call — the arrival refresh of mobs/containers/doors/nav/
 1858|    // props and the per-floor clocks — is identical, which is exactly why it

---- main.cpp ~3146 ----
 3132|                                            particlePass.emit_burst(ppos + vec3{0.0f, 0.5f, 0.0f},
 3133|                                                                    vec3{0.0f, -0.5f, 0.0f},
 3134|                                                                    vec3{0.40f, 0.70f, 0.60f},
 3135|                                                                    gpu::GpuParticleKind::DustMote,
 3136|                                                                    16, 1.5f, 0.5f, 0.10f, 90.0f);
 3137|                                        }
 3138|                                        game::NoiseProfile np{5.0f, 500, 1, game::NoiseSource::Door};
 3139|                                        game::noise_publish(noiseField, activeLayer, ppos, np, 0);
 3140|                                    }
 3141|                                }
 3142|                            }
 3143|                        }
 3144|                    }
 3145|                }
 3146|                if (possessWanted) {
 3147|                    possessWanted = false;
 3148|                    if (reg.valid(player)) {
 3149|                        const vec3 ppos = reg.get<Transform>(player).pos;
 3150|                        Entity newPlayer = possess_nearest_survivor(reg, pool, activeLayer, ppos, 8.0f);
 3151|                        if (newPlayer != entt::null) {
 3152|                            player = newPlayer;
 3153|                            const vec3 newPos = reg.get<Transform>(player).pos;
 3154|                            const auto* nr = reg.try_get<game::NpcRef>(player);
 3155|                            const game::NpcId newId = nr ? nr->id : 0;
 3156|                            std::snprintf(elevDiagLine, sizeof(elevDiagLine),
 3157|                                          "MIND PROJECTION: POSSESSED RESIDENT BODY #%u AT (%.1f, %.1f)",
 3158|                                          newId, newPos.x, newPos.z);
 3159|                            elevDiagAt = simTick;
 3160|                            if (particlePass.ready()) {
 3161|                                particlePass.emit_burst(newPos + vec3{0.0f, 1.0f, 0.0f},
 3162|                                                        vec3{0.0f, 1.0f, 0.0f},
 3163|                                                        vec3{0.30f, 0.95f, 0.85f},
 3164|                                                        gpu::GpuParticleKind::BioSpore,
 3165|                                                        48, 4.0f, 1.0f, 0.20f, 200.0f);
 3166|                            }

---- main.cpp ~3383 ----
 3369|                // Remember which pool row is the player BEFORE the death point,
 3370|                // because after it the entity is gone and the row is the only way
 3371|                // back to what was being carried.
 3372|                game::NpcId deadRow = game::kInvalidNpc;
 3373|                if (reg.valid(player))
 3374|                    if (const auto* nr0 = reg.try_get<game::NpcRef>(player))
 3375|                        deadRow = nr0->id;
 3376|                // The character sheet, for the same reason and at the same point:
 3377|                // after the death point there is no component left to read, and a
 3378|                // fresh body would otherwise arrive at level 1. `finalize_deaths`
 3379|                // may also LEVEL this up on the killing blow, so the snapshot is
 3380|                // taken before it runs and re-taken below when the body survives.
 3381|                if (reg.valid(player))
 3382|                    if (const auto* rs0 = reg.try_get<game::RpgStats>(player))
 3383|                        carriedRpg = *rs0;
 3384|                game::loot_dead_mobs(reg, activeLayer, currentFloor,
 3385|                                     static_cast<std::uint32_t>(simTick));
 3386|                // ONE death point per tick, after everything that can deal damage
 3387|                // (combat.h). Nothing else in the tree destroys a damaged entity.
 3388|                deaths += game::finalize_deaths(reg, pool, bus, simTick,
 3389|                                                &noiseField);
 3390|                // Containers first, then loose pickups: a crate emptied this tick
 3391|                // should be sweepable in the same tick if the inventory overflowed
 3392|                // onto the floor. [container.h]
 3393|                const std::int32_t fromBox =
 3394|                    game::loot_containers_step(reg, pool, activeLayer, &noiseField);
 3395|                if (fromBox != 0) {
 3396|                    loot += fromBox;
 3397|                    containerTake += fromBox;
 3398|                    game::sync_armour(reg, pool, player);
 3399|                }
 3400|                const std::int32_t got =
 3401|                    game::pickup_step(reg, pool, bus, activeLayer, simTick);
 3402|                if (got != 0) {
 3403|                    loot += got;

---- main.cpp ~3544 ----
 3530|                            activeLayer = nl;
 3531|                            player = pid != game::kInvalidNpc
 3532|                                         ? game::embody_as_player(reg, pool, pid,
 3533|                                                                  nl)
 3534|                                         : lr.player;
 3535|                            if (pid != game::kInvalidNpc) {
 3536|                                game::apply_player_snapshot(pool, pid,
 3537|                                                            runState.player);
 3538|                                game::sync_armour(reg, pool, player);
 3539|                            }
 3540|                            // Version 7: stamp the sheet onto the body and the
 3541|                            // death-surviving carried snapshot, then restore the
 3542|                            // crafting bank. embody_as_player may have rolled a
 3543|                            // fresh sheet from the record — overwrite it. [save.h]
 3544|                            carriedRpg = runState.rpg;
 3545|                            if (reg.valid(player))
 3546|                                reg.emplace_or_replace<game::RpgStats>(
 3547|                                    player, runState.rpg);
 3548|                            crafting = runState.craft;
 3549|                            // Per-floor clocks and channels reset, same as any
 3550|                            // arrival.
 3551|                            samosbor = game::samosbor_new_game(sbRng);
 3552|                            rumourLine[0] = 0;
 3553|                            rumourAt = 0;
 3554|                            game::noise_clear(noiseField);
 3555|                            // Arrival order is load-path law: containers before
 3556|                            // re-open, mobs, floor file, doors, freeze, bake,
 3557|                            // then placement. [save.h]
 3558|                            refresh_floor_containers(reg, stack.layer(nl),
 3559|                                                     currentFloor, nl);
 3560|                            const std::size_t reopened =
 3561|                                game::apply_opened_containers(
 3562|                                    reg, nl, currentFloor,
 3563|                                    runState.opened.data(),
 3564|                                    runState.opened.size());

---- main.cpp ~3766 ----
 3752|                }
 3753|                // The player is not exempt: if it died it no longer exists, and
 3754|                // everything below reads through it. Take another body now.
 3755|                if (!reg.valid(player)) {
 3756|                    // Bill the death before taking a new body. The dead pool row
 3757|                    // keeps its inventory forever (the pool never reclaims a slot),
 3758|                    // so those items are already gone in the only sense that
 3759|                    // matters — no living body can reach them. All that was missing
 3760|                    // was counting it. [extraction.h]
 3761|                    if (pool.valid(deadRow))
 3762|                        game::record_death(ledger, pool.inventory(deadRow));
 3763|                    // The PlayerMelee component dies with the body; the tally of
 3764|                    // what that person killed does not.
 3765|                    //
 3766|                    // Neither does the character sheet. `embody_as_player` rolls a
 3767|                    // fresh RpgStats from the new record, which is right for a
 3768|                    // possession but wrong across a DEATH — losing every level to a
 3769|                    // bad corridor is not the reference's rule, and the kill tally
 3770|                    // beside it already survives for the same reason. Captured
 3771|                    // before the possess (the old body is already gone by then, so
 3772|                    // this reads the value saved off at the top of the death path).
 3773|                    player = possess_a_survivor(reg, pool, activeLayer);
 3774|                    if (player == entt::null) { running = false; break; }
 3775|                    aim_player(reg, player);
 3776|                    reg.emplace_or_replace<game::PlayerMelee>(
 3777|                        player, game::PlayerMelee{0, kills});
 3778|                    reg.emplace_or_replace<game::RpgStats>(player, carriedRpg);
 3779|                } else if (const auto* rsLive = reg.try_get<game::RpgStats>(player)) {
 3780|                    // The body SURVIVED the tick. Refresh the snapshot, because
 3781|                    // finalize_deaths above may have just awarded XP and levelled it
 3782|                    // — without this, a level earned by the killing blow would be
 3783|                    // rolled back by the next death.
 3784|                    carriedRpg = *rsLive;
 3785|                }
 3786|                ++simTick;

---- main.cpp ~3778 ----
 3764|                    // what that person killed does not.
 3765|                    //
 3766|                    // Neither does the character sheet. `embody_as_player` rolls a
 3767|                    // fresh RpgStats from the new record, which is right for a
 3768|                    // possession but wrong across a DEATH — losing every level to a
 3769|                    // bad corridor is not the reference's rule, and the kill tally
 3770|                    // beside it already survives for the same reason. Captured
 3771|                    // before the possess (the old body is already gone by then, so
 3772|                    // this reads the value saved off at the top of the death path).
 3773|                    player = possess_a_survivor(reg, pool, activeLayer);
 3774|                    if (player == entt::null) { running = false; break; }
 3775|                    aim_player(reg, player);
 3776|                    reg.emplace_or_replace<game::PlayerMelee>(
 3777|                        player, game::PlayerMelee{0, kills});
 3778|                    reg.emplace_or_replace<game::RpgStats>(player, carriedRpg);
 3779|                } else if (const auto* rsLive = reg.try_get<game::RpgStats>(player)) {
 3780|                    // The body SURVIVED the tick. Refresh the snapshot, because
 3781|                    // finalize_deaths above may have just awarded XP and levelled it
 3782|                    // — without this, a level earned by the killing blow would be
 3783|                    // rolled back by the next death.
 3784|                    carriedRpg = *rsLive;
 3785|                }
 3786|                ++simTick;
 3787|                // THE MACRO CLOCK. kMacroPeriodTicks = kSimHz*2 = 250, which is
 3788|                // exactly 2.000 s at 125 Hz — kSimStepMs is exactly 8 ms, so the
 3789|                // period is lossless and cannot drift. Placed after ++simTick so the
 3790|                // first fire is tick 250 and not tick 0, before the world is embodied.
 3791|                // The coarse clock (tick_/dayTenths_) lives inside MacroSim, so main
 3792|                // holds no accumulator of its own.
 3793|                //
 3794|                // The live `factionRel` is lent, never copied: the social pass is the
 3795|                // only reader and it stays gated off while socialFormRatePerYear is 0
 3796|                // (that pass demands a 128 MiB rel_ column — see [npc_pool.h] — and
 3797|                // nothing renders the graph yet). Passing the real matrix now means
 3798|                // that when social is switched on it reads the one true source of

=== F5/F9 rpg+craft capture ===
 1815|    auto save_run_now = [&]() -> bool {
 1838|            runState.rpg = *rs;
 1840|            runState.rpg = carriedRpg;
 1841|        runState.craft = crafting;
 1965|        save_run_now();
 3427|                    if (save_run_now())
 3544|                            carriedRpg = runState.rpg;
 3547|                                    player, runState.rpg);
 3548|                            crafting = runState.craft;
 3777|                        player, game::PlayerMelee{0, kills});
 4882|                        save_run_now();

=== Does possess_a_survivor / nearest stamp weapons/rpg? ===
 1079|Entity possess_a_survivor(Registry& reg, game::NpcPool& pool, LayerId layer) {
 3773|                    player = possess_a_survivor(reg, pool, activeLayer);
 1107|Entity possess_nearest_survivor(Registry& reg, game::NpcPool& pool, LayerId layer, const vec3& playerPos, float reachM) {

=== tests mentioning possess / carriedRpg ===
tests/game_test.cpp:426:    CHECK(floor_depth01(12) == floor_depth01(-12)); // symmetric magnitude
tests/game_test.cpp:734:    // destroys the entity; without capture/restore every ride zeroes magCount
tests/game_test.cpp:735:    // (ammo already debited from inventory into the magazine) and drops kills.
tests/game_test.cpp:737:    CHECK(!reg.all_of<PlayerRanged>(p));
tests/game_test.cpp:738:    CHECK(!reg.all_of<PlayerMelee>(p));
tests/game_test.cpp:740:        PlayerRanged pr{};
tests/game_test.cpp:743:        pr.magCount = 12;
tests/game_test.cpp:747:        reg.emplace<PlayerRanged>(p, pr);
tests/game_test.cpp:748:        reg.emplace<PlayerMelee>(p, PlayerMelee{/*cooldownMs=*/0, /*kills=*/99});
tests/game_test.cpp:753:    // attr points / current psi are wiped unless captured like PlayerRanged.
tests/game_test.cpp:792:    CHECK(reg.all_of<PlayerRanged>(p));
tests/game_test.cpp:793:    CHECK(reg.get<PlayerRanged>(p).magCount == 12);
tests/game_test.cpp:794:    CHECK(reg.get<PlayerRanged>(p).weapon == static_cast<ItemId>(7));
tests/game_test.cpp:795:    CHECK(reg.get<PlayerRanged>(p).shots == 7);
tests/game_test.cpp:796:    CHECK(reg.get<PlayerRanged>(p).hits == 3);
tests/game_test.cpp:797:    CHECK(reg.all_of<PlayerMelee>(p));
tests/game_test.cpp:798:    CHECK(reg.get<PlayerMelee>(p).kills == 99);
tests/game_test.cpp:819:    CHECK(reg.all_of<PlayerRanged>(p));
tests/game_test.cpp:820:    CHECK(reg.get<PlayerRanged>(p).magCount == 12);
tests/game_test.cpp:821:    CHECK(reg.all_of<PlayerMelee>(p));
tests/game_test.cpp:822:    CHECK(reg.get<PlayerMelee>(p).kills == 99);
tests/game_test.cpp:824:    // Ride back down: floor 1 -> 0. Second fold_back must keep mag/kills too.
tests/game_test.cpp:834:    CHECK(reg.all_of<PlayerRanged>(p));
tests/game_test.cpp:835:    CHECK(reg.get<PlayerRanged>(p).magCount == 12);
tests/game_test.cpp:836:    CHECK(reg.get<PlayerRanged>(p).weapon == static_cast<ItemId>(7));
tests/game_test.cpp:837:    CHECK(reg.get<PlayerRanged>(p).shots == 7);
tests/game_test.cpp:838:    CHECK(reg.get<PlayerRanged>(p).hits == 3);
tests/game_test.cpp:839:    CHECK(reg.all_of<PlayerMelee>(p));
tests/game_test.cpp:840:    CHECK(reg.get<PlayerMelee>(p).kills == 99);
tests/game_test.cpp:1115:        CHECK(m.dmg <= 1000);            // PAUPSINA is the one 0-damage kind
tests/game_test.cpp:1166:    // Head-count depends on |floor|, so the two arms are mirror images.
tests/game_test.cpp:1310:    // order of magnitude, and both far above the hub.
tests/game_test.cpp:1500:// Defect 1 — one damage function, and it reports what it APPLIED. In the
tests/game_test.cpp:1501:// reference, pre-armour damage leaked into the kill feed and the threat model
tests/game_test.cpp:1504:static void test_damage_reports_applied_not_raw() {
tests/game_test.cpp:1513:    DamageResult r = apply_damage(reg, pool, e, 30, DamageChannel::Kinetic,
tests/game_test.cpp:1521:    a.resist[static_cast<std::size_t>(DamageChannel::Kinetic)] = 50;
tests/game_test.cpp:1523:    r = apply_damage(reg, pool, e, 40, DamageChannel::Kinetic, entt::null);
tests/game_test.cpp:1528:    r = apply_damage(reg, pool, e, 10, DamageChannel::Fire, entt::null);
tests/game_test.cpp:1533:    reg.get<Armour>(e).resist[static_cast<std::size_t>(DamageChannel::Energy)] =
tests/game_test.cpp:1535:    r = apply_damage(reg, pool, e, 10, DamageChannel::Energy, entt::null);
tests/game_test.cpp:1540:    r = apply_damage(reg, pool, e, 9999, DamageChannel::Kinetic, entt::null);
tests/game_test.cpp:1546:// Defect 2 — apply_damage never destroys; finalize_deaths is the only place a
tests/game_test.cpp:1560:    DamageResult r = apply_damage(reg, pool, e, 50, DamageChannel::Kinetic,
tests/game_test.cpp:1571:    DamageResult again = apply_damage(reg, pool, e, 50, DamageChannel::Kinetic,
tests/game_test.cpp:1793:    DamageResult r = apply_damage(reg, pool, boss, 999, DamageChannel::Kinetic,
tests/game_test.cpp:1914:// must actually reduce the damage that lands. That is the whole reason to pick
tests/game_test.cpp:1994:        apply_damage(reg, pool, e, 100, DamageChannel::Kinetic, entt::null).applied;
tests/game_test.cpp:2002:        apply_damage(reg, pool, e, 100, DamageChannel::Kinetic, entt::null).applied;
tests/game_test.cpp:2149:    // A shot must not damage on the frame it is fired, and must be able to reach.
tests/game_test.cpp:2375:    CHECK(d.deepestFloor == 9);             // equal magnitude does not overwrite
tests/game_test.cpp:2513:    // because Sculpture's row is 8.5 cells/s and 1000 damage — with the behaviour
tests/game_test.cpp:2534:    CHECK(wall_bias_damage(wb, true) > 1.0f);
tests/game_test.cpp:2535:    CHECK(wall_bias_damage(wb, false) == 1.0f);
tests/game_test.cpp:2536:    CHECK(wall_bias_damage(0u, true) == 1.0f);
tests/game_test.cpp:2590:        CHECK(d.dmg > 0 && d.pellets >= 1 && d.magazine >= 1);
tests/game_test.cpp:2606:    // Shotguns are the pellet weapons and their damage is PER PELLET, so a burst is
tests/game_test.cpp:2608:    // 96-damage blast into an 8-damage one.
tests/game_test.cpp:2617:    // 12-pellet shotgun beats a weapon with higher per-hit damage and a slower cycle.
tests/game_test.cpp:2647:                if (d->dmg == 22 && d->magazine == 8) mak = d;
tests/game_test.cpp:2707:// show a tracer leaving the barrel, which says nothing about who took the damage.
tests/game_test.cpp:2733:            if (d->pellets == 1 && d->magazine >= 8 && d->dmg >= 20) { gun = i; break; }
tests/game_test.cpp:2753:    // tell "damaged" from "deleted".
tests/game_test.cpp:2766:    // First call with the trigger down must RELOAD, not fire: the magazine starts
tests/game_test.cpp:2769:    const PlayerRanged* pr = reg.try_get<PlayerRanged>(shooter);
tests/game_test.cpp:2771:    CHECK(pr->magCount == def.magazine);       // a full magazine came out of the pack
tests/game_test.cpp:2778:    CHECK(left == 30 - def.magazine);
tests/game_test.cpp:2790:    const std::uint16_t magBefore = reg.get<PlayerRanged>(shooter).magCount;
tests/game_test.cpp:2794:    CHECK(reg.get<PlayerRanged>(shooter).reloadMs > 0);   // still not ready
tests/game_test.cpp:2798:    CHECK(reg.get<PlayerRanged>(shooter).reloadMs == 0);
tests/game_test.cpp:2799:    CHECK(reg.get<PlayerRanged>(shooter).magCount == magBefore - 1);
tests/game_test.cpp:2800:    CHECK(reg.get<PlayerRanged>(shooter).cooldownMs == def.cooldownMs);
tests/game_test.cpp:2828:    CHECK(reg.get<PlayerRanged>(shooter).hits == 1);
tests/game_test.cpp:4189:    test_damage_reports_applied_not_raw();
tests/suite_audit.inl:29://                          PlayerMelee::kills — the counter the HUD prints and the one
tests/suite_audit.inl:176:// pool inventory, a loaded magazine, and therefore a live AMMO row — finding 4) or
tests/suite_audit.inl:389:// `++pm.kills`) and `projectile_step` credited a HIT to PlayerRanged::hits
tests/suite_audit.inl:390:// (combat.cpp:827) but credited a KILL to nothing — PlayerRanged has no kill field and
tests/suite_audit.inl:391:// PlayerMelee was not touched. The HUD prints PlayerMelee::kills and carries it across
tests/suite_audit.inl:392:// possession, so it is the tally the player reads; you could shoot a whole floor dead
tests/suite_audit.inl:396:// `if (auto* pm = reg.try_get<PlayerMelee>(h.source)) ++pm->kills;` — the shot is
tests/suite_audit.inl:399:// Asserted on PlayerMelee::kills because that is the counter the HUD reads and the one
tests/suite_audit.inl:422:    reg.emplace<PlayerMelee>(shooter, PlayerMelee{0, 0});
tests/suite_audit.inl:423:    reg.emplace<PlayerRanged>(shooter, PlayerRanged{});
tests/suite_audit.inl:450:    CHECK(reg.get<PlayerRanged>(shooter).hits == 1);         // the hit is credited
tests/suite_audit.inl:451:    CHECK(reg.get<PlayerMelee>(shooter).kills == 1);         // and now so is the KILL
tests/suite_audit.inl:876:// Low damage on its own — ItemSlot::count is 16 bits, so nothing wraps — but it breaks
tests/suite_faction2.inl:297:    // damage to the crowd is permanent. Asserted as a bottom-out at the EXACT
tests/suite_faction2.inl:298:    // kFeudMinHpPct value rather than as "some damage", because an off-by-one in the
tests/suite_faction2.inl:317:        // damage offered against a 100 HP body.
tests/suite_faction2.inl:478:        // synthetic one — this is the state possess_a_survivor actually inherits.
tests/suite_noise.inl:340:        // A gun and a magazine's worth of ammo, so the shot is a real shot through
tests/suite_noise.inl:353:        // Pre-armed: weapon already drawn, magazine already full.
tests/suite_noise.inl:358:        // which empties the magazine, then finds magCount == 0, STARTS A RELOAD and
tests/suite_noise.inl:363:        // Arming the magazine here keeps the "fire exactly once, on tick 0" design,
tests/suite_noise.inl:366:            r.reg.emplace_or_replace<PlayerRanged>(
tests/suite_noise.inl:367:                r.player, PlayerRanged{0, 0, def->magazine, gun, 0, 0});
tests/suite_rpg.inl:163:    // WITH a weapon, damage is weaponDamage + (level-1); BARE HANDED the weapon
tests/suite_rpg.inl:164:    // number is ignored entirely and damage is the level itself.
tests/suite_rpg.inl:166:    CHECK(melee_damage(r, 0, 999) == 1);      // bare hands at level 1: 1, not 999
tests/suite_rpg.inl:167:    CHECK(melee_damage(r, 1, 25) == 25);      // armed at level 1: no level bonus
tests/suite_rpg.inl:170:    CHECK(melee_damage(r, 0, 999) == 10);     // bare hands: the level
tests/suite_rpg.inl:171:    CHECK(melee_damage(r, 1, 25) == 34);      // 25 + 9
tests/suite_rpg.inl:175:    CHECK(melee_damage(r, 1, 25) == 41);      // round(34 * 1.20) = 40.8 -> 41
tests/suite_rpg.inl:176:    CHECK(melee_damage(r, 0, 0) == 12);       // round(10 * 1.20)
tests/suite_rpg.inl:178:    // A zero-damage weapon still gets the level bonus, and never goes negative.
tests/suite_rpg.inl:180:    CHECK(melee_damage(r, 1, 0) == 4);
tests/suite_rpg.inl:517:// suite_rpg already pins melee_damage / agi_*_mult_e3 arithmetic in isolation.
tests/suite_rpg.inl:519:// brute swung for the same fist damage as a fresh spawn. This drives the real
tests/suite_rpg.inl:521:// numbers land on MobRef::hp and PlayerMelee/PlayerRanged::cooldownMs.
tests/suite_rpg.inl:568:        CHECK(reg.get<PlayerMelee>(self).cooldownMs == fist.cooldownMs);
tests/suite_rpg.inl:608:            melee_damage(rs, /*weaponId=*/0, static_cast<std::int16_t>(fist.dmg));
tests/suite_rpg.inl:626:        CHECK(reg.get<PlayerMelee>(self).cooldownMs ==
tests/suite_rpg.inl:645:                if (d->pellets == 1 && d->magazine >= 8 && d->dmg >= 20) {
tests/suite_rpg.inl:670:        reg.emplace<PlayerRanged>(
tests/suite_rpg.inl:671:            shooter, PlayerRanged{0, 0, def->magazine, gun, 0, 0});
tests/suite_rpg.inl:675:        CHECK(reg.get<PlayerRanged>(shooter).cooldownMs == def->cooldownMs);
tests/suite_rpg.inl:693:                if (d->pellets == 1 && d->magazine >= 8 && d->dmg >= 20) {
tests/suite_rpg.inl:721:        reg.emplace<PlayerRanged>(
tests/suite_rpg.inl:722:            shooter, PlayerRanged{0, 0, def->magazine, gun, 0, 0});
tests/suite_rpg.inl:733:        CHECK(reg.get<PlayerRanged>(shooter).cooldownMs ==
tests/suite_utilai.inl:626:    //    back, and possession hands it back too.
tests/suite_utilai.inl:684:        CHECK(t3.considered == 0);   // out of scope entirely once possessed
tests/suite_utilai.inl:1429:                // Damage one body on a fixed schedule so RECORDER 1 (the HP-drop
tests/suite_vendorammo.inl:23:// requires `PlayerRanged::magCount` to come back non-zero, which is the same code the
tests/suite_vendorammo.inl:81:// NpcRef into a live pool row whose inventory is where the magazine comes from.
tests/suite_vendorammo.inl:254:                     "for gun id %u (mag %u)\n",
tests/suite_vendorammo.inl:257:                     static_cast<unsigned>(def->magazine));
tests/suite_vendorammo.inl:277:        const PlayerRanged* pr = reg.try_get<PlayerRanged>(shooter);
tests/suite_vendorammo.inl:280:            // The magazine is non-empty, which is only reachable through
tests/suite_vendorammo.inl:282:            CHECK(pr->magCount > 0);
tests/suite_vendorammo.inl:285:            CHECK(pr->magCount <= def->magazine);
tests/suite_vendorammo.inl:287:            // a free magazine.
tests/suite_vendorammo.inl:288:            CHECK(held(inv, sold) == before - pr->magCount);
tests/suite_vendorammo.inl:291:                         "magazine\n",
tests/suite_vendorammo.inl:292:                         static_cast<unsigned>(pr->magCount),
