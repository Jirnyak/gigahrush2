=== src/game/noise.h lines 324
    83|// `kGazeRange = 25.0f`. Noise has to be comparable with the sight radius or
    84|// "heard but not seen" is not expressible, so it joins that convention. If the
    85|// two are ever reconciled, this whole file moves by one constant.
>   86|enum class NoiseSource : std::uint8_t {
    87|    None = 0,
    88|    WeaponFire,   // a gun going off — the loudest thing in the game
    89|    Melee,        // a heavy or metal swing connecting
---
   104|
   105|// One noise event. POD, exactly 32 bytes, no interior padding, trivially
   106|// copyable — same stance as MobDef and Event.
>  107|struct Noise {
   108|    float x = 0.0f;              //  0  world metres; x/y wrap toroidally, z does not
   109|    float y = 0.0f;              //  4
   110|    float z = 0.0f;              //  8
---
   145|inline constexpr float kNoiseRadiusMin = 0.75f;
   146|
   147|// The field. Fixed storage, no allocation, trivially copyable.
>  148|struct NoiseField {
   149|    std::array<Noise, kNoiseCap> slot{};
   150|    std::uint32_t nextId = 1;      // 0 is reserved for "no noise"
   151|    std::uint32_t dropped = 0;     // refused publishes since construction
---
   165|// A publishable loudness. Separate from `Noise` because a profile is what a
   166|// SOURCE knows (a rifle is this loud for this long) and a Noise is what the field
   167|// stores (that rifle, there, with this much life left).
>  168|struct NoiseProfile {
   169|    float radius = 0.0f;
   170|    std::uint16_t ttlMs = 0;
   171|    std::uint8_t severity = 0;
---
   182|//
   183|// Allocation-free and O(1) when a slot is free, O(kNoiseCap) when it must pick a
   184|// victim to evict. Never grows.
>  185|std::uint32_t noise_publish(NoiseField& field, LayerId layer, const vec3& pos,
   186|                            const NoiseProfile& p, std::uint32_t actor);
   187|
   188|// Age every live noise by `dtMs` and free the expired. O(kNoiseCap), branch-light,
---
   221|// column in `data/weapons_ranged.csv` — one column, one generator change, and this
   222|// function reads the row instead of computing it. That was not bundled in here
   223|// because touching a generated table mid-fan-out is how merges break.
>  224|NoiseProfile weapon_fire_noise(const RangedDef& d);
   225|
   226|// A body hitting the floor. Quiet and short: severity 2 is the investigation
   227|// threshold, so a death is worth one look and no more. Not in the reference as a
---
   228|// noise source at all — it publishes `entity_died` as an EVENT and several
   229|// behaviours react to that — so this is the honest minimum that makes a kill
   230|// audible without inventing a loudness for it.
>  231|NoiseProfile body_fall_noise();
   232|
   233|// A crate or safe being opened. The reference's `door` profile, non-hermetic,
   234|// non-quiet: radius 7, ttl 2.2 s, severity 2. A container lid and a door are the
---

=== call sites across src+tests ===
  src\app\main.cpp noise_publish 11
  src\app\main.cpp noise_step 1
  src\app\main.cpp noise_clear 3
  src\game\combat.cpp noise_publish 2
  src\game\combat.cpp weapon_fire_noise 1
  src\game\combat.cpp body_fall_noise 1
  src\game\container.cpp noise_publish 1
  src\game\faction_relations.cpp noise_publish 1
  src\game\noise.cpp noise_publish 1
  src\game\noise.cpp weapon_fire_noise 1
  src\game\noise.cpp body_fall_noise 1
  src\game\noise.cpp noise_audible 1
  src\game\noise.cpp noise_step 1
  src\game\noise.cpp noise_clear 1
  src\game\noise.h noise_publish 1
  src\game\noise.h weapon_fire_noise 1
  src\game\noise.h body_fall_noise 1
  src\game\noise.h noise_audible 1
  src\game\noise.h noise_step 1
  src\game\noise.h noise_clear 1
  tests\suite_noise.inl noise_publish 21
  tests\suite_noise.inl weapon_fire_noise 1
  tests\suite_noise.inl body_fall_noise 1
  tests\suite_noise.inl noise_audible 3
  tests\suite_noise.inl noise_step 8
  tests\suite_noise.inl noise_clear 1
=== src/game/combat.cpp lines 1364
   157|
   158|std::uint32_t finalize_deaths(Registry& reg, NpcPool& pool, EventBus& bus,
>  159|                              std::uint64_t tick, NoiseField* noise) {
   160|    // Collect before destroying: mutating while iterating a view invalidates it.
   161|    std::vector<Entity> doomed;
---
   167|        // A body hitting the floor is audible. Published BEFORE the destroy, for the
   168|        // same reason the NpcDied event is: afterwards there is no Transform to read.
>  169|        // The victim is credited as the noise's actor, not the killer — a monster must
   170|        // not skip investigating a corpse just because it did not make it.
   171|        if (noise)
---
   169|        // The victim is credited as the noise's actor, not the killer — a monster must
   170|        // not skip investigating a corpse just because it did not make it.
>  171|        if (noise)
   172|            if (const Transform* tr = reg.try_get<Transform>(e))
   173|                noise_publish(*noise, tr->layer, tr->pos, body_fall_noise(),
---
   171|        if (noise)
   172|            if (const Transform* tr = reg.try_get<Transform>(e))
>  173|                noise_publish(*noise, tr->layer, tr->pos, body_fall_noise(),
   174|                              static_cast<std::uint32_t>(entt::to_integral(e)));
   175|
---
   224|        // Everything that must react to a death reacts to this, never to noticing
   225|        // an entity has vanished.
>  226|        bus.publish(EventType::NpcDied, victim, kind,
   227|                    static_cast<std::uint32_t>(entt::to_integral(d.killer)), tick);
   228|
---
  1040|std::uint32_t player_ranged_step(Registry& reg, NpcPool& pool, LayerId layer,
  1041|                                 bool wantFire, float dt, std::uint64_t tick,
> 1042|                                 NoiseField* noise) {
  1043|    Entity shooter = entt::null;
  1044|    for (auto e : reg.view<const CameraTag, const Transform>()) {
---
  1159|    }
  1160|
> 1161|    // The shot is heard. ONE noise per trigger pull, not one per pellet: a shotgun
  1162|    // blast is one bang, and twelve records would also evict everything else in a
  1163|    // 64-slot field. Published at the SHOOTER's position rather than the muzzle so a
---
  1163|    // 64-slot field. Published at the SHOOTER's position rather than the muzzle so a
  1164|    // monster investigating the sound walks at the player and not at a point 1.7 m in
> 1165|    // front of him. [noise.h]
  1166|    if (noise)
  1167|        noise_publish(*noise, layer, tr.pos, weapon_fire_noise(*def),
---
  1164|    // monster investigating the sound walks at the player and not at a point 1.7 m in
  1165|    // front of him. [noise.h]
> 1166|    if (noise)
  1167|        noise_publish(*noise, layer, tr.pos, weapon_fire_noise(*def),
  1168|                      static_cast<std::uint32_t>(entt::to_integral(shooter)));
---
  1165|    // front of him. [noise.h]
  1166|    if (noise)
> 1167|        noise_publish(*noise, layer, tr.pos, weapon_fire_noise(*def),
  1168|                      static_cast<std::uint32_t>(entt::to_integral(shooter)));
  1169|
---
=== src/game/door.cpp lines 381
=== src/app/main.cpp lines 5100
  2004|        // floor's layer id and be heard there. [noise.h]
> 2005|        game::noise_clear(noiseField);
  2006|        currentSpec = spec_for_floor(currentFloor);
---
  2346|                // tick N+1 rather than racing the pass that fired it.
> 2347|                game::noise_step(noiseField,
  2348|                                 static_cast<std::uint32_t>(kSimDt * 1000.0f + 0.5f));
---
  2825|                        game::NoiseProfile footstepNoise{6.0f, 400, 1, game::NoiseSource::Footstep};
> 2826|                        game::noise_publish(noiseField, activeLayer, ppos, footstepNoise,
  2827|                                            static_cast<std::uint32_t>(entt::to_integral(player)));
---
  2867|                            game::NoiseProfile flashNoise{14.0f, 1800, 2, game::NoiseSource::Door};
> 2868|                            game::noise_publish(noiseField, activeLayer, tr.pos, flashNoise, static_cast<std::uint32_t>(entt::to_integral(me
  2869|                            if (reg.valid(player)) {
---
  2993|                                           game::NoiseSource::Door};
> 2994|                    game::noise_publish(noiseField, activeLayer,
  2995|                                        doorTick.lastBreakPos, np, 0);
---
  3005|                                           game::NoiseSource::Door};
> 3006|                    game::noise_publish(noiseField, activeLayer,
  3007|                                        doorTick.lastOpenPos, np, 0);
---
  3041|                                                   game::NoiseSource::Door};
> 3042|                            game::noise_publish(noiseField, activeLayer,
  3043|                                                doorPos, np, 0);
---
  3104|                                              game::NoiseSource::WeaponFire};
> 3105|                        game::noise_publish(noiseField, activeLayer,
  3106|                                            vec3{op.x, op.y, op.z}, np, 0);
---
  3142|                            game::NoiseProfile np{6.0f, 600, 1, game::NoiseSource::Door};
> 3143|                            game::noise_publish(noiseField, activeLayer, ppos, np, 0);
  3144|                        }
---
  3166|                                game::NoiseProfile np{12.0f, 2000, 3, game::NoiseSource::Door};
> 3167|                                game::noise_publish(noiseField, activeLayer, tres.propPos, np, 0);
  3168|                            }
---
  3196|                                        game::NoiseProfile np{16.0f, 2500, 3, game::NoiseSource::Door};
> 3197|                                        game::noise_publish(noiseField, activeLayer, sp, np, 0);
  3198|                                        break;
---
  3221|                                        game::NoiseProfile np{5.0f, 500, 1, game::NoiseSource::Door};
> 3222|                                        game::noise_publish(noiseField, activeLayer, ppos, np, 0);
  3223|                                    }
---
  3260|                            game::NoiseProfile np{15.0f, 1500, 3, game::NoiseSource::Door};
> 3261|                            game::noise_publish(noiseField, activeLayer, newPos, np, 0);
  3262|                        }
---
  3663|                            rumourAt = 0;
> 3664|                            game::noise_clear(noiseField);
  3665|                            // Arrival order is load-path law: containers before
---
  4239|                                       "  | %u live, %u investigating",
> 4240|                                       game::noise_source_name(
  4241|                                           static_cast<game::NoiseSource>(ln->source)),
---
  5032|                        // there. [noise.h]
> 5033|                        game::noise_clear(noiseField);
  5034|                        // Same place_body_safely as the keyboard ride path. Two travel
---

=== quest kill hooks ===
=== src/game/quest.h lines 509
   143|//     job — rolled from a hash and gone if not kept. A quest offer is just a `QuestId`
   144|//     naming a row that is already on disk, so there is nothing to hold: the caller
   145|//     keeps the id, and the log stays untouched until accept.
   146|//   * `Failed` splits into `Expired` and `Orphaned`, because only one of them is the
>  147|//     player's fault and they must not clear the same way. See `quest_on_giver_died`.
   148|enum class QuestState : std::uint8_t {
   149|    Unseen = 0,   // never taken
   150|    Active,
   151|    Complete,     // conditions met, reward paid
---
   150|    Active,
   151|    Complete,     // conditions met, reward paid
   152|    Expired,      // the deadline ran out. TERMINAL, and the point of the feature.
   153|    // The giver died before you delivered. Counted as a failure — and takeable again,
>  154|    // which is the one place quests deviate from contracts. See quest_on_giver_died.
   155|    Orphaned,
   156|    Count
   157|};
   158|
---
   163|// `Contract::subject` is, and unused for Descend. That double meaning is a known save
   164|// hazard ([save.h] says so at length); it is repeated here rather than fixed because
   165|// fixing it means interning string ids across `ItemSlot` and `Contract` too, and one
   166|// half-migrated id space would be worse than one consistent hazard.
>  167|struct QuestDef {
   168|    std::uint32_t limitMs;      //  0  deadline; 0 = none (see the banner)
   169|    std::int32_t target;        //  4  count, or DEPTH |z| for Descend
   170|    std::int32_t reward;        //  8  roubles, paid into RunLedger::banked
   171|    std::uint16_t subject;      // 12  ItemId (Fetch) / MobKind (Hunt) / unused
---
   202|    return kQuestBriefs[static_cast<std::size_t>(id) - 1];
   203|}
   204|
   205|// Per-row run state. 16 bytes, POD, no pointers.
>  206|struct QuestProgress {
   207|    // Milliseconds left on the clock. Meaningful only while `state == Active` and only
   208|    // when the row's `limitMs != 0` — the DEF decides whether there is a deadline at
   209|    // all, so a zero here can never be mistaken for "already expired" by a load.
   210|    std::uint32_t remainingMs = 0;
---
   215|    //
   216|    // It used to be a bare `NpcId`, resting on [npc_pool.h] never-reclaim. That is the
   217|    // DEFAULT policy and not a property of the type: `set_recycling(true)` suspends it,
   218|    // and a recycled id is a REUSED id. The hole was silent rather than loud, exactly as
>  219|    // it was for `Contract::giver` before the same migration: `quest_on_giver_died` fires
   220|    // only from the frame-top combat drain, so a giver killed by the MACRO sweep publishes
   221|    // no event, the slot goes to a newborn, and `quest_step`'s poll — then
   222|    // `!valid(id) || !alive(id)` — saw a living record at that index, kept the row, and
   223|    // paid an authored quest out to a stranger who never offered it. Which is the
---
   217|    // DEFAULT policy and not a property of the type: `set_recycling(true)` suspends it,
   218|    // and a recycled id is a REUSED id. The hole was silent rather than loud, exactly as
   219|    // it was for `Contract::giver` before the same migration: `quest_on_giver_died` fires
   220|    // only from the frame-top combat drain, so a giver killed by the MACRO sweep publishes
>  221|    // no event, the slot goes to a newborn, and `quest_step`'s poll — then
   222|    // `!valid(id) || !alive(id)` — saw a living record at that index, kept the row, and
   223|    // paid an authored quest out to a stranger who never offered it. Which is the
   224|    // reference's own broken quest binding that contract.h opens by claiming this engine
   225|    // is immune to.
---
   262|//
   263|// **There is deliberately no cap on concurrent active quests.** The deadline IS the
   264|// limiter, and it is a better one than a slot count: it charges you for over-committing
   265|// instead of merely forbidding it.
>  266|struct QuestLog {
   267|    QuestProgress row[kQuestCount]{};
   268|    std::int64_t earned = 0;              // roubles this log has paid out
   269|    std::uint32_t completed = 0;
   270|    std::uint32_t failed = 0;             // expired + orphaned
---
   270|    std::uint32_t failed = 0;             // expired + orphaned
   271|    std::uint32_t expired = 0;            // ...of which the clock killed
   272|    std::uint32_t orphaned = 0;           // ...of which a dead giver released
   273|    // Reward items that did not fit in the bag and were lost. Counted rather than
>  274|    // hidden: `quest_step` grants what fits and completes anyway, because blocking
   275|    // completion on a full inventory would leave an Active row the player has already
   276|    // finished, and the deadline would then kill it. A silent loss is worse than a
   277|    // counted one. (The reference blocks instead — `questRewardsFitAfterHandoff`.)
   278|    std::uint32_t rewardItemsLost = 0;
---
   336|// else to live. Passing the pool makes forgetting to mint unexpressible — the same
   337|// argument the LEDGER paragraph below makes for `baseline` — and it is what lets a giver
   338|// who died between offer and accept be REFUSED rather than minted from a corpse:
   339|// `pool.handle()` on a dead record returns the already-bumped generation, so accepting
>  340|// one would write an Active row that `quest_step` orphans on the very next tick, billing
   341|// the run a `failed` for a job it never had. `quest_step`'s liveness poll still has to
   342|// exist regardless, because a body can be lost to a floor unload rather than to a death
   343|// and only one of those publishes.
   344|//
---
   337|// argument the LEDGER paragraph below makes for `baseline` — and it is what lets a giver
   338|// who died between offer and accept be REFUSED rather than minted from a corpse:
   339|// `pool.handle()` on a dead record returns the already-bumped generation, so accepting
   340|// one would write an Active row that `quest_step` orphans on the very next tick, billing
>  341|// the run a `failed` for a job it never had. `quest_step`'s liveness poll still has to
   342|// exist regardless, because a body can be lost to a floor unload rather than to a death
   343|// and only one of those publishes.
   344|//
   345|// The Descend clause is contract_accept's guard, ported for the same measured reason: the
---
   376|// One test for three distinct ways the person can stop being the person: an id past the
   377|// high-water mark, a cleared alive bit, and — the one a bare id could never see — a stale
   378|// GENERATION, i.e. the slot was reclaimed and handed to somebody else. Deaths in the macro
   379|// sweep publish no `NpcDied` event ([macro_sim.h]), so for those this poll is the ONLY
>  380|// guard; `quest_on_giver_died` is a fast path, not the guarantee.
   381|//
   382|// **Fetch consumes the items**, for contract.cpp's reason: a courier job that let you
   383|// keep the cargo would pay twice for the same loot.
   384|std::int32_t quest_step(QuestLog& log, const NpcPool& pool, Inventory& inv,
---
   380|// guard; `quest_on_giver_died` is a fast path, not the guarantee.
   381|//
   382|// **Fetch consumes the items**, for contract.cpp's reason: a courier job that let you
   383|// keep the cargo would pay twice for the same loot.
>  384|std::int32_t quest_step(QuestLog& log, const NpcPool& pool, Inventory& inv,
   385|                        RunLedger& led, std::uint32_t stepMs);
   386|
   387|// A kill happened. Same event, same payload, same drain as `contract_on_kill` — main
   388|// already reads `NpcDied` off the bus once per frame and this hooks the same batch
---
   386|
   387|// A kill happened. Same event, same payload, same drain as `contract_on_kill` — main
   388|// already reads `NpcDied` off the bus once per frame and this hooks the same batch
   389|// rather than inventing a second notion of "something died".
>  390|void quest_on_kill(QuestLog& log, std::uint8_t mobKind);
   391|
   392|// A giver died. Their active quests become `Orphaned`.
   393|//
   394|// **This is where quests deviate from contracts, and the deviation is the point.**
---
   422|// function refuses that value outright anyway.
   423|//
   424|// This is a fast path, not the guard, and the asymmetry is the reason: it fires only for a
   425|// death that publishes, i.e. the combat drain, never for a macro-sweep death.
>  426|// `quest_step`'s `handle_valid` poll is what actually catches a dead giver.
   427|void quest_on_giver_died(QuestLog& log, NpcId who);
   428|
   429|// ---------------------------------------------------------------------------
   430|// Rewards
---
   423|//
   424|// This is a fast path, not the guard, and the asymmetry is the reason: it fires only for a
   425|// death that publishes, i.e. the combat drain, never for a macro-sweep death.
   426|// `quest_step`'s `handle_valid` poll is what actually catches a dead giver.
>  427|void quest_on_giver_died(QuestLog& log, NpcId who);
   428|
   429|// ---------------------------------------------------------------------------
   430|// Rewards
   431|// ---------------------------------------------------------------------------
---
=== src/app/main.cpp lines 5100
  1395|    // broken — I made that mistake twice and was corrected twice:
  1396|    //   DONE  MacroSim::Journey::id  — stamps the departing generation, compares on landing.
> 1397|    //   DONE  Contract::giver        — an NpcHandle; contract_step polls handle_valid().
  1398|    //                                  Measured A/B: with a bare id the job paid 700 rub to
  1399|    //                                  a newborn who never offered it and read Complete;
---
  2077|                // `b` is the mob kind, 0xFF when the dead thing was not a monster.
  2078|                if (ev.b != 0xFFu)
> 2079|                    game::contract_on_kill(contracts, static_cast<std::uint8_t>(ev.b));
  2080|                // `a` is the pool id, kInvalidNpc when the dead thing had no record.
  2081|                if (ev.a != game::kInvalidNpc)
---
  3821|                    if (const auto* nrc = reg.try_get<game::NpcRef>(player))
  3822|                        if (pool.valid(nrc->id)) {
> 3823|                            contractPaid += game::contract_step(
  3824|                                contracts, pool, pool.inventory(nrc->id), ledger);
  3825|                            questPaid += game::quest_step(
---
  3823|                            contractPaid += game::contract_step(
  3824|                                contracts, pool, pool.inventory(nrc->id), ledger);
> 3825|                            questPaid += game::quest_step(
  3826|                                quests, pool, pool.inventory(nrc->id), ledger,
  3827|                                static_cast<std::uint32_t>(kSimDt * 1000.0f + 0.5f));
---
=== src/game/investigate.h lines 113
    17|// velocity of the mobs that heard something and are not already in sight-aggro.
    18|//
    19|// That is a deliberate choice about blast radius, not laziness. Hoisting the seen
    20|// half up here would be a refactor of the one steering path that is proven, inside
    21|// a change whose actual job is to add a missing primitive — and the two would then
>   22|// have to be verified together instead of separately. As an override, `investigate_step`
    23|// is purely additive: delete the call and the game is exactly what it was.
    24|//
    25|// The honest cost of that shape: two passes over the mobs on the layer instead of
    26|// one. It is paid only when something is making a noise — `investigate_step` returns on
    27|// `NoiseField::quiet()`, which is true on almost every tick, before it looks at a
---
    21|// a change whose actual job is to add a missing primitive — and the two would then
    22|// have to be verified together instead of separately. As an override, `investigate_step`
    23|// is purely additive: delete the call and the game is exactly what it was.
    24|//
    25|// The honest cost of that shape: two passes over the mobs on the layer instead of
>   26|// one. It is paid only when something is making a noise — `investigate_step` returns on
    27|// `NoiseField::quiet()`, which is true on almost every tick, before it looks at a
    28|// single entity. The stagger predicate is deliberately IDENTICAL to
    29|// `wander_step`'s, so a mob's hearing decision lands on the same tick as the
    30|// velocity it is overriding rather than one visit behind it.
    31|//
---
    93|// Should this mob go and look at something? Answers no for a behaviour that is
    94|// authored deaf, and no when nothing audible passes the severity gate.
    95|//
    96|// `mobId` is the mob's own `entt::to_integral`, so it cannot investigate a noise it
    97|// made itself (13 of the 69 kinds shoot).
>   98|InvestigateHeard investigate_hear(const NoiseField& field, MobBehaviour beh, LayerId layer,
    99|                    const vec3& mobPos, std::uint32_t mobId);
   100|
   101|// One staggered investigation pass. Overwrites horizontal velocity for every mob on
   102|// `layer` that heard something and is not in sight-aggro; leaves every other mob
   103|// exactly as `wander_step` left it. Returns the number of mobs steered by sound.
---
   105|// Belongs immediately AFTER `wander_step` and BEFORE `physics_step`: after, because
   106|// it overrides; before, because physics is what turns the velocity into movement.
   107|//
   108|// `pool` is read only to resolve the camera holder's faction, for the same reason
   109|// `wander_step` reads it — the sight test must be the same test.
>  110|std::uint32_t investigate_step(Registry& reg, const NoiseField& field, NpcPool& pool,
   111|                        LayerId layer, std::uint64_t tick);
   112|
   113|} // namespace giga::game
---
=== src/game/player_command.h lines 102
>    1|// PlayerCommand — the client→server input seam (netcode.md increment #1).
---
>    2|//
---
>    3|// The one payload that crosses UP across the client/server boundary: a client's
---
>    4|// movement + look + button intent for a single tick, shaped like a Source engine
---
>    5|// *usercmd*. It is a flat, trivially-copyable, versioned POD so the future
---
>    6|// `NetworkConnection` (netcode.md) serializes it with a memcpy rather than a graph
---
>    7|// walk — the same dense-data discipline as the rest of the engine.
---
>    8|//
---
>    9|// WHY THIS LIVES IN giga_game, NOT src/input
---
>   10|// ------------------------------------------
---
>   11|// netcode.md rule #1: "the client never mutates authoritative state" — no system
---
>   12|// writes a component the sim reads except THROUGH a PlayerCommand applied on the
---
>   13|// server. So the *application* (`apply_player_command`) is server-side and headless
---
>   14|// (no SDL), which is what lets it be unit-tested in `game_test` with no window —
---
>   15|// the doc's stated win. The SDL input bridge (src/input) only *builds* a command
---
>   16|// from devices and hands it to this apply step; on a `LocalConnection` (today's
---
>   17|// single-player listen server) that happens the same tick, so the feel is
---
>   18|// byte-identical to the old direct-write path.
---
>   19|//
---
>   20|// OWNERSHIP IS PER-COMMAND, NOT GLOBAL (netcode.md rule #4). `apply_player_command`
---
>   21|// takes the avatar entity explicitly; there is no "the player" singleton. N
---
>   22|// sessions each own an embodied entity and each apply their own command — the
---
>   23|// engine already forbids a player singleton (npcs.md), so multi-client needs no
---
>   24|// special case here.
---
>   25|#pragma once
---
>   26|#include <cstdint>
---
>   28|#include "core/math.h"
---
>   29|#include "ecs/registry.h"
---
>   31|namespace giga::game {
---
>   33|// Button intents, one bit each. The FULL contract per netcode.md is declared here
---
>   34|// so the POD is the complete seam and later increments only add *producers* — but
---
>   35|// increment #1 populates only Jump + FlyToggle, which is everything the SDL input
---
>   36|// bridge (src/input) owned directly before this. The remaining bits are the game's
---
>   37|// existing intent flags in main.cpp (eat/drink/heal/sell/buy/door/save/load,
---
>   38|// elevator up/down, attack/use/interact); routing those through the command is a
---
>   39|// later increment, so they are reserved rather than wired to keep this change
---
>   40|// additive and behaviour-identical.
---
>   41|enum class Button : std::uint32_t {
---
>   42|    None         = 0,
---
>   43|    Jump         = 1u << 0,
---
>   44|    FlyToggle    = 1u << 1,
---
>   45|    // Reserved (declared, not yet produced) — the game-layer intents main.cpp still
---
>   46|    // owns as its own flags today. Named now so the wire format is stable.
---
>   47|    Attack       = 1u << 2,
---
>   48|    Use          = 1u << 3,
---
>   49|    Interact     = 1u << 4,
---
>   50|    ElevatorUp   = 1u << 5,
---
>   51|    ElevatorDown = 1u << 6,
---
>   52|    Eat          = 1u << 7,
---
>   53|    Drink        = 1u << 8,
---
>   54|    Heal         = 1u << 9,
---
>   55|    Sell         = 1u << 10,
---
>   56|    Buy          = 1u << 11,
---
>   57|    Door         = 1u << 12,
---
>   58|    Save         = 1u << 13,
---
>   59|    Load         = 1u << 14,
---
>   60|};
---
>   62|// Bit helpers, mirroring the AiFlag pattern in mob_table.h (RTTI-free, constexpr).
---
>   63|constexpr std::uint32_t bit(Button b) noexcept {
---
>   64|    return static_cast<std::uint32_t>(b);
---
>   65|}
---
>   66|constexpr bool has_button(std::uint32_t buttons, Button b) noexcept {
---
>   67|    return (buttons & bit(b)) != 0u;
---
>   68|}
---
>   70|// One client's intent for one tick. Trivially-copyable + standard-layout (asserted
---
>   71|// in player_command.cpp) so the wire format is a memcpy.
---
>   72|//
---
>   73|// Look angles are ABSOLUTE (Source's usercmd view angles), not deltas: the client
---
>   74|// owns its running view angle and sends the resolved value; the server assigns it
---
>   75|// and clamps pitch (never trusting the client — server-authoritative clamp). When
---
>   76|// mouselook is off, the client seeds these from the avatar's current camera and
---
>   77|// applies no delta, so `apply_player_command` writes them back unchanged — a no-op,
---
>   78|// which is exactly the old frozen-view behaviour.
---
>   79|struct PlayerCommand {
---
>   80|    static constexpr std::uint16_t kVersion = 1;
---
>   82|    std::uint16_t version = kVersion; // bumped when the layout changes
---
>   83|    std::uint16_t pad_ = 0;           // explicit — keeps the u32s aligned on the wire
---
>   84|    std::uint32_t buttons = 0;        // OR of Button bits
---
>   85|    std::uint32_t clientTick = 0;     // client's tick number, for ordering/acks
---
>   87|    vec3 wishDir{0, 0, 0}; // camera-local move intent (fwd, right, up), each [-1,1]
---

=== status mult dead helpers ===
status_water_drain_e3 src\game\status.cpp 1
status_water_drain_e3 src\game\status.h 1
status_water_drain_e3 tests\suite_status.inl 3
status_heal_mult_e3 src\game\status.cpp 1
status_heal_mult_e3 src\game\status.h 1
status_melee_mult_e3 src\app\main.cpp 1
status_melee_mult_e3 src\game\status.cpp 1
status_melee_mult_e3 src\game\status.h 1

=== ARCHITECTURE 280-360 ===
280:    универсальна, остальная ситуативна).
281:    Монстры — без инвентаря/атрибутов, чисто таблица (hp/урон/скорость/типы).
282:    **Урон рушит мир**: тип урона × материал = вероятность сноса субвокселя
283:    (расширение carve-ролла [destruct.md](destruct.md); у материала НЕТ HP —
284:    очередь из автомата лишь царапает краску). **Инвентарь** 8×8 + вес (50 кг
285:    база) + слоты оружие/броня/пси/инструменты (заряд/прочность — фонарик).
286:    **Нужды тикают У ВСЕХ воплощённых** — держатель камеры ничем не особенный
287:    (тот же закон, что «игрок не специален»).
288:    **Снаряды — честная баллистика в торе**, никаких friendly-fire-исключений;
289:    граната скачет по вокселям, осколки бьют и владельца. Смерть — дроп
290:    инвентаря на этаж (маркеры сюжетных квестов переживают смерть
291:    квестодателя на дропе). **Смерть игрока (РЕШЕНО)**: главное меню
292:    (можно грузиться) ИЛИ переселение в случайного NPC макропопуляции
293:    (твой труп с лутом остаётся на этаже); респавнов НЕТ. XP — за убийства
294:    (таблицы + контекст уровня) и квесты (линейная сюжетная цепочка +
295:    процедурные квесты/ивенты). |absN| этажа = уровень опасности → монстры,
296:    лут, уровень жителей.
297: 6. **Технические константы цели.** **16k воплощённых — КОНСТАНТА**, на
298:    которую ориентируется вся игра; 1024³ разрушаемый живой изотропный
299:    этаж — техническая цель. **Запекать на загрузке — МНОГОПОТОЧНО**: всё,
300:    что можно генерить/запекать параллельно с последующей сшивкой — делать
301:    так (результат статичен, синхронизации нет — бесплатная скорость
302:    загрузки; без фанатизма, но по умолчанию — да).
303: 
304: Старый прототип `/Users/jirnyak/Mirror/gigahrush` — справочник решений
305: (интеракторы навешиваются на пропы/фичи этапом генерации, сшивка
306: самосбора, квесты) — но не образец кода.
307: 
308: ### Конфликты и зазоры манифест ↔ код (сверено 2026-08-01)
309: 
310: | Манифест | Сегодня в коде | Статус |
311: |---|---|---|
312: | 8 атрибутов | `Attr{Str,Agi,Int}` ([rpg.h](src/game/rpg.h)); пул УЖЕ держит 8 слотов (3..7 свободны, засеяны) | **КОНФЛИКТ**: расширить enum + `RpgStats.attr[
313: | Пси | `kBasePsi = 100`, +1/уровень, max от INT +1% | **РЕШЕНО**: пул 100 остаётся; владелец max-пси — воля +1% (не INT); INT% → пси-урон; реген только при пол
314: | Перки (Fallout/Underrail: статы → события → мир) | нет системы (XP-кривая и очки атрибутов есть) | **GAP**: таблица + гейты + эффекты-модификаторы + хуки чере
315: | Типы урона/брони | в данных УЖЕ 5 резистов: kinetic/buckshot/energy/fire/psi (items.csv), `damageType` частично в combat | **ПОЧТИ**: сверить номенклатуру (ко
316: | Тип урона × материал | carve: один скаляр power vs hardness | **GAP**: колонки типов в [material_props.h](src/world/material_props.h) — data-driven, без HP |
317: | Инвентарь 8×8 + слоты | `kInvSlots = 64 = 8×8`, `equip_slot` в items.csv | **СОВПАДАЕТ** ✓ |
318: | Вес (50 кг + выносливость) | в items.csv нет колонки веса, системы нет | **GAP** (CSV + генератор + гейт) |
319: | Нужды еда/вода/туалет/сон | `Needs{food,water,sleep,pee,poo}` + needs_step | **ЧАСТИЧНО**: структура ✓, но тикает только у держателя камеры — РЕШЕНО: тик у ВС
320: | Игрок = NPC, possess | `NpcPlayer`-бит, `possess` | **СОВПАДАЕТ** ✓ |
321: | Макропопуляция = источник истины, fold-back | streamer/embody/fold-back, сейв несёт пул verbatim | **СОВПАДАЕТ** ✓ |
322: | Комнаты-зоны как поля 128³ | комнаты в floor_gen, `spawn_rooms` в лут-таблице; зонального поля для AI-нужд нет | **ЧАСТИЧНО** |
323: | Гермо-комнаты неразрушимые | механизм `kHardnessUnbreakable` есть; жилых гермозон нет | **ЧАСТИЧНО** |
324: | Лифты 4×4 fast + 4×4↓ + 4×4↑ столбами | hub-пады на z-уровнях {16,48,80,112} + extract-пады + [/]-ride | **КОНФЛИКТ раскладки** (см. [elevators.md](elevators.
325: | Самосбор-перестройка + сшивка + ребейк | сирена/туман/duty/варианты есть; волновой перестройки геометрии НЕТ | **GAP, дизайн ЗАФИКСИРОВАН** (GPU-стенсил + op-
326: | Баллистика снарядов | `projectile_step` + carve от попаданий | **СОВПАДАЕТ** ✓ (гранаты — проверить) |
327: | Квесты: линейка + процедурные | quests.csv + suite_quest; процедурные/ивенты частично ([events.md](events.md)) | **ЧАСТИЧНО** |