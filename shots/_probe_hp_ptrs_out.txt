
===== src/game/combat.cpp =====
  13|#include "game/mob_behaviour.h"
  14|#include "game/mob_table.h"
  15|#include "game/monster_traits.h"
  16|#include "game/ranged_table.h"
>>17|#include "game/rpg.h"      // RpgStats, xp_for_monster_kill, award_xp
  18|#include "game/weapon_table.h"
  19|#include "sim/camera.h"   // camera_forward
  20|#include "world/macro_grid.h"
  21|#include "world/world.h"
---
  61|        if (!pool.valid(n->id)) return false;
  62|        // const_cast: NpcPool's accessors are non-const by design (it is an SoA
  63|        // table meant to be written through), but reading is genuinely const here.
  64|        NpcPool& p = const_cast<NpcPool&>(pool);
>>65|        hp = p.hp(n->id);
  66|        maxHp = p.max_hp(n->id);
  67|        return true;
  68|    }
  69|    return false;
---
  62|        // const_cast: NpcPool's accessors are non-const by design (it is an SoA
  63|        // table meant to be written through), but reading is genuinely const here.
  64|        NpcPool& p = const_cast<NpcPool&>(pool);
  65|        hp = p.hp(n->id);
>>66|        maxHp = p.max_hp(n->id);
  67|        return true;
  68|    }
  69|    return false;
  70|}
---
  116|    std::int16_t* hp = nullptr;
  117|    if (MobRef* m = reg.try_get<MobRef>(target)) {
  118|        hp = &m->hp;
  119|    } else if (NpcRef* n = reg.try_get<NpcRef>(target)) {
>>120|        if (pool.valid(n->id)) hp = &pool.hp(n->id);
  121|    }
  122|    if (!hp) return out;  // nothing to damage; `hit` stays false
  123|
  124|    out.hit = true;
---
  212|                // max-HP growth is credited there. A killer with no record keeps
  213|                // the level and the point and simply has no HP to raise.
  214|                const NpcRef* kn = reg.try_get<NpcRef>(d.killer);
  215|                if (kn != nullptr && pool.valid(kn->id))
>>216|                    award_xp(*kr, gain, &pool.hp(kn->id), &pool.max_hp(kn->id));
  217|                else
  218|                    award_xp(*kr, gain);
  219|            }
  220|        }
---
  214|                const NpcRef* kn = reg.try_get<NpcRef>(d.killer);
  215|                if (kn != nullptr && pool.valid(kn->id))
  216|                    award_xp(*kr, gain, &pool.hp(kn->id), &pool.max_hp(kn->id));
  217|                else
>>218|                    award_xp(*kr, gain);
  219|            }
  220|        }
  221|
  222|        // Published BEFORE the entity goes away, so a listener can still read it.
---

===== src/app/main.cpp =====
  1820|        runState.player.clock = pool.needs(nr->id);
  1821|        runState.player.inv = pool.inventory(nr->id);
>>1822|        runState.player.hp = pool.hp(nr->id);
  1823|        runState.player.maxHp = pool.max_hp(nr->id);
  1824|        // The SIGNED floor, explicitly: NpcPool::floor() is the seeding label
---
  1821|        runState.player.inv = pool.inventory(nr->id);
  1822|        runState.player.hp = pool.hp(nr->id);
>>1823|        runState.player.maxHp = pool.max_hp(nr->id);
  1824|        // The SIGNED floor, explicitly: NpcPool::floor() is the seeding label
  1825|        // and LayerId is a recycled storage slot. [save.h]
---
  2683|                        const float* fluidData = giga::fluid_data(stack.layer(activeLayer));
  2684|                        if (game::pos_wet(fluidData, tr.pos)) {
>>2685|                            mr.hp = std::min<std::int16_t>(mr.maxHp, mr.hp + static_cast<std::int16_t>(regenRate * 0.13f + 0.5f));
  2686|                            if (particlePass.ready()) {
  2687|                                particlePass.emit_burst(tr.pos + vec3{0.0f, 0.5f, 0.0f}, vec3{0.0f, 0.5f, 0.0f},
---
  3279|                }
  3280|                needs = game::needs_step(reg, pool, activeLayer, kSimDt);
>>3281|                needsHpLost += needs.hpLost;
  3282|                // Corpses pay out BEFORE they are destroyed. The gap between
  3283|                // "hp hit zero" and "gone" is precisely what the Dead tag exists
---
  3648|                        drinkWanted = false;
  3649|                    }
>>3650|                    if (cr.used && cr.hpCost > 0 && reg.valid(player)) {
  3651|                        game::apply_damage(reg, pool, player, cr.hpCost,
  3652|                                           game::DamageChannel::Kinetic, player);
---
  3649|                    }
  3650|                    if (cr.used && cr.hpCost > 0 && reg.valid(player)) {
>>3651|                        game::apply_damage(reg, pool, player, cr.hpCost,
  3652|                                           game::DamageChannel::Kinetic, player);
  3653|                        consumeHpCost += cr.hpCost;
---
  3651|                        game::apply_damage(reg, pool, player, cr.hpCost,
  3652|                                           game::DamageChannel::Kinetic, player);
>>3653|                        consumeHpCost += cr.hpCost;
  3654|                    }
  3655|                }
---
  3989|                        const game::MobRef& m = reg.get<const game::MobRef>(me_);
  3990|                        threat = game::mob_name(static_cast<game::MobKind>(m.kind));
>>3991|                        thp = m.hp;
  3992|                        tmax = m.maxHp;
  3993|                    }
---
  3990|                        threat = game::mob_name(static_cast<game::MobKind>(m.kind));
  3991|                        thp = m.hp;
>>3992|                        tmax = m.maxHp;
  3993|                    }
  3994|                }
---

===== src/game/npc_pool.h =====
  77|// An id is 20 bits (kNpcPoolBits) inside a 32-bit word, so 12 bits are already spare
  78|// and NO save format has to move to make room: [save.h] serializes the player's ROW
>>79|// (needs / inventory / hp / floor / cell), never an id, and states in as many words
  80|// that the pool itself is not in the format because it is reproduced by
  81|// `seed_floor_population`. So the generation column is free of the wire.
---
  149|
  150|// The per-body survival clock — food/water/sleep and the two pressures. Lives in
>>151|// the pool row for the same reason hp does: an elevator ride DESTROYS the
  152|// player's body and builds a new one (`fold_back` -> `embody_as_player`,
  153|// [embody.h]), so an ECS component would silently reset the clock on every floor
---
  162|    float food = 0.0f;        // 0..100 reserve, drains; 0 = starving
  163|    float water = 0.0f;       // 0..100 reserve, drains; 0 = dehydrated
>>164|    float sleep = 0.0f;       // 0..100 reserve, drains; costs speed, never hp
  165|    float pee = 0.0f;         // 0..100 pressure, fills; 100 = overflowing
  166|    float poo = 0.0f;         // 0..100 pressure, fills; 100 = overflowing
---
  167|    float pendingPee = 0.0f;  // drunk/eaten but not yet digested into `pee`
  168|    float pendingPoo = 0.0f;  // ditto for `poo`
>>169|    float hpDebt = 0.0f;      // sub-1-HP attrition carried between ticks
  170|    std::uint8_t seeded = 0;  // 0 = never rolled (see above)
  171|    std::uint8_t pad_[3] = {};
---
  211|//
  212|// Measured row widths: inv_ 256, rel_ 128, needs_ 36, name_ 24, surname_ 24, attr_ 8,
>>213|// faction_/hp_/maxHp_/floor_/heightMm_ 2 each, flags_/cx_/cy_/cz_/age_/sex_/level_ 1
  214|// each = 493 B when this policy was written. Slot recycling added two more: gen_ 2 B
  215|// (LIVE) and nextFree_ 4 B (DEMAND, and only when recycling is actually on), for 499 B
---
  223|// Three policies, chosen per column by who actually touches it:
  224|//
>>225|//   EAGER  — flags_, faction_, hp_, maxHp_, floor_, cx_/cy_/cz_, heightMm_, inv_,
  226|//            needs_. Read on live paths (combat, loot, needs, embody, wander), so
  227|//            they keep the old "sized once at init(), never resized" contract and a
---
  466|    std::uint8_t&  flags(NpcId id)   { return flags_[id]; }
  467|    std::uint16_t& faction(NpcId id) { return faction_[id]; }
>>468|    std::int16_t&  hp(NpcId id)      { return hp_[id]; }
  469|    std::int16_t&  max_hp(NpcId id)  { return maxHp_[id]; }
  470|
---
  467|    std::uint16_t& faction(NpcId id) { return faction_[id]; }
  468|    std::int16_t&  hp(NpcId id)      { return hp_[id]; }
>>469|    std::int16_t&  max_hp(NpcId id)  { return maxHp_[id]; }
  470|
  471|    // SIGNED, and that is load-bearing: the building descends, so the demo stack's
---
  566|    std::vector<std::uint8_t>  flags_;
  567|    std::vector<std::uint16_t> faction_;
>>568|    std::vector<std::int16_t>  hp_;
  569|    std::vector<std::int16_t>  maxHp_;
  570|    std::vector<std::int16_t>  floor_;  // signed logical floor label ([floors.md])
---
  567|    std::vector<std::uint16_t> faction_;
  568|    std::vector<std::int16_t>  hp_;
>>569|    std::vector<std::int16_t>  maxHp_;
  570|    std::vector<std::int16_t>  floor_;  // signed logical floor label ([floors.md])
  571|    std::vector<std::uint8_t>  cx_, cy_, cz_;  // macro cell within the floor
---
exists src/game/npc_pool.h

===== src/game/npc_pool.h =====
  78|// and NO save format has to move to make room: [save.h] serializes the player's ROW
>>79|// (needs / inventory / hp / floor / cell), never an id, and states in as many words
  80|// that the pool itself is not in the format because it is reproduced by
---
  97|inline constexpr std::uint32_t kNpcGenMask = (1u << kNpcGenBits) - 1u;    // 0xFFF
>>98|inline constexpr std::uint16_t kInvalidGen = static_cast<std::uint16_t>(kNpcGenMask);
  99|
---
  104|
>>105|inline constexpr NpcHandle npc_handle(NpcId id, std::uint16_t gen) {
  106|    return (static_cast<NpcHandle>(gen & kNpcGenMask) << kNpcPoolBits) |
---
  109|inline constexpr NpcId npc_handle_id(NpcHandle h) { return h & kNpcIdMask; }
>>110|inline constexpr std::uint16_t npc_handle_gen(NpcHandle h) {
  111|    return static_cast<std::uint16_t>((h >> kNpcPoolBits) & kNpcGenMask);
---
  110|inline constexpr std::uint16_t npc_handle_gen(NpcHandle h) {
>>111|    return static_cast<std::uint16_t>((h >> kNpcPoolBits) & kNpcGenMask);
  112|}
---
  120|// "on no floor": a spawned-but-unplaced record, or a corpse. INT16_MIN and not the
>>121|// branch 0xFFFF, because labels are SIGNED and 0xFFFF read back as int16 is -1 --
  122|// which is a LEGAL floor (FloorRegistry allows -127..127). A sentinel that collides
---
  123|// with real data is not a sentinel.
>>124|inline constexpr std::int16_t kNoFloorLabel = -32768;
  125|
---
  140|    NpcId target = kInvalidNpc;
>>141|    std::int16_t affinity = 0;  // signed valence, -32768..32767
  142|    std::uint16_t pad = 0;
---
  141|    std::int16_t affinity = 0;  // signed valence, -32768..32767
>>142|    std::uint16_t pad = 0;
  143|};
---
  150|// The per-body survival clock — food/water/sleep and the two pressures. Lives in
>>151|// the pool row for the same reason hp does: an elevator ride DESTROYS the
  152|// player's body and builds a new one (`fold_back` -> `embody_as_player`,
---
  163|    float water = 0.0f;       // 0..100 reserve, drains; 0 = dehydrated
>>164|    float sleep = 0.0f;       // 0..100 reserve, drains; costs speed, never hp
  165|    float pee = 0.0f;         // 0..100 pressure, fills; 100 = overflowing
---
  168|    float pendingPoo = 0.0f;  // ditto for `poo`
>>169|    float hpDebt = 0.0f;      // sub-1-HP attrition carried between ticks
  170|    std::uint8_t seeded = 0;  // 0 = never rolled (see above)
---
  182|// no affinity sentinel: an empty edge is `target == kInvalidNpc`, so -128 is free.
>>183|inline constexpr std::int16_t kRelAffinityMin = -127;
  184|inline constexpr std::int16_t kRelAffinityMax =  127;
---
  183|inline constexpr std::int16_t kRelAffinityMin = -127;
>>184|inline constexpr std::int16_t kRelAffinityMax =  127;
  185|inline constexpr std::int16_t kRelHostile     =  -64;
---
  184|inline constexpr std::int16_t kRelAffinityMax =  127;
>>185|inline constexpr std::int16_t kRelHostile     =  -64;
  186|inline constexpr std::int16_t kRelFriendly    =   64;
---
  185|inline constexpr std::int16_t kRelHostile     =  -64;
>>186|inline constexpr std::int16_t kRelFriendly    =   64;
  187|
---
  212|// Measured row widths: inv_ 256, rel_ 128, needs_ 36, name_ 24, surname_ 24, attr_ 8,
>>213|// faction_/hp_/maxHp_/floor_/heightMm_ 2 each, flags_/cx_/cy_/cz_/age_/sex_/level_ 1
  214|// each = 493 B when this policy was written. Slot recycling added two more: gen_ 2 B
---
  224|//
>>225|//   EAGER  — flags_, faction_, hp_, maxHp_, floor_, cx_/cy_/cz_, heightMm_, inv_,
  226|//            needs_. Read on live paths (combat, loot, needs, embody, wander), so
---
  395|    // handle()/handle_valid() instead of comparing this by hand.
>>396|    std::uint16_t generation(NpcId id) const {
  397|        return id < gen_.size() ? gen_[id] : static_cast<std::uint16_t>(0);
---
  396|    std::uint16_t generation(NpcId id) const {
>>397|        return id < gen_.size() ? gen_[id] : static_cast<std::uint16_t>(0);
  398|    }
---
  466|    std::uint8_t&  flags(NpcId id)   { return flags_[id]; }
>>467|    std::uint16_t& faction(NpcId id) { return faction_[id]; }
  468|    std::int16_t&  hp(NpcId id)      { return hp_[id]; }
---
  467|    std::uint16_t& faction(NpcId id) { return faction_[id]; }
>>468|    std::int16_t&  hp(NpcId id)      { return hp_[id]; }
  469|    std::int16_t&  max_hp(NpcId id)  { return maxHp_[id]; }
---
  468|    std::int16_t&  hp(NpcId id)      { return hp_[id]; }
>>469|    std::int16_t&  max_hp(NpcId id)  { return maxHp_[id]; }
  470|
---
  472|    // labels are {0,1,2,-8,-14,-26,-36,-50,14,30} and FloorRegistry's legal range is
>>473|    // kMinFloor -127 .. kMaxFloor +127. As std::uint16_t this column stored floor -50
  474|    // as 65486 and floor -127 as 65409; nothing in src/ read it back, which is the
---
  476|    // pool.floor(id), replacing FloorStreamer's fixed [firstId, count) roster) is
>>477|    // exactly the reader that would have hit it. int16_t, not int32_t, so the column
  478|    // stays 2 B/row — the fix costs nothing.
---
  478|    // stays 2 B/row — the fix costs nothing.
>>479|    // READ-ONLY on purpose. main originally exposed `std::int16_t& floor(NpcId)` to
  480|    // fix the signedness below, and the branch added the per-floor bucket index that a
---
  495|    // labels are {0,1,2,-8,-14,-26,-36,-50,14,30} and FloorRegistry legal range is
>>496|    // kMinFloor -127 .. kMaxFloor +127. Returned as std::uint16_t this read floor -50
  497|    // back as 65486 and floor -127 as 65409. Nothing in src/ read it until the
---
  498|    // per-floor bucket index arrived, which is exactly the consumer that would have
>>499|    // hit it. int16_t, not int32_t, so the column stays 2 B/row -- the fix is free.
  500|    std::int16_t floor(NpcId id) const { return floor_[id]; }
---
  499|    // hit it. int16_t, not int32_t, so the column stays 2 B/row -- the fix is free.
>>500|    std::int16_t floor(NpcId id) const { return floor_[id]; }
  501|    void set_floor(NpcId id, std::int16_t label);
---
  500|    std::int16_t floor(NpcId id) const { return floor_[id]; }
>>501|    void set_floor(NpcId id, std::int16_t label);
  502|
---
  507|    // callers must treat it as a set, not a sequence.
>>508|    const std::vector<NpcId>& floor_bucket(std::int16_t label) const;
  509|
---
  514|    std::uint8_t&  sex(NpcId id)     { return sex_[id]; }      // NpcSex code
>>515|    std::uint16_t& height_mm(NpcId id) { return heightMm_[id]; } // stature, mm
  516|    std::uint8_t&  level(NpcId id)   { return level_[id]; }
---
  566|    std::vector<std::uint8_t>  flags_;
>>567|    std::vector<std::uint16_t> faction_;
  568|    std::vector<std::int16_t>  hp_;
---
  567|    std::vector<std::uint16_t> faction_;
>>568|    std::vector<std::int16_t>  hp_;
  569|    std::vector<std::int16_t>  maxHp_;
---
  568|    std::vector<std::int16_t>  hp_;
>>569|    std::vector<std::int16_t>  maxHp_;
  570|    std::vector<std::int16_t>  floor_;  // signed logical floor label ([floors.md])
---
  569|    std::vector<std::int16_t>  maxHp_;
>>570|    std::vector<std::int16_t>  floor_;  // signed logical floor label ([floors.md])
  571|    std::vector<std::uint8_t>  cx_, cy_, cz_;  // macro cell within the floor
---
  573|    std::vector<std::uint8_t>  sex_;    // LIVE — NpcSex code (0 = unset)
>>574|    std::vector<std::uint16_t> heightMm_; // stature in mm; drives embodied AABB
  575|    std::vector<std::uint8_t>  level_;  // LIVE
---
  589|    // the column is the cheapest in the table bar the flag byte.
>>590|    std::vector<std::uint16_t> gen_;
  591|
---
  621|    // label -> bucket slot, or -1 for the sentinel / out of range.
>>622|    static int bucket_slot(std::int16_t label) {
  623|        if (label == kNoFloorLabel) return -1;
---
src/game\ai.cpp:46|// Health pressure: 0 at full HP, 1 at 0 HP.
src/game\ai.cpp:47|inline float health_pressure(float hp, float maxHp) {
src/game\ai.cpp:48|    return maxHp > 0.0f ? clamp01f(1.0f - hp / maxHp) : 0.0f;
src/game\ai.cpp:149|    const float hpP = health_pressure(p.hp, p.maxHp);
src/game\ai.cpp:626|        // --- RECORDER 1: an HP drop, checked EVERY tick ----------------------
src/game\ai.cpp:628|        // whose cell is the fact. `AiBrain::lastHp` is what makes that detectable
src/game\ai.cpp:632|        const std::int16_t hpNow = pool.hp(id);
src/game\ai.cpp:633|        if (useMem && hpNow < brain.lastHp) {
src/game\ai.cpp:634|            const float maxHp = static_cast<float>(pool.max_hp(id));
src/game\ai.cpp:635|            const float lost = static_cast<float>(brain.lastHp - hpNow);
src/game\ai.cpp:636|            // Strength is the FRACTION of max HP lost — a real varying number, not
src/game\ai.cpp:638|            const float frac = maxHp > 0.0f ? lost / maxHp : 1.0f;
src/game\ai.cpp:642|        brain.lastHp = hpNow;
src/game\ai.cpp:669|            p.maxHp = static_cast<float>(pool.max_hp(id));
src/game\ai.h:200|// tick"), and an HP DROP (checked every tick, because damage is an event and the
src/game\ai.h:297|    // The HP this body had when `ai_step` last visited it, so a DROP is
src/game\ai.h:298|    // detectable without a damage event bus: `hp < lastHp` on any tick means the
src/game\ai.h:303|    // sentinel: the first visit sees `hp > 0 == lastHp`, i.e. a heal, records
src/game\ai.h:306|    std::int16_t lastHp = 0;
src/game\ai.h:404|    MemHurt,      // I lost HP at this cell
src/game\ai.h:692|    float maxHp = 0.0f;
src/game\ai.h:900|//     tick"); an HP DROP is filed the tick it happens, because damage is an event
src/game\ai.h:901|//     and `AiBrain::lastHp` is what detects it with no event bus.
src/game\faction_relations.h:203|// The share of its own max HP below which a feud will not push a crowd body, in
src/game\faction_relations.h:206|// A hard floor of 1 HP was the first version and it is WRONG for a reason that only
src/game\faction_relations.h:208|// touched would sit at 1 HP forever, and hunt.h's median monster (13 damage on a
src/game\faction_relations.h:209|// 1.55 s cooldown) kills a 1 HP body on its first swing instead of its eighth. The
src/game\faction_relations.h:217|inline constexpr int kFeudMinHpPct = 50;
src/game\npc_pool.cpp:59|    hp_.assign(kNpcPoolSize, 0);
src/game\npc_pool.cpp:60|    maxHp_.assign(kNpcPoolSize, 0);
src/game\npc_pool.cpp:216|    hp_[id] = 0;
src/game\npc_pool.cpp:217|    maxHp_[id] = 0;
src/game\npc_pool.cpp:389|    return column_bytes(flags_) + column_bytes(faction_) + column_bytes(hp_) +
src/game\npc_pool.cpp:390|           column_bytes(maxHp_) + column_bytes(floor_) + column_bytes(cx_) +
src/game\npc_pool.cpp:452|// hp/maxHp/floor 6 + cell 3 + height 2 + age/sex/level 3 + attrs 8 + gen 2) +
src/game\npc_pool.cpp:473|        put_i16(out, hp_[id]);
src/game\npc_pool.cpp:474|        put_i16(out, maxHp_[id]);
src/game\npc_pool.cpp:538|        hp_[id] = r.i16();
src/game\npc_pool.cpp:539|        maxHp_[id] = r.i16();
src/game\npc_pool.h:169|    float hpDebt = 0.0f;      // sub-1-HP attrition carried between ticks
src/game\npc_pool.h:213|// faction_/hp_/maxHp_/floor_/heightMm_ 2 each, flags_/cx_/cy_/cz_/age_/sex_/level_ 1
src/game\npc_pool.h:225|//   EAGER  — flags_, faction_, hp_, maxHp_, floor_, cx_/cy_/cz_, heightMm_, inv_,
src/game\npc_pool.h:468|    std::int16_t&  hp(NpcId id)      { return hp_[id]; }
src/game\npc_pool.h:469|    std::int16_t&  max_hp(NpcId id)  { return maxHp_[id]; }
src/game\npc_pool.h:568|    std::vector<std::int16_t>  hp_;
src/game\npc_pool.h:569|    std::vector<std::int16_t>  maxHp_;
src/game\rpg.h:12|// carries the monster-scaling half of `rpg.ts`: `mob_hp_at_level` is that file's
src/game\rpg.h:13|// `scaleMonsterHp` (1 + 0.12*(L-1)). The dmg/speed curves (1 + 0.10, 1 + 0.02) are
src/game\rpg.h:89|// derived rather than stored: `maxPsi` (always `max_psi(*this)`) and `maxHp`
src/game\rpg.h:90|// (always `max_hp(*this)`). The reference stores maxPsi AND recomputes it at four
src/game\rpg.h:108|inline constexpr std::uint16_t kBaseHp = 100;
src/game\rpg.h:109|inline constexpr std::uint16_t kHpPerLevel = 1;
src/game\rpg.h:114|std::uint16_t level_hp(std::uint8_t level);
src/game\rpg.h:117|// Level base times the attribute multiplier, rounded. STR feeds HP at +1%/point,
src/game\rpg.h:119|std::uint16_t max_hp(const RpgStats& r);
src/game\rpg.h:136|//   LINEAR      1 + p*k          — grows without bound (HP, melee dmg, move speed)
src/game\rpg.h:199|// `hp`/`maxHp` are optional and updated in step: a level-up raises max HP via the
src/game\rpg.h:200|// STR curve, and the reference adds the DIFFERENCE to current HP rather than
src/game\rpg.h:202|// nullptr for a character whose HP lives elsewhere ([combat.h] documents that split).
src/game\rpg.h:207|                 std::int16_t* hp = nullptr, std::int16_t* maxHp = nullptr);
src/game\rpg.h:212|// STR and INT immediately re-derive max HP / max PSI and credit the gain to the
src/game\rpg.h:215|                      std::int16_t* hp = nullptr, std::int16_t* maxHp = nullptr);
src/game\save.h:28|//     reproduce is the PLAYER's — its clock, its bag, its HP — so that is the row that
src/game\save.h:334|    std::int32_t maxHp = 0;
src/game\save.h:668|// `maxHp` is restored only when the save carries a positive one. A v1 save written by a
Traceback (most recent call last):
  File "C:\hades\gigahrush2\shots\_probe_hp_ptrs.py", line 37, in <module>
    grep("src/game/components.h", ["struct MobRef", "struct Health", "hp", "maxHp"], 5)
    ~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "C:\hades\gigahrush2\shots\_probe_hp_ptrs.py", line 5, in grep
    lines = Path(path).read_text(encoding="utf-8", errors="replace").splitlines()
            ~~~~~~~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "C:\Users\Admin\AppData\Local\Programs\Python\Python313\Lib\pathlib\_local.py", line 546, in read_text
    return PathBase.read_text(self, encoding, errors, newline)
           ~~~~~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "C:\Users\Admin\AppData\Local\Programs\Python\Python313\Lib\pathlib\_abc.py", line 632, in read_text
    with self.open(mode='r', encoding=encoding, errors=errors, newline=newline) as f:
         ~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "C:\Users\Admin\AppData\Local\Programs\Python\Python313\Lib\pathlib\_local.py", line 537, in open
    return io.open(self, mode, buffering, encoding, errors, newline)
           ~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
FileNotFoundError: [Errno 2] No such file or directory: 'src\\game\\components.h'
