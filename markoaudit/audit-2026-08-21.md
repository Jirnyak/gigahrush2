# Большой аудит 2026-08-21 («говно может быть везде» — заказ владельца)

Метод: три read-only агента по зонам, классы дефектов — те, что вскрыли
световую грязь 2026-08-20 (расщепление источников правды, врущая проза,
дубли, мёртвые лейны, голые числа, вторые реализации, ветки-вместо-данных).
Свет/антураж/пропы/якоря НЕ входили — отаудичены и починены той же сессией.

## Зона 1: ядро world/sim/core/ecs — 25 пунктов

ТОП по важности:

1. **los.cpp:103 — луч судится по КЛЕТКЕ** (класс S2, улика «стрельба сквозь
   свою дыру»): клетка с 1 субвокселем из 512 непрозрачна целиком;
   потребители — окклюзия звука (spatial_audio.cpp:108) и осколки гранаты
   (combat.cpp:1807). Рядом лежит sub_march. → волна occupancy, но можно
   подключить точечно.
2. **stain.cpp:66-84 — ВТОРОЙ субвоксельный марш** (S11): свой шаг 0.5
   воксель вместо sub_march, объявленного единственным.
3. **fluid_step МЁРТВ в тике** (вызов только в тестах; main.cpp признаёт
   «вода нарисована и заморожена»), а шапка fluid.h перечисляет живых
   потребителей; kGasField обещает шаг газа, которого нет ни строки.
4. **diffusion.cpp:97 + fluid.cpp:17 — «частичных масок не бывает»**: оба
   строят доказательства на утверждении, опровергнутом carve_sphere в тике.
5. **diffusion.h:265 — третья реализация проходимости** (свой 256КиБ битсет =
   WalkBits бит в бит, мимо nav::build_walk_bits/patch_walk_bit).
6. **physics.cpp:199 vs components.h:31 — два фолбэк-габарита тела**
   (0.4³ против 0.4×0.4×0.9): сущность без AABB коллидится вдвое ниже.
7. **gravity.h:113,126 — два источника правды** (вектор global + квант
   regime, согласие — ручной договор): physics читает вектор,
   fluid/controller/frames — регим.
8. **camera.cpp:9 + components.h:78 — Z-привилегия камеры** (сферика от +Z;
   в fly-режиме взгляд и ход расходятся под не-Z фреймом).
9. **nav.cpp:177 vs los.cpp:103 — противоположные клеточные предикаты**:
   полупроточенная клетка проходима для NPC и непрозрачна для луча.
10. **macro_grid.h:33-56 — kCentreZ/lowest_layer/lowest_layer_centre мертвы**
    (сессия 2026-08-21 убила последних потребителей) + врущая проза «горячие».

Врущая проза (греп-подтверждено): los.h:41-46 (z-стена, противоположно коду);
physics.h:20-34 (1/120 — тест-сайты давно на kSimDt, условие миграции
выполнено); lattice.h:30 (числа шагов не существуют); field.h:69 (ассерт,
которого нет); types.h:6 («one-line change» против static_assert в
destruct.cpp:12); physics.cpp:16 (комментарий над пустым namespace);
physics.cpp:223 (фолбэк-паттерн «тот же» — неправда для AABB).

Дубли/голые числа: stain.cpp:14 vs destruct.h:66 (константы суб-тора);
physics.cpp:130,261,294,331,352 (0.01/4.0/0.02/0.35/0.85 без вывода);
components.h:31,71,87 (габарит/прыжок/скорость назначены, S11 велит вывод);
stain.cpp:78-81 (O(rays×dirty) дедуп при sort+unique рядом в destruct);
fluid.cpp:24 (popcount×5 на мокрую клетку при готовых empty()/full());
diffusion.cpp:524 (% в тике при лестнице сдвигов S15);
materials.h:22 (kMatWaterMark без потребителей — чинить в CSV).

Чего НЕТ (проверено, не искать заново): глобальных O(n²)/BFS в тике в зоне
нет; тор-wrap един (core/wrap.h + маски степени двойки, согласованы).

## Зона 2: game-сим (AI/нужды/бой/лут/шум/макросим) — 25 пунктов

ТОП по важности:

1. **ai.h:715-731 — 18 мёртвых полей Perception** (armed/cornered/
   visibleHostiles/threatDistance/hostilePower/samosbor*/gunfire/fire/monster/
   factionAssaultTarget/minuteOfDay…): продюсеров НОЛЬ при живых
   samosbor/NoiseField/Equipped. Следствие: IntentCombat вечно платит −16
   «безоружен» и не выигрывает, IntentFactionAssault — константный 0.
2. **ai.cpp:904-906 — ветка по виду интента в скоре** (S13.9) под
   комментарием «no intent is named in this branch»; + белый список
   intent_room_mask. Уйдёт с agent-goals, но проза врёт уже сейчас.
3. **ai.cpp:909 + needs.cpp:295 — «я в комнате» решается 2D-колонной**
   (room_bit_at без cz): тело на любом ярусе восстанавливает нужды от кухни
   двадцатью этажами ниже. Ждёт rooms-object, но это ЖИВОЙ баг нужд.
4. **combat.cpp:73-79 + 167-169 — граната**: клеточный cell_solid (записан
   как решение) — отскакивает от прокарванной дыры; z НЕ заворачивается при
   врущей ссылке на конвенцию пули (пуля честно оборачивает все три оси).
5. **ai.cpp:713-742 — медик = O(n²) каждый тик** (вложенный view по слою
   с дистанциями, без клеточной сетки и стаггера) — прямое нарушение S9.
6. **macro_sim.cpp:14-15 — второй календарь** (365 дней/децидни) рядом с
   лестницей core/watch.h; quest.h:69 уже оговаривается — след расхождения.
7. **population.cpp:24 — вторая решётка комнат** (stride 16 против 4 при
   импортированном floor_room_stride()) — канон S13.10 подтверждён.
8. **ai.cpp:1059-1061 — фолбэк «цель потерялась → wander»** (S13.9) —
   вторая система выбора; уйдёт с agent-goals.
9. **combat.cpp:1655-1668 — поимённые списки материалов** (isSteel/
   isConcrete) рядом с material_hardness; isConcrete не добавляет НИЧЕГО
   (все ≥180); «сталь vs бетон» — это плотность из данных.
10. **encumbrance.cpp:134 — NoiseProfile шага собран из трёх голых чисел в
    горячем цикле** при фабриках-выводах у всех остальных источников.

Врущая проза (греп): ai.cpp:100-124 (needs_for спорит с needs.h, ДВА разных
ролла на один ряд нужд); ai.h:33 (slow_step «not wired» — вызывается
main.cpp:4116); ai.h:10 (ссылка на несуществующий каталог); ai.h:214,
:1060 (Mem* «no caller» — room_zone.cpp вызывает всеми четырьмя); ai.h:131
(work/patrol «не взяты» — взяты); ai.cpp:871 (IntentFlee «единственный
владеющий» — эррэнд и патруль тоже владеют); noise.h:90-91 (Footstep/Door
«reserved» — оба живые).

Прочее: 1.35f скорость ходьбы литералом в 4 местах; loot.cpp:31-55 два
switch по MobTier с 12 назначенными числами; combat.cpp:43-51 adjacent_wall
4 соседа из 6 (анизотропия, живой вход множителя урона) + int-каст вместо
floor; ai.cpp:641 kSeatClaimCap молча деградирует; ai.cpp:1166 патруль
«первый достижимый» + % в тике; population.cpp:6 мёртвый инклюд +
height_for_age из назначенных чисел; AiBrain::decisions — мёртвое поле.

## Зона 3: app/render швы — ожидает агента
