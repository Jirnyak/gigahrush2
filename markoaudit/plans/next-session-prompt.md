# ПРОМПТ СЛЕДУЮЩЕЙ СЕССИИ (переписан 2026-08-28 вечером, после закрытия масок и облегчения)

## Правила работы с этим владельцем (соблюдать буквально)

* У него в голове ВСЯ архитектура и замысел — **уточнять и обсуждать до кода**,
  а не после. Он отвечает быстро и точно. Предлагать варианты с рекомендацией.
* **Не гадать.** Любое утверждение о причине — после замера/лога/эксперимента.
* **Не хардкодить, не костылить, не дублировать** (S11). Перед новой формулой
  искать готовую.
* **Гейт пишется в том же коммите, что система**, и мутация гоняется в обе
  полярности ДО записи «гейт это ловит» (в этой сессии первый гейт self-skip
  мутацию НЕ ловил — усилен наклоном взгляда).
* Логи он читать не обязан. `./build/gigahrush2 2> ride.log`; первая строка —
  `[build]`. `--pitch`/`--yaw` харнесса — В РАДИАНАХ.
* Мы «полируем ядро и ищем говно»: код может врать — где код против канона,
  чинится код (или канон уточняется владельцем, не молча).

## СДЕЛАНО СЕССИЕЙ 2026-08-28 (подтверждено владельцем в игре)

* «F» в центре живёт (свой self перебивал фокус + табличка под дебаг-флагом);
  двери открываются/закрываются. Табличка — элемент худа (Center-слот).
* МАСКИ S18 закрыты: щит+дверь на едином субвоксельном `MaskGroup`
  (`world/mask.h`); ProtectMask и DoorPortal мертвы; активация ссылкой
  (`DoorRef` на кнопке лифта — деривация хаба из позиции умерла).
* ОБЛЕГЧЕНИЕ закрыто: осознанное на P + невольный слив на капе В КЛОКЕ
  (kOverflowHpPerSec мёртв, урона от давлений нет, «естественно только лужа»).
* Канон S19 СВИДЕТЕЛЬСТВО записан (деяние-событие, свидетель-восприятие,
  цена-данные) — только закладка, кода нет (`witness-system.md`).

## ФОКУС: КОМНАТА КАК ОБЪЕКТ (`rooms-object.md`) — решение владельца

Выход из моратория стабилизации по порядку владельца: **К3-комнаты первыми.**
Это фундамент, в который упирается ВСЁ игровое ядро: глаголы (S12.3,
`verbs-table.md`), предложение/запас (S12.4-12.5), владелец комнаты (S14.3),
репутация (S13.6), сделка-наборы (S14.2), свидетельство (S19), сюжет
(`plot-spine.md` прямо говорит «сначала К3-комнаты»), честный выбор целей
S13 (сегодня — суррогаты `kRoomAffordance`/`flow[11]`/`RoomBit`).

План готов и сверен с кодом: `rooms-object.md` (таблица «что сегодня» +
целевая модель). При старте:
* переспросить владельца про nearRoom 3D (4 МиБ/слот) — он «ещё подумает»;
* пересчитать выжившее из `room_zone.*` (оракул проходимости тела остаётся);
* язык комнат — глаголы: `verbs-table.md` сажается тем же эпиком (S12.2:
  словарь = таблица данных + кодоген, как materials.csv).

## ХВОСТЫ (мельче, по готовности)

* Плейтест владельца: кнопка вызова лифта СНАРУЖИ (DoorRef посажен), крафт
  от верстака, лужа по P.
* Лифты, очередь 5-7 (`elevators-2x2.md`): иллюзия поездки, модульные
  лобби+PSEUDOLIFT, могила nav-пути — после приёмки владельцем.
* Дебрис у лестниц: гипотеза «живая вода площадок меняет маски ступеней →
  судья детача рушит нависающие куски» — НЕ проверена, искать причину
  (`GIGA_DETACH_DBG=1`).
* Стенд решёток груб (`GIGA_GRATE_REPRO=1` считает висящим любой атом).
* Инженерное: суб-степовый инжект писателей; растр-ручки + кривая; уборщик
  фронтира; verlet-merge этап 2 + кап антуража к канону (24576 против 2^20,
  `caps-to-canon.md`); «тени реже пикселей» вариант B.
* UI: Settings-окно с вкладками (меню И пауза).

## СОСТОЯНИЕ ДЕРЕВА

Ветка `torus`, всё запушено. ctest 7/7, пин чеков 223274.
Сейв v19. Кадр GPU ~9.6 мс. Клавиш 17 (16 + relief/P).
