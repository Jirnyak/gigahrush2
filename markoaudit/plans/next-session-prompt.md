# ПРОМПТ СЛЕДУЮЩЕЙ СЕССИИ (переписан 2026-08-29 ночью, после автономной F/G/S19/§63-замера)

## Правила работы с этим владельцем (соблюдать буквально)

* У него в голове ВСЯ архитектура и замысел — **уточнять и обсуждать до кода**
  (ночная сессия была санкционированным исключением: «делай всё сам»).
* **Не гадать.** Любое утверждение о причине — после замера/лога/эксперимента.
* **Не хардкодить, не костылить, не дублировать** (S11). Упрощение по
  построению лучше кода.
* **Гейт в том же коммите, что система**; мутация в обе полярности ДО записи
  «гейт это ловит». Пины чеков поднимать с выводом. A/B-перф — ТОЛЬКО
  чередованием.
* Логи: `./build/gigahrush2 2> /tmp/giga.log`, первая строка `[build]` —
  проверять свежесть.
* Коммит-пуш после каждого зелёного инкремента (ветка `torus`).

## СДЕЛАНО НОЧНОЙ СЕССИЕЙ 2026-08-29 (автономно, по приказу «делай всё»)

Коммиты 7502e8db..01b001cd (7 боевых). ЭПИК skeleton-anchor ЗАКРЫТ ЦЕЛИКОМ
(A–G), витнес S19 — ядро посажено, §63 — замер сделан.

* **Хотфикс плейтеста**: yaw настенного пропа из грани якоря
  (`prop_wall_yaw`) — кнопка лифта больше не ребром. Двери падика 384 —
  вердикт «норм», хвост C закрыт.
* **F (S20.6, ПОЛНАЯ персистентность)**: floor_<N>.sav v3 = материя И
  сущности (PropRecord с якорем/инвентарём ящика/DoorRef/detached,
  PickupRecord — лут больше не теряется, прежние трупы/обломки, ключи
  обесточки) под КЛЮЧОМ МОДУЛЯ (kind+seed+genVersion,
  `module_gen_version` — таблица в floor_gen, ПОДНИМАТЬ РУКОЙ при правке
  генератора). run.sav v20 потерял секции сущностей; OpenedContainerKey и
  весь матчинг мертвы. Развилка `floor_entity_half` на всех 4 сайтах:
  restore НЕ сеет, завершается якорной пробой (мёртвый якорь = честный
  prop_detach). Гейты: suite_persist (round-trip бит-в-бит + ворота ключа +
  проба, мутации в обе полярности), floor_records_survive_a_restart на
  настоящем этаже, e2e двумя прогонами бинаря ([floor] RESTORED, ноль сева).
* **Попутно убитые живые баги**: прибытие оставляло этаж БЕЗ ЯЩИКОВ с
  2026-08-21 (сев до clear_layer_props); труп усыхал ×0.9 каждый цикл
  сейва; PowerGridState гасил клетки на ВСЕХ этажах; F9 сеял ящики до
  комнат и не чистил диффузию. Аудит-агентом после посадки: гонка двух
  писателей одного floor-файла (flush в синхронном писателе), «Новая
  игра» наследовала сущности мёртвого рана (rebuild_current_floor),
  стухший pending-restore (erase-first), граната-обломок, ворчание
  писателя на капы счётчиков, LOOT над выпотрошенным трупом.
* **G (S20.1, акустика на скелете)**: слышимость отвечает СКЕЛЕТ — шар
  путевых дистанций флудом по граням (газовый закон clearance, «звуку
  хватает щели»); `noise_acoustics_step` одним шагом, 9 писателей шума не
  тронуты; `skeleton_audible` точка-точка для событий. Гейт: за стеной не
  слышно / сквозь щель слышно обходом; мутация ловится.
* **S19 ВИТНЕС — ядро**: цены деяний = 5 колонок verbs.csv кодогеном +
  глаголы strike/kill/rob; EventType::Deed; witness_step (зрение
  sub_march 25 м ИЛИ слух skeleton_audible; уместность зануляет; дедуп по
  фракции; Room::rep с выцветанием ~3 мин; Room::owner — кодировка
  0=ничья/1..=фракция+1). **relations_drain_deaths ВЫРЕЗАН — незамеченное
  убийство не гнёт матрицу** (S19.3 закрыт). Продюсеры: kill
  (finalize_deaths), relief игрока. План: markoaudit/plans/witness-system.md.
* **§63 ЗАМЕР СДЕЛАН** (вопрос 1 закрыт): live растёт монотонно
  3.9k→11.9k; состав = жидкость + СУХИЕ живые (газа НОЛЬ); z-фронт едет
  вниз 126→111; топ-колонки — у лифтовых столбов. **Корень: водопад в
  замкнутой шахте не имеет дна, z заворачивается — падает по кольцу
  ВЕЧНО.** Инструмент: GIGA_MEDIUM_DBG=1. Кандидаты лечения в §63.

## ПЕРВОЕ ДЕЙСТВИЕ СЕССИИ — ВОПРОСЫ ВЛАДЕЛЬЦУ

1. **Плейтест ночной сборки**: этаж помнит всё? (сорви лампу/выпотроши
   ящик → уедь → вернись); кнопка лифта теперь ЛИЦОМ?; «Новая игра»
   девственна?; fps не хуже?
2. **§63 лечение — выбор владельца**: (а) налив генератора мимо объёма
   шахт (объём уже назван маской щита), (б) дно шахте ниже storey входа,
   (в) отдельно — сон сухого шлейфа. Водопады — «замысел», но вечный
   кольцевой водопад = 10-16 fps.
3. **Витнес — дефолты ночи на пересмотр**: восприятие без броска
   «заметил» (чистые los+слух); игрок = та же матрица; цены
   strike=kill/5, rob=kill/2 (данные CSV — крутить смело); репутация
   комнаты ТРАНЗИЕНТНА (персист = v4 floor-файла — надо ли?);
   НАЗНАЧЕНИЕ владельцев комнат — чья политика в падике?
4. Аудит-находка №8: несомый проп при отъезде остаётся на СТАРОМ этаже в
   позиции рук (линки транзиентны) — норм или везти с игроком?

## ОЧЕРЕДЬ ПОСЛЕ ОТВЕТОВ

1. **§63 лечение** по выбору владельца (замер готов, править по нему).
2. **Витнес хвосты**: продюсеры strike (шина в apply_damage хвостовым
   параметром)/rob (сделка без согласия S14.2)/heal; назначение
   владельцев комнат модулем; потребители Room::rep (HUD? спич? выбор
   цели?).
3. agent-goals (очередь INDEX) — после стабилизации.

## Инструменты (переиспользовать)

* Перф: `--shot /tmp/x.png --frames 2000 --floor N 2>&1 | grep -E
  "cpu-shot|gpu-shot|judge|medium"`; A/B чередованием; floor 4 ≈ 9 мс.
* Состав live сред: `GIGA_MEDIUM_DBG=1` → `[medium-dbg]` (классы/этажи/
  колонки).
* `GIGA_DETACH_DBG=1` — вердикты судьи. Лог владельца: /tmp/giga.log.
* Пины: game_test 222287, audit_test 1371.
