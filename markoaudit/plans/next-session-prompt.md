# ПРОМПТ СЛЕДУЮЩЕЙ СЕССИИ (переписан 2026-08-31, после перф-эпика + большого суда)

## Правила работы с этим владельцем (соблюдать буквально)

* У него в голове ВСЯ архитектура и замысел — **уточнять и обсуждать до кода**.
* **Не гадать.** Любое утверждение о причине — после замера/лога/эксперимента.
  Эта сессия дала три урока метода подряд: замер отменил три «очевидных»
  оптимизации (§59.17/59.4/59.6), мутация вскрыла слишком добрый стенд
  (верёвочная цепь спала без лечения), а «виновник» 50-мс кадров оказался
  не тем, на кого думали (rigid, не конверсия).
* **Не хардкодить, не костылить, не дублировать** (S11).
* **Гейт в том же коммите, что система**; мутации в обе полярности ДО записи
  «гейт это ловит» — и ПОШТУЧНО, если ног несколько (§64: две несущие из трёх).
* A/B-перф — ТОЛЬКО чередованием; между прогонами `rm -rf gigahrush2_save
  build/gigahrush2_save` (floor-файлы v3 наследуют материю!); пути абсолютные
  (cwd Bash липнет).
* Логи: `./build/gigahrush2 2> /tmp/giga.log`, первая строка `[build]`.
* Коммит-пуш после каждого зелёного инкремента (ветка `torus`).

## СДЕЛАНО СЕССИЕЙ 2026-08-30..31 (эпик «перф + большой суд», ~15 коммитов 129fa1dd..5d7a6a47)

* **GIGA_PROF=1 построен**: per-system свод CPU-кадра (15 слотов + все 11
  GPU-пассов + счётчики live/rigid/суда) раз в 256 кадров. Слепая зона
  §59.25 закрыта. Плюс rigid-stats, med-apply-mix, big-judge строки.
* **§63 ЗАКРЫТ**: (1) med_apply 9.9→1.8 мс — битсет фронтира «раз на клетку
  за этаж» (ел open_frontier, не memcmp); (2) горб = ~2 мин на этаж, не
  вечность; сухой шлейф = −исполнители блоков Марголуса (нужные зомби).
* **S16.8 БЮДЖЕТНЫЙ ДИСПАТЧ (канон!)**: ≤budget клеток за подтик, хэш-ключ
  клетки, нечётный страйд; live > бюджета = равномерное замедление этажа
  (закон владельца); fps не зависит от live навсегда.
* **БОЛЬШОЙ СУД (канон в S20.5)**: отсечка малого = передача дела;
  тор-перколяция = опора; конверсия порциями; кэш оправданий регионом;
  помпа платит полный долг писателя (лампы падают вместе с домом —
  принято владельцем). Малый судья НЕ ТРОНУТ (решение владельца).
  Хрущёвка карвом СКЛАДЫВАЕТСЯ. План: markoaudit/plans/big-judge.md.
* **§64 джиттер ЗАКРЫТ**: труп достигает сна — стикция от µ мат-пары +
  ЛЮФТ сустава 1 см (несущая) + wakePass будит движущимся (несущая).
* Пины: game_test 222303, world_test 23617, medium_test 878, audit 1371.

## ПЕРВОЕ ДЕЙСТВИЕ СЕССИИ — ВОПРОСЫ ВЛАДЕЛЬЦУ

1. **Плейтест §64**: после обрушения `[prof] rigid-stats noisy` сходит к ~0
   за секунды? Кадры после осадки без 40-мс rigid? Трупы в бою засыпают?
2. **Плейтест обрушения целиком**: карв хрущёвки → лампы отваливаются
   (принято 31-го), дом складывается с боковым расползанием, fps держится?
3. **Накопление тел** (открытый хвост §64): тела копятся без реапера —
   спящие почти бесплатны, но память/сейв растут. Решение «осевшее-в-проп»
   (труп → один лежащий проп с лутом, пинок возвращает тела) — когда?
4. **GPU raster 12–18 мс на открытых этажах** (окно владельца) — потолок
   fps на хрущах; тени 25%. Принять или копать? Для раскопки нужен пин
   камеры с его точки (сейв или поза).

## ОЧЕРЕДЬ ПОСЛЕ ОТВЕТОВ

1. Хвосты по плейтесту (§64/обрушение), если найдутся.
2. Витнес-хвосты S19: продюсеры strike/rob/heal, назначение владельцев
   комнат, потребители Room::rep (план witness-system.md).
3. agent-goals (очередь INDEX).
4. Загрузка/поездка (хитчи 1.6–7 с: RESTORED ~2 с + lightvis ~1.2 с) —
   если владелец захочет «лифт как ширму» дожать.

## Инструменты (переиспользовать)

* `GIGA_PROF=1` — свод per-system + GPU; `GIGA_MEDIUM_DBG=1` — состав
  live + зомби-счётчики; `GIGA_HITCH_MS=20` — чуткий детектор.
* Суд: `GIGA_BIGJUDGE_NODES/CONVERT` — бюджеты; строки [big-judge].
* Автомат: `GIGA_MEDIUM_BUDGET` (0 = без бюджета) — A/B same-binary.
* Перф-скрипт: `--shot /tmp/x.png --frames 1500 --floor N` + grep
  "wall med|gpu lgrid|rigid-stats|big-judge".
* Лог владельца: /tmp/giga.log. Память сессии:
  ~/.claude/.../memory/perf-epic-2026-08-30.md.
