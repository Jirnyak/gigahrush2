# Кластеризация света: стоимость кадра не зависит от контента — по построению

**ПОСТРОЕНО ЦЕЛИКОМ 2026-08-20/21** (bf6ff4e5 синтез, ee5a03da паковка,
d97e1570 включение+таблица, b8e94d71 шаг 3 чистоты, 605d315c оракул+packed
mean). Практика владельца: худший зал 35 -> 40 fps на шаге 2, «выглядит как
честные лампы, кластеризация не чувствуется»; после шага 3 (все 8 честных
маршатся, бюджеты умерли) — 35 в худшем, принято владельцем («пофиг», чистота
дороже). Формулировка-итог (поправка владельца): для ГЛАЗА источники разные
(эмиссив каждого жив), одним они становятся только в вычислении вклада.

Решения владельца 2026-08-20 (ночь-2): (1) направление — **кластеризация
хвоста в бейке**, топ-K честных ламп + хвост слит в кластеры **тем же структом
PointLight**; камерный кап отвергнут (воскрешение похороненного капа-512);
(2) **кластеры без теневого марша** — весь бюджет лучей уходит сильнейшим.
Мотив владельца: «любая сцена с многим светом ложит fps» — класс должен
умереть, а не лечиться ручками; «сделать и потом не крутить никакие параметры».

## 1. Почему это одна система, а не новая

- `PointLight` уже универсален: лампочка, прожектор, трассер и **неон-кластер
  светоматериалов** (`bake_material_lights` уже сливает светящиеся субвоксели
  в кластерные лампы: центроид + радиус охвата + сумма). Кластер ламп — та же
  идея, применённая уровнем выше. Потребители (surface_light, light_grid.comp)
  **не меняются ни строчкой** — кластер неотличим от лампы.
- Бейк видимости уже даёт каждой клетке отсортированный по вкладу список
  достижимых ламп. Кластеризация — пост-шаг над готовым списком.

## 2. Дизайн

### 2.1 Кластер = пространственный бакет ламп, слот в статик-таблице

Кластер на клетку синтезировать нельзя (262144 × M записей); кластеры —
**общие**, по пространственным бакетам: лампы группируются по блоку
**8 м = 2×2×2 клетки светосетки** (вывод: ошибка позиции кластера ≤
полудиагонали бакета ≈ 6.9 м — хвостовые лампы по построению дальние/слабые,
их угловая ошибка тонет в аттенюации; подтверждается замером и глазами,
§4). Кластер бакета: позиция = центроид членов, взвешенный силой; радиус =
охват членов (максимум дистанции до члена + его радиус); сила = Σ членов;
цвет = взвешенное среднее. Кластеры живут **в статик-таблице** обычными
слотами после ламп (kRootLights 131072 — места навалом: ~12.5k ламп + единицы
тысяч бакетов).

### 2.2 Состав клетки решает бейк

Для каждой клетки после сортировки по вкладу:

```
слоты клетки = топ-K честных ламп (по вкладу, как сейчас)
             + кластеры бакетов, в которые попал ХВОСТ (по одному id на бакет,
               отсортированы той же метрикой — суммарным вкладом хвоста бакета)
```

- **K = 8**: вывод — максимум теневого бюджета пикселя (4 близко) × запас 2 на
  отбраковку порогом/ndotl (список один на клетку, лучи выбирает пиксель).
  Финал K — замером (mean длина списка, fps, глаза), но это константа С
  ВЫВОДОМ, а не ручка.
- Кластеры ниже порога-надгробия (kTombstoneIntensity на дистанции клетки) —
  выброшены с печатью (S11, молча не режем).
- Списки клеток становятся ~K+M ≈ 8-12 вместо 63 → скан на пиксель дешевеет
  на порядок, и **worst case перестаёт зависеть от контента**: зал с 500
  ламп = коридор с тремя.

### 2.3 Кадр: интенсивность кластера — сумма членов

CPU уже обновляет интенсивности всех статиков каждый кадр (мерцание,
обесточка, надгробия). Кластер = ещё один проход: `intensity[слот кластера] =
Σ intensity[члены]` по CSR-списку членов (десятки тысяч сложений — доли мс).
Что выпадает даром: мерцание кластера статистически усредняется (физически
честно для толпы ламп); обесточка района щитком гасит его кластер; разбитая
лампа-надгробие уменьшает сумму тем же кадром.

### 2.4 Тени и инварианты

- Кластер **не маршится** (решение владельца): он запечённо-достижим для
  клетки (достижимость = хоть один член достижим — консервативно), ошибка
  только «посветить лишним» в 4-м гранулярности — инвариант (б).
- Грязные клетки (карв-фолбэк) биннят всю таблицу дистанционно — увидят и
  члены хвоста, и их кластер: временный двойной свет до свапа ребейка. Ошибка
  в сторону света, инвариант (б); ребейк ужимает.
- S7 чист: состав клетки — функция геометрии, камера ни при чём.

## 3. Инкременты

1. **СДЕЛАНО 2026-08-20 (bf6ff4e5).** Синтез в бейке: `LightVisLamp.color`,
   бакетизация всех статиков 8 м, `LightVisCluster` (центроид по radiusM²,
   цвет взвешенно, радиус-охват, CSR членов), детерминизм по индексу бакета;
   suite с точными числами вывода. Клетки кластеры ещё НЕ ссылают.
   **Шаг 2 — рецепт (уточнён 2026-08-20, ночь-3). Ключ: кластеры живут в
   ВЕРХНЕМ регионе таблицы** — слот = kClusterSlotBase + clusterIdx, где
   kClusterSlotBase = kRootLights − kClusterGridDim³ = 131072 − 32768 = 98304
   (регион = ровно максимум бакетов). Это убивает обе проблемы ремапа:
   (а) растущие статики+динамики [0..N) не сталкиваются с верхом никогда
   (12k ≪ 98k; переполнение и так печатается); слоты кластеров стабильны на
   поколение бейка БЕЗ ремапа при аппендах карва; (б) грязные клетки биннят
   только [0, activeCount) — кластеры в фолбэк не попадают, двойного света
   в грязных клетках НЕТ (лучше плана §2.4).
   Подшаги: (1) бейк: синтез ПЕРЕД паковкой клеток; паковка = top-K=8 ламп +
   refs кластеров хвостовых бакетов (первое появление бакета в отсортированном
   хвосте = ранжирование даром), ref = clusterRefBase + ci, base приходит
   параметром (game лейаут-агностичен; рендер передаёт kClusterSlotBase через
   set_light_table рядом со slots). (2) GpuLightGrid: kClusterSlotBase +
   static_assert(base > реалистичный статик-потолок); set_cluster_records
   (запись в stagingLights_ при свапе), второй memcpy региона кластеров в
   update_and_dispatch, getter staged_intensity(slot). (3) main.cpp свап:
   set_cluster_records из bake.clusters (GpuPointLight: pos/radius/color,
   омни, intensity 0), g_clusterMembers/slots копия для кадра. (4)
   collect_scene_lights: после статиков — сумма интенсивностей членов по CSR
   → set_static_intensity(слот кластера). (5) Suite: хвостовая лампа клетки
   обязана быть членом кластера, на который клетка ссылается (не потерять
   свет); полярность — сломать паковку, тест красный. (6) A/B: база владельца
   35 fps в худшем зале Z0 падика; печать mean длины списков до/после.
2. **Таблица**: кластерные слоты при Fresh/Rebake-свапе (стабильные на
   поколение, как статики), CPU-сумма интенсивностей в collect_scene_lights.
3. **Suite**: оракул потока — на сэмплах (клетка, точка) суммарный вклад
   (топ-K + кластеры) ≈ вклад полного списка в пределах вывода §2.1; пин
   бейка (хеш состава клеток); обратная полярность — сломать суммирование,
   тест обязан покраснеть.
4. **Замер**: A/B тем же GIGA_LIGHT_BUDGET-приёмом + fps владельца в худшем
   зале; печать сводки: mean/max длина списка, число кластеров, выброшено
   ниже надгробия.

## 3б. Шаг 3 — ВОЗВРАТ ЧИСТОТЫ (директива владельца 2026-08-20, ночь-4)

Раз fps решается кластеризацией универсально, срезки-компромиссы обязаны
УМЕРЕТЬ, а не остаться дешёвыми: конечное состояние — ядро без бюджетов.
После посадки и замера шага 2:

- **Ступени теневых лучей 4/2/1 умирают**: пиксель маршит ВСЕ честные лампы
  списка — их ровно K по построению. Две сущности (бюджет лучей + длина
  списка) сливаются в одну: K. GIGA_SHADOW_RAYS остаётся только как
  измерительная ручка.
- **Порог вклада 0.004 до луча** — кандидат на смерть той же волной: список
  короткий, отбраковка теряет смысл; решить замером (если марши к
  отвернувшимся лампам заметны — порог остаётся с выводом).
- Гало дымки и god rays НЕ возвращаются этим шагом: это эстетические решения
  владельца («без них лучше»), не перф-срезки; возврат гало — отдельный слой,
  если когда-нибудь захочется.
- Проверка чистоты: в surface_light не остаётся ни одной ветки, зависящей от
  дистанции камеры или «бюджета», — только список клетки и марш к каждому.
  Владелец: «готов всегда обсудить» — спорные смерти показывать замером.

## 4. Что подтверждается замером (не ручки, а верификация выводов)

Бакет 8 м, K=8 — константы с выводами; если глаза владельца увидят ступень
кластера (дальний зал «плоский») — бакет уменьшается вдвое с перезамером, это
изменение вывода, а не подгонка. Цель владельца: поставил и забыл.

## 5. ВЕРДИКТ 2026-08-24: ПОДХОД ОПРОВЕРГНУТ КОНСТРУКЦИЕЙ (вторая попытка)

Вторая попытка (e63b5a73, откачена 5фикс-коммитом следом) починила всё, что
убило первую (гварда биннинга узнала регион, кластер получил тень по решению
владельца, якорь стал реальной лампой), прошла пины и замер кривой бакета
(8 м не сжимает ВООБЩЕ: packed max 30->30/49->40; только 32 м дал 17/18) —
и провалилась в игре владельца: Z=0 10 fps (хуже базовых 20), «все лампы
ярче, наслоение».

Корень — ДВОЙНОЙ СЧЁТ ПО ПОСТРОЕНИЮ, и он неустраним в этой схеме:
интенсивность кластера — сумма ВСЕХ членов бакета (глобальная, одна запись
на всех потребителей), а деление «честные топ-K / хвост» — ПОКЛЕТОЧНОЕ.
Клетка, у которой лампа X в честном топе, всё равно ссылается на бакет X
(ради других его хвостовых членов) — и X светит дважды: лично + в сумме
кластера. С крупным бакетом (а сжатие даёт только крупный — замер выше)
задвоено почти всё. Плюс перф-минус: кластер = лампа с радиусом-охватом в
десятки метров, теневой марш к далёкому якорю ДЛИННЫЙ. Первая попытка несла
тот же дефект скрыто — она просто не светила.

Честная кластеризация требует ПОКЛЕТОЧНОЙ суммы хвоста — ровно то, что §2.1
отверг по памяти (262144 × M). Либо другой формат клетки (вес хвоста как
per-cell запись — смена лейаута GpuGridCell), либо не-кластерный рычаг.
Проверенный рычаг для Z=0 сегодня — полурезный световой пасс (2×, ждёт
ручки настроек графики, d14c7fb6 в истории).
