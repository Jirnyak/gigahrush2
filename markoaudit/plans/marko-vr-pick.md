# РАЗБОР КОММИТА marko1olo bb62c64a — ПРОМПТ СЕССИИ

**Заведён 2026-09-12.** Решение владельца: **наша линия приоритетна. Берём то,
что хорошо, коммитим наше поверх в main, что насрано — трём без сожаления.**

Это промпт: открыть сессию с него и работать по нему.

---

## 0. ПРАВИЛО СЕССИИ

**Torus — истина. Main — линия приёмки.** Всё, что приходит со стороны, проходит
через torus и наши гейты, а не наоборот. Прецедент в памяти проекта: пачки
marko1olo разбираются **поштучно**, а не сливаются доверием (`markoaudit.md`,
ветка `pick/marko-2026-08-13`, из прошлой пачки уцелел один fast travel).

**Не `git merge`.** Переносить по СМЫСЛУ на сегодняшний API, а не разрешать
конфликты текста. Причина — в фактах ниже.

---

## 1. ФАКТЫ (проверены, не пересказ)

| Что | Значение |
|---|---|
| Коммит | `bb62c64a feat(vr): implement stereoscopic 3D raymarching mode and gamepad navigation` |
| Автор, дата | marko1olo, 2026-09-12 18:37 |
| Куда | напрямую в `origin/main` |
| Объём | 16 файлов, +498 −175 |
| **База** | `97bdf13e` (DDA-свет) — **17 августа** |
| **Отставание базы** | **416 коммитов torus** поверх неё |
| Конфликты при слиянии | `src/app/main.cpp`, `src/render/raymarch_pass.cpp` |

**Почему слияние текстом бессмысленно.** Его сторона зовёт
`player_ranged_step(reg, pool, layer, haveGun && isAttacking, kSimDt, ...)` —
сигнатуру ДО эпика двух рук. У нас сегодня две руки раздельно
(`attackHeld`, `rmbHeld`). Это не «две правки рядом», это месяц архитектурного
расхождения: принять его сторону = откатить two-hands.

### Нарушения процесса — три, и одно серьёзное

1. **ПИН ТЕСТОВ ПОНИЖЕН.** На его базе `game_test: 243576 checks`, он поставил
   `243209`. **367 проверок исчезли**, обоснование — комментарий «suite check
   pin update», то есть никакого. Файлов тестов коммит не трогает вообще
   (`git show --stat` → ноль `tests/`), значит проверки отвалились побочным
   эффектом правок в `input`/`camera`/`hud`. Пин — храповик; он существует
   ровно для того, чтобы это было невозможно сделать молча.
   → **СНЯТО ЗАМЕРОМ 2026-09-12, обвинение было ложным. См. §1а.**
2. **Ноль гейтов на ~500 строк новой системы** — прямое нарушение закона
   «гейт в том же коммите, что система» (AGENTS.md). → **стоит.**
3. **Пуш в общую линию от месячной базы** мимо ветки разработки. → **стоит.**

### §1а. 367 ПРОВЕРОК — ЗАМЕРЕНО, ОБВИНЕНИЕ СНЯТО

Собрано отдельным деревом (`git worktree`, Release, AppleClang 17, те же
GIGA-опции и флаги — сверено по `CMakeCache.txt`):

| Дерево | `game_test` печатает |
|---|---|
| `bb62c64a` (его коммит) | **243209** |
| `137f3052` (его РОДИТЕЛЬ, нетронутый) | **243209** |
| `97bdf13e` (торовая сторона того же слияния) | **243209** |

`git diff 97bdf13e 137f3052 -- tests/ CMakeLists.txt` — **пусто**.
Счёт не зависит ни от рабочего каталога (проверено: корень репо, `build/`,
пустой `/tmp`), ни от кэшей.

**Вывод: marko1olo не терял 367 проверок. Он поправил пин, который на его базе
уже не воспроизводился.** Число `243576` было записано 2026-08-17
(«`243565 -> 243576 (+11)`, light_bake») и на этой машине не печаталось никогда
— то есть **ctest на `97bdf13e` был красным, и это наш дефект, а не его**.
Наша линия ушла вперёд и сегодня пинится своим числом (было 223288), поэтому
протухший пин больше ни разу не укусил.

Что из этого следует для процесса: пин обязан быть **прогнан на той машине, где
записан**, а не переписан по памяти. «Понизил пин» — сильное обвинение, и
проверять его нужно сборкой, а не чтением дифа. Стоило это полчаса, как и
оценивалось.

---

## 2. РАЗДЕЛИМОСТЬ — проверено, две системы независимы

| Система | Файлы | Пересечение |
|---|---|---|
| **ГЕЙМПАД** | `src/input/input.h`, `input.cpp`, `SDL_INIT_GAMEPAD` в `main.cpp`, вызов `input.action_held()` | две строки плюмбинга в `main.cpp` |
| **СТЕРЕО/VR** | `src/sim/camera.{h,cpp}`, `src/render/raymarch_pass.{h,cpp}`, `vk_renderer.{h,cpp}`, `settings_ui.{h,cpp}`, `hud_ui.{h,cpp}` | тот же `main.cpp` |

Взять одно без другого **можно**.

---

## 3. ТАБЛИЦА РЕШЕНИЙ

### БРАТЬ — геймпад

**Обоснование:** закрывает настоящую дыру, названную аудитом
([steam-readiness.md](steam-readiness.md) трек D): «подсистема геймпада даже не
инициализируется, ни одного `SDL_Gamepad` в репозитории». Для страницы Стима —
строка в поддерживаемых устройствах.

**Что в нём сделано хорошо и стоит сохранить:** `InputState` получил
правило пяти с удалённым копированием и мувами — потому что теперь владеет
ресурсом `SDL_Gamepad*`. Это аккуратно, не переписывать.

**Что чинить при переносе:**
* `gamepadLookSpeed_ = 2.5f` — назначенное число без вывода. Закон S11:
  константа выводится, вывод пишется рядом. Вывести от чувствительности мыши
  или от времени полного оборота, и записать откуда.
* `lastDt_ = 1.0f / 60.0f` — **допущение 60 Гц в проекте, где сим 125 Гц**
  (`core/tick.h`). Разобраться, что это за поле и почему не `kSimDt`.
* `action_held()` через триггеры/бамперы склеивает две руки в одну кнопку —
  у нас руки раздельные (two-hands.md). Развести: левый триггер — левая рука,
  правый — правая.
* Мёртвые поля проверить: `gamepadJumpEdge_`, `gamepadFlyToggleEdge_` — есть ли
  потребители.

**Гейт (обязателен, в том же коммите):** `suite_keybind.inl` уже стережёт бинды
— дописать туда ветку геймпада: отсутствие устройства не ломает ввод; оси в
мёртвой зоне дают ноль; триггер левый/правый попадает в свою руку. Мутировать в
обе полярности поштучно.

### СПРОСИТЬ ВЛАДЕЛЬЦА — стерео/VR

Не «трём» и не «берём» — это решение об ОЧЕРЁДНОСТИ, а не о качестве.

**Аргумент против сейчас:** стерео удваивает работу пасса, который **и есть
узкое место кадра**: `raster` = 13.9-15.2 мс из 17-18 мс GPU-кадра (83%),
fps 22-56 на M2 Pro (`problems.md` §67.2). И делается это до того, как игра
запускается хоть у кого-то, кроме владельца (трек B: билда не существует).

**Аргумент за:** сама математика сделана вменяемо — `compute_stereo_camera` с
IPD параметром, дефолт `0.064` м (реальное межзрачковое расстояние, а не
выдуманное число), аспект глаза считается явно. Ручки в настройках есть.

**Если владелец говорит «потом»:** код не выбрасывать — сохранить
`compute_stereo_camera` как отдельный коммит-заготовку на ветке, не подключая к
`raymarch_pass`. Математика полезна и без режима.

**Если «берём»:** переносить ТОЛЬКО после трека B, с замером до/после на
`--shot` и честным вердиктом по кадру.

### ТРЁМ

* **Понижение пина** — не переносить ни в каком виде. Пин на torus свой
  (`223288`) и растёт от наших коммитов.
* Всё, что тянет за собой старую сигнатуру `player_ranged_step` и прочий код до
  two-hands, — не переносить, переписывать на сегодняшний API.

---

---

## 3а. ЧТО СДЕЛАНО 2026-09-12 (исполнение)

**Геймпад — ВЗЯТ.** `1e0b5cfe`. Перенесён по смыслу, не cherry-pick.

* Вся арифметика вынесена в `src/game/gamepad.h` — headless, без SDL, линкуется
  в `giga_game`. Это и есть ответ на «нечем гейтить»: `src/input` в тесты не
  линкуется, а `giga_game` линкуется. Мост теперь только снимает SDL и
  складывает результат в `PlayerCommand`.
* `gamepadLookSpeed_ = 2.5f` → **выведено**: ω = v / r =
  `Controller::moveSpeed` / `kCellSize` = 6 / 2 = **3 рад/с**. Взгляд обязан
  удерживать точку, мимо которой тело идёт по касательной на полной скорости на
  дистанции клетки. Вывод пинится строкой теста, а не комментарием.
* `lastDt_ = 1/60` **убит**. `dt` — параметр `build_command`; взгляд с геймпада
  есть СКОРОСТЬ, кэш прошлого кадра врал на любой смене частоты.
* **Две руки разведены**: левый триггер — левая рука (ЛКМ), правый — правая
  (ПКМ), зажимаются одновременно. `action_held()` оригинала не перенесён.
* **Утечка ввода закрыта**: оригинал ставил `Button::Attack` ВНЕ гейта по
  `mouselook_` — триггер бил сквозь открытый инвентарь.
* Тоггл полёта на кнопку **не заведён**: у клавиатуры его тоже нет (чистка
  2026-08-28), завести на геймпаде значило бы молча отменить то решение. Ось
  полёта жива и работает только в полёте.
* Мёртвые поля (`gamepadFlyToggleEdge_`, `gamepadAttackHeld_`) не перенесены.
  Правило пяти на владении `SDL_Gamepad*` сохранено — там сделано аккуратно.
* Гейт: `tests/suite_gamepad.inl`, **+50 проверок**, каждая в обе полярности.
  Мутации прогнаны: наивная мёртвая зона → 2 красные; склейка рук → 2 красные.

**Стерео — ОТЛОЖЕНО, математика сохранена.** `83f3d2db`. Решение владельца.
`compute_stereo_camera` живёт в `sim/camera.cpp` под гейтом
(`test_stereo_camera_splits_by_ipd`, +13), к `raymarch_pass`/`main.cpp`/
настройкам **не подключено**. Заодно убрана копия near/far и переворота Y:
теперь один `camera_matrices()` на монокуляр и на каждый глаз. Мутации: глаза
местами → красный знак базы; снята защита вырожденной оси → зенит рвёт базу.

**Стёрто.** Понижение пина (наш пин свой и растёт), весь код до two-hands,
подключение стерео к пассам.

`ctest 8/8`. `game_test` 223288 → 223338, `world_test` 23617 → 23630.

---

## 4. ВОПРОС, КОТОРЫЙ НАДО ЗАДАТЬ АВТОРУ

~~**Откуда 367 пропавших проверок?**~~ **Вопрос снят: проверено сборкой (§1а),
проверки никуда не делись, пин был протухшим у НАС.** Спрашивать не о чем —
наоборот, обвинение надо было бы взять назад, если бы оно было озвучено.

Остаётся сказать автору одно, и без обвинительного тона: **ветка разработки —
`torus`, не `main`**; работа от базы старше недели требует перебазирования до
пуша, иначе получается месяц архитектурного расхождения в одном коммите.

---

## 5. ПОРЯДОК РАБОТ

1. ✅ Собрать `bb62c64a` в отдельном каталоге сборки, прогнать ctest, **найти,
   где 367 проверок**. Записать сюда результат. → §1а, обвинение снято.
2. ✅ Перенести ГЕЙМПАД на torus по смыслу (не cherry-pick), с исправлениями из
   §3 и гейтом в том же коммите. ctest 8/8. → `1e0b5cfe`.
3. ✅ Спросить владельца по стерео (§3). Исполнить решение. → «отложить,
   математику сохранить», `83f3d2db`.
4. ✅ Слить torus в main **нашей линией поверх**: main принимает torus, как все
   предыдущие 149 раз. Конфликты разрешать в пользу torus везде, где спор идёт
   о коде старше 17 августа.
5. Сообщить автору: ветка разработки — **torus**, не main; работа от базы
   старше недели требует перебазирования до пуша.

### Хвост на потом

* **Пин `223338` записан на macOS/AppleClang.** Раз `243576` оказался
  невоспроизводимым, стоит один раз проверить, печатает ли то же число
  Windows-сборка (Петушков) — иначе храповик ловит платформу, а не регрессию.
* Стерео подключать **только после трека B** (билд существует) и с замером
  кадра до/после на `--shot`.

---

## 6. ЧЕГО НЕ ДЕЛАТЬ

* Не разрешать конфликт в `raymarch_pass.cpp` текстом. Это 83% кадра; спорные
  строки там — про дескрипторные пулы и число сетов, и «выбрать одну сторону»
  наугад = тихая поломка рендера.
* Не принимать понижение пина «чтобы собралось».
* Не выбрасывать геймпад заодно со стерео — это разные решения.
* Не переносить ничего без гейта в том же коммите.
