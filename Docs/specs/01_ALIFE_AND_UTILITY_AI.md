# 01 — A-Life, Utility AI и социальные роли

> Сверено с деревом **2026-08-09** на `sync/gravity-on-owner-main` (HEAD `2610479`).
> Закон: [AGENTS.md](../../AGENTS.md). Контракт подсистемы: [ai.md](../../ai.md),
> [npcs.md](../../npcs.md), [macrosim.md](../../macrosim.md). Реестр дефектов:
> [problems.md](../../problems.md) §27, §35.
> Этот файл — **проект**, а не отчёт о построенном. Всё, что уже работает, вынесено
> в §1 именно для того, чтобы его не спроектировали второй раз.

---

## 1. Что УЖЕ построено — не проектировать заново

Мандат сессии предполагал «расширение 13-интентового Utility AI». Тринадцать
интентов существуют, **и они живые**: `main.cpp:2126` ставит `aiCfg.enabled = true`,
`ai_init` вызывается из двух мест (`main.cpp:1377`, `main.cpp:1914`), `ai_step`
крутится в фиксированном тике (`main.cpp:2873`).

| Механизм | Где | Состояние |
|---|---|---|
| 13 интентов, argmax + гистерезис | [ai.h:253-268](../../src/game/ai.h), `select_intent` | ЖИВОЙ |
| Стаггер по идентичности, 16-байтный `AiBrain` | [ai.h:306-324](../../src/game/ai.h) | ЖИВОЙ |
| Токен владения движением `MotionOwner` | [ai.h:292-295](../../src/game/ai.h) | ЖИВОЙ, два guard-а на месте |
| Память фактов `AiMemory`, 8 слотов, 64 Б/строка | [ai.h:579-631](../../src/game/ai.h) | ЖИВАЯ, demand-колонка |
| Зональные поля комнат, 3 бита × 2 МиБ | [room_zone.h:331-345](../../src/game/room_zone.h) | ЖИВЫЕ |
| Восстановление нужд в комнате | [room_zone.h:192-204](../../src/game/room_zone.h) | ЖИВОЕ |
| Мебель как контент (стол, плита, унитаз, койка) | [room_zone.h:251-263](../../src/game/room_zone.h) | ЖИВАЯ |
| Матрица фракций 6×6 + `rel_` рёбра | [faction.h](../../src/game/faction.h), `macro_sim.cpp` | ЖИВАЯ |
| Диффузионное поле опасности + градиент бегства | [diffusion.md](../../diffusion.md) | ЖИВОЕ |

**Вывод:** задача направления 1 — не «построить Utility AI», а **закрыть шесть
названных дыр** в уже работающем. Они перечислены ниже и каждая имеет владельца
в коде.

---

## 2. Контекст и проблемы кода

### 2.1. Роли не существует как понятия

`kFactionTraits` ([ai.h:387-394](../../src/game/ai.h)) — таблица на 6 строк,
индексируемая **фракцией**. Все шесть строк имеют `workDrive = 0.50f`, и это
задокументировано как заглушка: *«workDrive is the reference default 0.5
everywhere until occupations land»*.

Следствие: Жилец, Дежурный, Санитар, Мародёр и Сектант **неотличимы**, если
принадлежат одной фракции. Поведенческая дисперсия сегодня берётся только из
`identity_seed`-джиттера, то есть это шум, а не роль.

### 2.2. Три интента выигрывают и никуда не идут

`kRoomAffordance` ([room_zone.h:120-125](../../src/game/room_zone.h)) содержит
ровно четыре строки:

```
IntentEat    -> Kitchen
IntentDrink  -> Kitchen
IntentToilet -> Bathroom
IntentSleep  -> Living
```

`IntentWork`, `IntentSocial`, `IntentPatrol` строк не имеют, и заголовок это
прямо называет: *«work/social/patrol are a ROW away and deliberately not taken
yet»*. Побеждая, они делегируют `wander_step` — то есть решение «идти на работу»
неотличимо от блуждания.

### 2.3. `IntentHeal` — подтверждённый дедлок

[hunt.h:38-42](../../src/game/hunt.h): *«nothing heals a crowd body ([needs.h]
ticks the camera holder's row alone)»*. Строка `Medical` в `kRoomRecovery` —
нулевая, и [room_zone.h:178-180](../../src/game/room_zone.h) объясняет почему: HP
у толпы целочисленное, дробного банка нет. Тело с низким HP выбирает `IntentHeal`,
восстановиться не может и залипает в неудовлетворимой аварии до смерти.

### 2.4. Шесть видов памяти не имеют ни одного производителя

[ai.h:1009-1013](../../src/game/ai.h) честно перечисляет: `MemFood`, `MemWater`,
`MemRest`, `MemToilet`, `MemFoe`, `MemAlly` **не пишет никто** в `src/`. Читающая
сторона готова (`ai_recall` → `Perception::localScore`/`targetPenalty`), но термы
всегда нулевые. Это дефект класса §35 «данные, которые код не читает», только
зеркальный: **читатель есть, писателя нет**.

Прямое следствие для `grudge`: `Perception::grudge` заполняется из `MemFoe`,
которого никто не создаёт, поэтому `IntentFactionAssault` не может сработать по
личной обиде — только по матрице фракций.

### 2.5. `Perception::armed` всегда false

[ai.h:721](../../src/game/ai.h) — поле стаба. Скорер `IntentCombat` читает
`armed`, `IntentFlee` вычитает по нему. Оружие в инвентаре есть
(`ranged_pick.cpp` выбирает лучший ствол), но в восприятие оно не попадает.
Толпа принимает решения «драться или бежать», не зная, вооружена ли она.

### 2.6. Таксономия комнат двумерна

[room_zone.h:71-76](../../src/game/room_zone.h): комната — это **колонка сквозь
все этажи-перекрытия**, ключ `(kind, number, rx, ry)`, без `z`. Кухня на первом
ярусе и кухня на седьмом — одна и та же комната для поля. Это осознанное решение
(три существующих потребителя читают так же), но оно ограничивает «свидание в
конкретной комнате» из манифеста §4.

### 2.7. Скрытые издержки на теле-тик

Из частичного аудита (подтверждено чтением заголовков): `door_nearest_shelter`
для каждого бегущего тела делает линейный проход по дверям, а
`diffusion_gradient` — рассыпанные чтения в 128³. Оба на пути `ai_step`. При 16k
воплощённых это не O(n) с малой константой, а O(n·D).

---

## 3a. Пять ролей — полная поведенческая спецификация

Мандат владельца назвал пять ролей поимённо. Ниже каждая расписана до уровня,
на котором её можно реализовать без домысливания: чем она отличается **в
числах**, какие интенты для неё меняют смысл, что она делает в кризисе, и с кем
конфликтует.

Общий принцип, повторяемый для всех пяти: **роль не добавляет интентов**.
Тринадцать интентов остаются. Роль меняет (а) множители в скоринге,
(б) какие комнаты считаются «своими», (в) реакцию на кризис.

---

### 3a.1. Жилец (`RoleId::Resident`) — база, относительно которой читаются прочие

**Доля популяции:** ~70 % на жилом этаже, ~20 % на промышленном.

| Черта | Значение | Почему |
|---|---|---|
| `workDrive` | 0.30 | работает мало и неохотно |
| `patrolDrive` | 0.05 | не патрулирует вообще |
| `sociability` | 1.00 | базовая социальность |
| `scavengeDrive` | 0.10 | подберёт, если под ногами |
| `careDrive` | 0.05 | не лечит чужих |
| `homeRooms` | `Living` | своя квартира |
| `workRooms` | `Common` | максимум — общая комната |

**Суточный контур** (когда появятся часы; сегодня `minuteOfDay = -1`):
`Living` (сон) → `Kitchen` (еда) → `Bathroom` → `Common` (социальность) →
`Living`. Это уже работающий цикл: кухня заряжает `pendingPoo`/`pendingPee`,
давления растут, выигрывает `IntentToilet`, тело идёт в `Bathroom`. Петля из двух
строк данных и без единого автомата состояний
([room_zone.h:156-162](../../src/game/room_zone.h)).

**В кризисе:** высокий `panic` (0.50 у Citizens), низкий `risk` (0.32). Бежит
первым, бежит в `homeRooms`, а при наличии гермозон — к ближайшей гермодвери.
Не защищает, не помогает.

**Роль-«ноль»:** её черты равны сегодняшним фракционным значениям Citizens, что
даёт критерий обратной совместимости — **этаж, полностью населённый Жильцами,
ведёт себя байт-в-байт как сегодня**.

---

### 3a.2. Дежурный (`RoleId::Duty`) — первый потребитель `route_step`

**Доля:** 2–5 %, но влияние непропорционально доле.

| Черта | Значение | Отличие от Жильца |
|---|---|---|
| `workDrive` | 1.00 | ×3.3 |
| `patrolDrive` | 1.00 | **×20** |
| `sociability` | 0.60 | ×0.6 |
| `scavengeDrive` | 0.05 | ×0.5 |
| `careDrive` | 0.20 | ×4 |
| `homeRooms` | `Living` | |
| `workRooms` | `Hq` | |

**Механика, которой нет ни у кого другого — маршрут.** `IntentPatrol` для
Дежурного означает не «ходить по коридору», а **обход узлов решётки 4×4×4**.
Это закрывает годовой долг: `route_step` написан, отлажен, покрыт тестами и
**не имеет ни одного потребителя** ([master_prompt.md](../../master_prompt.md) §7 #11).

```
patrol_step(body, plan, coarse, fine):
    # Обход по решётке: тело идёт от nodeFrom к nodeTo, добравшись —
    # выбирает следующий узел детерминированно от своей identity.
    dir = nav::route_step(coarse, fine, myCell, lattice_cell(plan.nodeTo))
    if dir == kFlowArrived:
        plan.nodeFrom = plan.nodeTo
        # Следующий узел — сосед по решётке, выбранный от identity и hops.
        # Детерминированно, поэтому маршрут ВОСПРОИЗВОДИМ: игрок может
        # выучить обход конкретного дежурного. Это геймплей, а не побочный
        # эффект.
        plan.nodeTo = lattice_neighbor(plan.nodeTo,
                          rand_below(hash3(idSeed, plan.hops, kPatrolSalt), 6))
        plan.hops++
    else:
        steer_along(dir, kErrandSpeed)
```

**Реакция на шум.** Дежурный — единственная роль, читающая поле шума
(`noise.cpp`) как вход в скоринг: услышанный шум поднимает `IntentInvestigate`
через существующий `investigate.cpp`, который сегодня работает **только для
мобов** ([ai.h:24-27](../../src/game/ai.h)).

**В кризисе:** `risk` берётся у Ликвидаторов (0.74), `panic` низкий (0.22).
Дежурный **не бежит** — он идёт к гермодвери и **стоит рядом**, что и есть
«групповая защита» из мандата: не новая механика, а роль, которая не убегает.

**Конфликт с Мародёром — первый эмерджентный сценарий.** Дежурный, чей маршрут
проходит через `Storage`, встречает там Мародёра. Матрица фракций может быть
нейтральна, но **роли антагонистичны**, и это разрешается уже существующим
механизмом: Дежурный подаёт `ai_remember_actor(MemFoe)` на Мародёра, у того
падает отношение, и в следующую встречу `IntentFactionAssault` получает вход.
**Ни одной новой системы** — только производитель памяти, которого сегодня нет
(§2.4).

---

### 3a.3. Санитар (`RoleId::Medic`) — единственная роль с механикой на других

**Доля:** 1–3 %.

| Черта | Значение | Отличие |
|---|---|---|
| `workDrive` | 1.00 | ×3.3 |
| `patrolDrive` | 0.20 | ×4 |
| `sociability` | 0.80 | ×0.8 |
| `scavengeDrive` | 0.10 | = |
| `careDrive` | **1.00** | **×20** |
| `workRooms` | `Medical` | единственная роль, которой Medical — «своя» |

**Это единственная роль, чья механика действует на ЧУЖОЕ тело.** Все прочие
роли меняют своё поведение; Санитар меняет чужой `Needs::hpBank`.

```
medic_step(medic, nearby, dt):
    # Санитар несёт восстановление С СОБОЙ. Радиус мал — это не аура,
    # а «подошёл и обработал»: тело должно физически дойти.
    for body in nearby within kMedicReachM:            # 2.0 м = одна клетка
        if hp_frac(body) < kMedicThreshold:            # 0.6
            needs(body).hpBank += kMedicHealPerSec * dt
            # Санитар ТРАТИТ на это: расходник из инвентаря либо
            # собственный сон. Бесплатное лечение сделало бы роль
            # доминирующей — этаж с двумя санитарами стал бы бессмертным.
            needs(medic).sleep -= kMedicFatiguePerSec * dt
```

**`careDrive` даёт новый вход в скоринг** — `Perception::nearbyWounded01`. Это
единственное новое поле восприятия во всей спеке, и оно заполняется тем же
проходом, который уже считает соседей.

**Почему Санитар обязан появиться раньше честного дружественного огня.**
[Спека 09](09_COMBAT_BALLISTICS_AND_RPG.md) §3.1 удаляет `Projectile::team`, и
толпа начинает получать сопутствующий урон. Без Санитара `crowd_dead_total`
растёт линейно, и `hunt.h:38-42` уже доказал, что размазывание урона смертность
не снижает. **Санитар — предпосылка, а не украшение.**

**В кризисе:** идёт **навстречу** раненым, а не от опасности. Это делает роль
видимо героической и одновременно смертной — Санитар должен гибнуть чаще
Жильца, и это правильный баланс, а не дефект.

---

### 3a.4. Мародёр (`RoleId::Looter`) — роль без дома

**Доля:** 3–8 %, растёт с `|floor|`.

| Черта | Значение | Отличие |
|---|---|---|
| `workDrive` | 0.20 | ×0.67 |
| `patrolDrive` | 0.10 | ×2 |
| `sociability` | 0.40 | ×0.4 |
| `scavengeDrive` | **1.00** | **×10** |
| `careDrive` | 0.00 | **ноль** |
| `homeRooms` | **0** | **нет дома** |
| `workRooms` | `Storage` | |

**`homeRooms = 0` — это не пропуск, а определение роли.** Мародёр не имеет
своей комнаты, поэтому `IntentSleep` не находит цели и делегирует `wander_step`.
Он спит там, где застало. Одно нулевое поле создаёт видимо иное поведение без
единой строки кода.

**Механика: очередь целей от памяти.** Мародёр — первый настоящий потребитель
`MemFood`/`MemWater`/`MemRest`, которые сегодня без производителей (§2.4).

```
looter_target(memory, id, now):
    # Мародёр идёт к ЗАПОМНЕННОМУ контейнеру, а не к ближайшему.
    # Это принципиально: он должен знать этаж лучше игрока, и знание
    # приходит от того, что он там был.
    recall = ai_recall(memory, id, cx, cy, cz, now)
    best = argmax over kind in {MemFood, MemWater, MemRest} of
               recall.siteStrength[kind] / (1 + recall.siteDistCells[kind] * 0.1)
    return best
```

**Конфликт с игроком встроен.** Мародёр опустошает контейнеры, к которым идёт
игрок. Это не скрипт — следствие того, что обе стороны используют один и тот же
контейнерный слой. **Гонка за ресурс возникает сама.**

**В кризисе:** `risk` 0.60, `panic` 0.35. Не бежит к гермодвери — бежит к
**ближайшему неопустошённому контейнеру**, потому что самосбор для него —
возможность. Это делает Мародёра единственной ролью, для которой кризис
**повышает** активность.

---

### 3a.5. Сектант (`RoleId::Cultist`) — роль, требующая группы

**Доля:** 0 % на жилых, до 40 % на `Derelict` (`factionMix{1,0,4,0,3}`).

| Черта | Значение | Отличие |
|---|---|---|
| `workDrive` | 0.60 | ×2 |
| `patrolDrive` | 0.40 | ×8 |
| `sociability` | **1.20** | **выше 1.0** |
| `scavengeDrive` | 0.30 | ×3 |
| `careDrive` | 0.10 | ×2 |
| `workRooms` | `Smoking` \| `Hq` | сходки |

**`sociability = 1.20` — единственное значение выше единицы во всей таблице**, и
это намеренно: Сектант тянется к своим сильнее, чем Жилец к кому угодно.

**Механика сбора — переиспользование поля паники с обратным знаком.**

```
cultist_gather_step(cultist, rallyField, dt):
    # Сектанты ИЗЛУЧАЮТ в поле сбора и ЧИТАЮТ его же. Тот же контур,
    # что паника (§4.2), только знак градиента противоположен:
    # паника гонит ОТ источника, сбор тянет К нему.
    cell = wrap_macro(pos / kCellSize)
    rallyField.at(cell) += kRallyEmit * dt * rt.sociability

    # IntentSocial для Сектанта идёт ВВЕРХ по градиенту.
    if intent == IntentSocial:
        steer_along(+diffusion_gradient(rallyField, cell), kErrandSpeed)
```

**Почему это даёт настоящую сходку.** Поле — общее. Двое Сектантов, оказавшихся
рядом, поднимают его вдвое, притягивая третьего сильнее. Возникает
положительная обратная связь с естественным пределом: тела физически не влезают
в одну клетку (swept-AABB), а испарение поля гасит контур, если приток
прекратился. **Толпа собирается сама, кода толпы нет.**

**В кризисе:** Сектанты **не бегут**. `panic` 0.35, и самосбор для них —
событие, а не угроза. Они идут в `Hq`. Это создаёт видимую аномалию: пока
жилой этаж бежит к гермодверям, `Derelict` собирается в одной комнате.

---

### 3a.6. Таблица ролей целиком

```cpp
inline constexpr RoleTraits kRoleTraits[static_cast<std::size_t>(RoleId::kRoleCount)] = {
  /*                workDrv patrol social scaveng  care  homeRooms                     workRooms */
  /* Resident */ {   0.30f, 0.05f, 1.00f, 0.10f, 0.05f, room_bit(RoomBit::Living),
                     room_bit(RoomBit::Common)},
  /* Duty     */ {   1.00f, 1.00f, 0.60f, 0.05f, 0.20f, room_bit(RoomBit::Living),
                     room_bit(RoomBit::Hq)},
  /* Medic    */ {   1.00f, 0.20f, 0.80f, 0.10f, 1.00f, room_bit(RoomBit::Living),
                     room_bit(RoomBit::Medical)},
  /* Looter   */ {   0.20f, 0.10f, 0.40f, 1.00f, 0.00f, 0,
                     room_bit(RoomBit::Storage)},
  /* Cultist  */ {   0.60f, 0.40f, 1.20f, 0.30f, 0.10f, room_bit(RoomBit::Living),
                     room_bit(RoomBit::Smoking) | room_bit(RoomBit::Hq)},
};
```

**Пять чисел на роль. Двадцать пять чисел на всю систему.** Альтернатива —
пять ролей × тринадцать интентов = 65 коэффициентов, каждый из которых пришлось
бы балансировать вручную, потеряв референсную базу.

---

### 3a.7. Раздача ролей — детерминированная функция, не хранимое решение

```
role_for(npcId, floorKind, faction):
    # Роль выводится из id, а не назначается и хранится. Тот же приём,
    # что seat_offset и identity_seed: функция от стабильного ключа не
    # требует ни байта хранения и переживает лифт бесплатно.
    #
    # НО: роль может МЕНЯТЬСЯ по сюжету (Жилец стал Мародёром после
    # самосбора), поэтому колонка role_ в пуле всё же нужна — как
    # ПЕРЕОПРЕДЕЛЕНИЕ, инициализируемое этой функцией.
    h = hash3(npcId, floorKind, kRoleSalt)
    # Веса зависят от типа этажа: на Derelict Сектантов много,
    # на Residential нет вовсе.
    return weighted_pick(h, kRoleWeights[floorKind])
```

```cpp
// Веса ролей по типу этажа. Строки данных, не ветвление.
inline constexpr std::uint8_t kRoleWeights[kFloorKindCount][kRoleCount] = {
  /*                Resident Duty Medic Looter Cultist */
  /* Residential */ {   70,    5,    3,     3,     0 },
  /* Commercial  */ {   50,   10,    3,     8,     2 },
  /* Industrial  */ {   30,   15,    5,     8,     5 },
  /* Derelict    */ {   10,    2,    1,    20,    40 },
  /* Padic       */ {   20,    5,    2,    15,    20 },
};
```

**Проверяемое следствие:** гистограмма ролей на `Residential` и на `Derelict`
обязана различаться качественно, и это ровно тот критерий приёмки, который
отличает работающую систему от таблицы, которую никто не читает.

---

## 3b. Эмерджентные сценарии, возникающие БЕЗ дополнительного кода

Список ниже — не пожелания, а следствия уже описанного. Каждый возникает из
пересечения двух-трёх механизмов и не требует ни одной новой системы. Это
проверка на то, что дизайн ролей действительно эмерджентный, а не декоративный.

| # | Сценарий | Из чего складывается |
|---|---|---|
| 1 | Дежурный ловит Мародёра в кладовой и запоминает его врагом | маршрут по решётке × `workRooms=Storage` × `MemFoe` |
| 2 | Санитар гибнет чаще всех, и этаж деградирует | `careDrive` идёт навстречу урону × отсутствие второго Санитара |
| 3 | Сектанты собираются, пока жилой этаж бежит | поле сбора × `panic 0.35` × гермозоны |
| 4 | Игрок приходит к пустому контейнеру | Мародёр использует тот же контейнерный слой |
| 5 | Кухня→туалет→кухня как видимый суточный ритм | `pendingPoo` × `kRoomRecovery` × аффордансы |
| 6 | Смерть вожака секции разваливает её поведение | иерархия §4.3 × бонус к `workDrive` |
| 7 | Мародёр спит в коридоре | `homeRooms = 0` → `IntentSleep` без цели |
| 8 | Паника распространяется волной по коридору | эмиссия в `danger` × диффузия × чтение того же поля |
| 9 | Дежурный идёт на выстрел игрока | поле шума × `investigate` (сегодня только для мобов) |
| 10 | На `Derelict` некому лечить | `kRoleWeights[Derelict]` даёт 1 % Санитаров |

**Сценарий 10 — пример того, как таблица весов создаёт сложность бесплатно.**
Никто не писал «на заброшенных этажах опаснее». Это следствие того, что
Санитаров там почти нет, а Мародёров вдвое больше.

---

## 3. Проектируемые структуры

### 3.1. Роль как строка таблицы, а не как ветвление

Роль **ортогональна** фракции: Санитар-Цитизен и Санитар-Учёный — одна роль,
разные фракции. Поэтому это второй индекс, а не расширение `kFactionTraits`.

```cpp
// src/game/role.h  (НОВЫЙ ФАЙЛ, giga_game)

// Занятие тела. Ordinal — индекс в kRoleTraits, поэтому порядок load-bearing
// ровно так же, как порядок IntentId ([ai.h] «The INDEX is load-bearing»).
enum class RoleId : std::uint8_t {
    Resident = 0,  // Жилец   — дом, кухня, сон; работы почти нет
    Duty,          // Дежурный— патруль по маршруту, реагирует на шум
    Medic,         // Санитар — идёт к раненым, единственный носитель heal-банка
    Looter,        // Мародёр — идёт к контейнерам и трупам, избегает патрулей
    Cultist,       // Сектант — идёт в Hq/Smoking, собирается в группы
    kRoleCount
};

// То же по форме, что kFactionTraits: чистые данные, без поведения.
// Умножается на фракционные черты, не заменяет их — фракция говорит
// «кто мы», роль говорит «чем занят».
struct RoleTraits {
    float workDrive;     // множитель к IntentWork
    float patrolDrive;   // множитель к IntentPatrol
    float sociability;   // множитель к IntentSocial
    float scavengeDrive; // НОВЫЙ терм: тяга к контейнерам/трупам
    float careDrive;     // НОВЫЙ терм: тяга к раненым
    std::uint16_t homeRooms;  // RoomBit-маска «где я живу»
    std::uint16_t workRooms;  // RoomBit-маска «где я работаю»
};

inline constexpr RoleTraits kRoleTraits[static_cast<std::size_t>(RoleId::kRoleCount)] = {
  /* Resident */ {0.30f, 0.05f, 1.00f, 0.10f, 0.05f,
                  room_bit(RoomBit::Living), room_bit(RoomBit::Common)},
  /* Duty     */ {1.00f, 1.00f, 0.60f, 0.05f, 0.20f,
                  room_bit(RoomBit::Living), room_bit(RoomBit::Hq)},
  /* Medic    */ {1.00f, 0.20f, 0.80f, 0.10f, 1.00f,
                  room_bit(RoomBit::Living), room_bit(RoomBit::Medical)},
  /* Looter   */ {0.20f, 0.10f, 0.40f, 1.00f, 0.00f,
                  0,                        room_bit(RoomBit::Storage)},
  /* Cultist  */ {0.60f, 0.40f, 1.20f, 0.30f, 0.10f,
                  room_bit(RoomBit::Living), room_bit(RoomBit::Smoking)},
};
```

**Где хранится роль.** НЕ компонентом. По тому же аргументу, по которому `Needs`
и `AiMemory` живут в пуле, а не на сущности ([ai.h:140-148](../../src/game/ai.h)):
лифт уничтожает тело, а роль обязана пережить поездку. Роль — 3 бита в
**существующем** 8-слотовом блоке `attrs` пула (`NpcPool`, слоты 3..7 свободны по
[README.md](../../README.md) §5), либо новая узкая колонка `role_` шириной 1 Б =
1.0 МиБ при 2²⁰. Рекомендация: **отдельная колонка `uint8`**, потому что `attrs`
зарезервирован манифестом под 8 RPG-атрибутов и займётся ими.

### 3.2. Расширение таблицы аффордансов — строки, не код

```cpp
// src/game/room_zone.h — ДОПОЛНЕНИЕ к kRoomAffordance
inline constexpr RoomAffordance kRoomAffordance[] = {
    {IntentEat,    room_bit(RoomBit::Kitchen)},
    {IntentDrink,  room_bit(RoomBit::Kitchen)},
    {IntentToilet, room_bit(RoomBit::Bathroom)},
    {IntentSleep,  room_bit(RoomBit::Living)},
    // --- НОВОЕ ---
    {IntentWork,   room_bit(RoomBit::Production) | room_bit(RoomBit::Office)
                 | room_bit(RoomBit::Hq)},
    {IntentSocial, room_bit(RoomBit::Common)  | room_bit(RoomBit::Smoking)},
    {IntentPatrol, room_bit(RoomBit::Corridor)},
    {IntentHeal,   room_bit(RoomBit::Medical)},
};
```

`kRoomFieldMask` вычисляется из таблицы автоматически, поэтому бейк подхватит
новые биты без правки. **Цена:** поля пекутся только для битов, реально
присутствующих на этаже (`bake_room_zones` фильтрует), но верхняя граница растёт
с 3 до 8 бит × 2 МиБ = **16 МиБ на этаж**. Это укладывается в бюджет
[performance.md](../../performance.md) и должно быть напечатано в логе бейка.

`IntentPatrol → Corridor` — намеренно вырожденный случай: коридор есть везде, и
поле коридора почти всюду `kFlowArrived`. Патруль обязан получить **маршрут**, а
не точку, поэтому для него нужна отдельная механика (§3.4), а строка выше —
только чтобы интент перестал молча делегировать.

### 3.3. Heal-банк: закрытие дедлока §2.3

Минимальное изменение, которое делает `IntentHeal` осмысленным. Дробный банк —
одно `float`-поле в существующей структуре `Needs`, а не новая система:

```cpp
// src/game/needs.h — ДОПОЛНЕНИЕ
struct Needs {
    // ... существующие поля ...
    // Дробный банк заживления. HP у толпы целочисленный, поэтому «+2.8 HP/с»
    // невыразимо напрямую: банк копит дробь, а целая часть переливается в hp
    // и списывается отсюда. Тот же приём, что pendingPee/pendingPoo уже
    // используют для давлений — накопитель, а не второй счётчик здоровья.
    float hpBank = 0.0f;
};
```

```cpp
// src/game/room_zone.h — строка Medical перестаёт быть нулевой
struct RoomRecovery {
    float food, water, sleep, pee, poo, pendingPee, pendingPoo;
    float hpBank;   // НОВОЕ: + HP в секунду, дробно
};
/* 7 Medical */ {0,0,0,0,0,0,0, /*hpBank*/ 1.2f},
```

Санитар (`RoleId::Medic`) получает право нести восстановление **с собой**: это
единственная роль, для которой `room_recover` применяется к соседу, а не к себе.
Так «Санитар» становится ролью с механикой, а не перекраской.

### 3.4. Патруль как маршрут по решётке — переиспользование, а не новая система

Патруль не нуждается в новом поле. 64-узловая решётка `lattice.h` уже
существует, `route_step` уже даёт O(1) шаг к любому узлу, и у него **до сих пор
нет потребителя** ([master_prompt.md](../../master_prompt.md) §7 #11: *«`route_step`
still awaits a consumer»*). Патруль — его естественный первый клиент.

```cpp
// src/game/ai.h — ДОПОЛНЕНИЕ к AiBrain. Влезает в существующие 16 байт?
// НЕТ: AiBrain уже ровно 16 Б и static_assert это пинует. Патрульная цель
// кладётся в отдельный РАЗРЕЖЕННЫЙ компонент, который есть только у тел с
// ролью Duty — их единицы процентов от толпы, поэтому дорогой плотной
// колонки они не стоят.
struct PatrolPlan {
    std::uint8_t nodeFrom = 0;   // lattice id 0..63
    std::uint8_t nodeTo = 0;
    std::uint8_t hops = 0;       // сколько узлов пройдено в текущем обходе
    std::uint8_t pad_ = 0;
};
static_assert(sizeof(PatrolPlan) == 4);
```

### 3.5. Производители памяти — закрытие §2.4

Ни одна из шести дыр не требует новой системы: у каждой уже есть система,
которая знает факт и его выбрасывает.

| Вид | Кто обязан вызвать `ai_remember_*` | Файл |
|---|---|---|
| `MemFood` / `MemWater` | открытие контейнера с едой/водой | `container.cpp` |
| `MemRest` | тело успешно поспало в комнате | `room_zone.cpp` |
| `MemToilet` | тело сняло давление в Bathroom | `room_zone.cpp` |
| `MemFoe` | получен урон от именованного NPC | `combat.cpp` |
| `MemAlly` | завершён положительный социальный обмен | `speech.cpp` |

Каждый вызов — одна строка, без аллокации, без реестра. Это заявлено самим
заголовком как дизайн: *«filing a fact is ONE call ... which is what makes it
cheap for combat/loot/container code to become a producer later»*.

---

## 4. Алгоритмы и псевдокод

### 4.1. Скоринг с ролью — умножение, не ветвление

```
score_intents(p, needs, out[13]):
    # существующий код без изменений до этой точки
    ft = faction_traits(p.faction)      # кто мы
    rt = role_traits(p.role)            # чем заняты   <- НОВОЕ

    # Роль МАСШТАБИРУЕТ уже посчитанный терм. Ни один коэффициент
    # референса не переписывается — это второй множитель, а не новая формула.
    out[IntentWork]   *= rt.workDrive
    out[IntentPatrol] *= rt.patrolDrive
    out[IntentSocial] *= rt.sociability

    # Два НОВЫХ аддитивных терма, оба гасятся угрозой тем же способом,
    # что и все прочие невыживательные интенты.
    out[IntentWork]   += rt.scavengeDrive * p.localScore[IntentWork] * (1 - threat)
    out[IntentHeal]   += rt.careDrive * p.nearbyWounded01 * (1 - threat)

    clamp each to [0, 100]
```

**Почему умножение, а не своя строка на роль:** пятнадцать ролей × тринадцать
интентов = 195 коэффициентов, которые придётся балансировать вручную. Умножение
даёт то же поведенческое разделение из пяти чисел на роль и сохраняет
референсный баланс как базу.

### 4.2. Паника как поле, а не как флаг

Манифест требует «механику паники и групповой защиты». Честная реализация —
**переиспользовать существующее диффузионное поле**, а не заводить эмоцию на теле:

```
panic_publish_step(reg, layer, danger_field, dt):
    # Тело в аварийном интенте ИЗЛУЧАЕТ в то же поле, из которого читает.
    # Это замыкает контур: один побежал -> поле поднялось -> соседи прочли
    # угрозу -> побежали -> подняли ещё. Стадо возникает само, кода стада нет.
    for each body on layer with AiBrain:
        if intent_is_emergency(brain.currentIntent):
            cell = wrap_macro(body.pos / kCellSize)
            danger_field.at(cell) += kPanicEmit * dt * ft.panic
            # ft.panic уже существует в kFactionTraits и до сих пор
            # использовался только на чтение
```

Стоимость: один O(n) проход, одна запись во float-поле на тело. Диффузия и
испарение уже есть и уже вызываются. **Групповая защита** — обратный знак того же
приёма: тело с высоким `risk` (Ликвидатор, `risk = 0.74`) излучает в поле
`rally` вместо `danger`, а `IntentCombat` читает его как аллюр.

### 4.3. Иерархия в запертой секции

Манифест требует «иерархию в запертых секциях Хруща». Не нужен ни граф власти, ни
новая система: иерархия — **производная от уже существующей матрицы отношений**.

```
# Вожак секции = argmax по (level, |rel_| положительных рёбер внутри секции).
# Считается ОДИН РАЗ при загрузке этажа, кладётся в RoomZones — это бейк,
# а не тик. AGENTS.md: «bake at load, tick in O(1)».
bake_section_hierarchy(pool, rooms, out):
    for each hermetic section S (комнаты, связные по дверям):
        best = argmax over residents r in S of
                   (pool.level(r) * kLevelWeight + positive_rel_count(r, S))
        out.leader[S] = best
```

Потребитель: `IntentWork`/`IntentPatrol` получают бонус, если вожак секции жив и
близко; смерть вожака обнуляет бонус, и секция видимо «разваливается». Это
проверяемое поведение, а не декоративный титул.

---

## 5. Критерии приёмки (Definition of Done)

Ни один пункт ниже не принимается зелёным тестом в одиночку. §27 закрывалась
именно печатаемыми числами на живом прогоне, и это правильный образец.

### 5.1. Роли

- [ ] `role_traits(RoleId)` — чистая функция, `constexpr`-таблица, `static_assert`
      на ширину.
- [ ] `suite_role.inl`: при **одной и той же** фракции и одном и том же
      `identity_seed` тела с разными ролями дают **различающийся** argmax
      минимум на 3 из 13 интентов. Это ровно тот тест, которого не хватает
      сегодня: он проверяет, что роль что-то меняет.
- [ ] Живой прогон печатает `[aimem] roles resident=N duty=N medic=N looter=N
      cultist=N` — гистограмма ролей.

### 5.1a. Механики отдельных ролей

**Обратная совместимость — первым, иначе остальное непроверяемо:**

- [ ] Этаж, населённый **только** Жильцами, ведёт себя **байт-в-байт** как до
      введения ролей. Черты Жильца равны сегодняшним значениям Citizens, поэтому
      это проверяемо прямым сравнением, а не на глаз.

**Дежурный:**
- [ ] `route_step` получает **первого потребителя** за год. Гейт: счётчик вызовов
      `route_step` из `ai_step` больше нуля и печатается.
- [ ] Маршрут **воспроизводим**: два прогона с одним `idSeed` дают одну
      последовательность узлов. Без этого игрок не может выучить обход, а это
      заявленный геймплей.
- [ ] Дежурный реагирует на шум: выстрел в 20 м меняет его цель. Тест на
      траекторию, не на флаг.
- [ ] `investigate.cpp` перестаёт быть только для мобов.

**Санитар:**
- [ ] Раненое тело рядом с Санитаром восстанавливает HP; **без** Санитара — нет.
      Прямое сравнение двух прогонов.
- [ ] Лечение **стоит** Санитару: его `sleep` падает. Бесплатное лечение делает
      роль доминирующей, и это надо исключить числом, а не намерением.
- [ ] `Perception::nearbyWounded01` заполняется существующим проходом по
      соседям, **без** второго обхода. Замер: время тела-тика не выросло.
- [ ] **Санитар живёт меньше Жильца** в среднем по прогону. Это критерий
      правильности роли, а не дефект.

**Мародёр:**
- [ ] `homeRooms = 0` даёт видимо иное поведение: Мародёр спит вне `Living`.
- [ ] Мародёр идёт к **запомненному** контейнеру, а не к ближайшему. Тест: тело
      с записанным `MemFood` в 30 м предпочитает его пустому контейнеру в 5 м.
- [ ] Контейнеры **пустеют** от Мародёров. Счётчик опустошённых за прогон
      печатается — иначе гонка за ресурс существует только на бумаге.
- [ ] В активной фазе самосбора активность Мародёра **растёт**, а не падает.

**Сектант:**
- [ ] Двое Сектантов в 10 м **сходятся**; двое Жильцов — нет.
- [ ] Сходка имеет предел: поле не растёт бесконечно, тела не накладываются.
- [ ] На `Derelict` сходка видна, на `Residential` Сектантов нет вовсе.

**Раздача ролей:**
- [ ] Гистограмма ролей на `Residential` и `Derelict` **различается
      качественно**, и обе печатаются.
- [ ] Роль стабильна по `npcId`: поездка на лифте её не меняет.
- [ ] Роль **может** быть переопределена (Жилец → Мародёр) и переопределение
      переживает сейв.

**Эмерджентность — проверка, что сценарии из §3b реальны:**
- [ ] Хотя бы **четыре** из десяти сценариев §3b наблюдаются в прогоне 10 минут
      и печатаются счётчиком. Четыре из десяти — а не десять из десяти: часть
      зависит от часов суток и гермозон, которых пока нет.

### 5.2. Аффордансы work/social/patrol

- [ ] `AiTick::roomOwned` растёт после добавления строк — сегодня в него
      попадают только eat/drink/toilet/sleep.
- [ ] `AiTick::errandDistCells / (errandStep + errandColumn)` — **среднее
      оставшееся расстояние — ПАДАЕТ** на протяжении 2000 тиков. Это единственное
      число, отделяющее «идёт» от «упирается в стену»; счётчик владения не может.
- [ ] `errandColumn` не превышает `errandStep` — иначе толпа ходит по азимуту в
      обход геометрии, а не по полю.
- [ ] Бейк печатает `[rooms] baked bits=N bytes=N` и N ≤ 16 МиБ.

### 5.3. Heal-дедлок

- [ ] Тело с hp < 30 %, помещённое в Medical, за 60 с восстанавливает HP —
      печатается до/после.
- [ ] `crowd_dead_total` на прогоне 10 минут **не растёт** после появления
      Санитара на этаже, при том что до изменения растёт. Обе цифры в отчёте.

### 5.4. Производители памяти

- [ ] Для каждого из шести видов: `AiTick::remembered` растёт после
      соответствующего игрового события, и `ai_recall` возвращает
      `siteStrength[kind] > 0`.
- [ ] **Гейт класса**, а не точечная проверка: тест перечисляет все
      `kMemKindCount` видов и падает, если у вида нет ни производителя, ни строки
      в явном списке отложенных. Это лечение §35 по образцу
      [item_table.h:14-24](../../src/game/item_table.h).

### 5.5. Паника

- [ ] Один испуг в углу 128³ поднимает `danger` у соседей в радиусе 6 клеток за
      ≤ 2 с, и это печатается как число, а не как ощущение.
- [ ] Контур **затухает**: без нового источника поле возвращается к фону за
      ≤ 30 с. Незатухающая паника — самоподдерживающийся дедлок и должна ловиться
      тестом, а не игроком.

### 5.6. Бюджет

- [ ] `ai_step` при 16k тел остаётся ≤ 1.0 мс на потоке. Замер — **наклоном двух
      прогонов**, как требует [AGENTS.md](../../AGENTS.md) §Build, и с указанием
      типа сборки. Сегодняшняя цифра неизвестна и обязана быть напечатана
      **до** изменений.
- [ ] `door_nearest_shelter` перестаёт быть линейным по дверям на тело — либо
      бейк ближайшего убежища в `RoomZones`, либо ограничение радиусом.

---

## 6. Порядок работ и зависимости

```
A. Heal-банк + строка Medical           <- ни от чего не зависит, снимает дедлок
B. Производители памяти (6 вызовов)     <- ни от чего не зависит, оживляет термы
C. RoleId + таблица + колонка в пуле    <- зависит от решения по attrs vs role_
D. Строки work/social/heal в аффордансах<- зависит от A (Medical) и C (workDrive)
E. Патруль через route_step             <- зависит от C, первый потребитель route_step
F. Паника через diffusion               <- зависит от B (иначе нечем помнить испуг)
G. Иерархия секций                      <- зависит от C и от гермозон (спека 02)
```

**A и B можно делать параллельно и они самые дешёвые.** E закрывает годовой долг
`route_step` без потребителя. G упирается в гермозоны, которых ещё нет, —
см. [02_VOXEL_PHYSICS_AND_FLUIDS.md](02_VOXEL_PHYSICS_AND_FLUIDS.md) §гермозоны.

---

## 7. Чего эта спека НАМЕРЕННО не делает

- **Не трогает `needs_step`.** [needs.h:25-35](../../src/game/needs.h) несёт
  письменный аргумент за узкую область, и 63 CHECK-а в `suite_needs.inl:865-891`
  её пинуют. §27 уже один раз откатили за попытку начать отсюда.
- **Не вводит суточный клок.** `Perception::minuteOfDay = -1` гасит все
  ритмические термы; включение клока — отдельное решение с собственными
  последствиями для `Living`/`Office` (обе строки `kRoomRecovery` ждут именно его).
- **Не переписывает скорер.** Коэффициенты выверены по референсу; роль входит
  множителем именно чтобы их не трогать.
