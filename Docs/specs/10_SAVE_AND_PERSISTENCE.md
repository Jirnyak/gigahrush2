# 10 — Сейв, персистентность и что мир забывает

> Сверено с деревом **2026-08-09** на `sync/gravity-on-owner-main`.
> Контракт: [save.h](../../src/game/save.h), [save.cpp](../../src/game/save.cpp),
> тесты в [tests/suite_saveload.inl](../../tests/suite_saveload.inl).

---

## 1. Поправка к постановке задачи

Мандат владельца просил авто-тесты на «**загрузку сейвов v6**». В дереве этого не
может быть:

```cpp
// save.cpp:492
if (h.version != kSaveVersion) return fail(SaveError::BadVersion);
```

`kSaveVersion = 9`. Проверка — **на точное равенство**, не на `>=`. Сейв v6
отвергается, как и v7, и v8. **Миграции в дереве нет ни одной.** Тест на загрузку
v6 написать нельзя, пока не написана миграция; и это не пробел в тестах, а
проектное решение, которое надо либо подтвердить, либо изменить.

---

## 2. Что построено — и построено аккуратно

История версий ведётся в самом файле, каждая версия названа с содержимым
(`save.h:81-106`):

| Версия | Что добавила |
|---|---|
| 2 | `QuestLog` |
| 3 | лог карв-операций |
| 4 | встроенный снимок активного этажа |
| 6 | **макромир целиком** — вся таблица `NpcPool` |
| 7 | лист персонажа `RpgStats` + банк крафта |
| 8 | состояние патронника + накопленный счёт убийств |
| 9 | живые статус-эффекты `StatusSet` |

**Главное решение сделано верно и повторено трижды.** `save.h:222`, `:228`,
`:237` — каждая структура пишется **поле за полем в little-endian, а не
`sizeof`**:

> Version 7: RpgStats wire — field-by-field LE, **NOT sizeof** (pad_ is written so
> the layout stays explicit)

Это ровно то, что делает сейв переносимым между Windows-хостом владельца и его
Mac. `sizeof`-дамп POD-структуры дал бы файл, зависящий от паддинга компилятора,
и разошёлся бы между двумя платформами молча. Автор написал это трижды и трижды
объяснил — переделывать нечего.

Есть даже защита от собственной ошибки роста (`save.cpp:405`):

> `ContractBook` is serialized; grow it and bump `kSaveVersion` in the same...

---

## 3. Что мир забывает при загрузке

Проверено грепом по `save.cpp` — ноль упоминаний:

| Символ | Упоминаний | Следствие |
|---|---|---|
| `SamosborState` | **0** | **часы самосбора сбрасываются** |
| `AiMemory` | **0** | **NPC забывают всё** |
| `RoomZones` | 0 | перепекается — законно |
| `FineNav` | 0 | перепекается — законно |
| `CoarseGraph` | 0 | перепекается — законно |

Три последних — **правильно**: они выводимы из геометрии, и `performance.md`
требует печь их при загрузке. Хранить их в блобе было бы дублированием истины.

Первые два — дыры, и разные по цене.

### 3.1. Часы самосбора не сохраняются — ЗАКРЫТО 2026-08-12

> Версия сейва **10 / SAVCLOCK**: `SamosborState` (17 байт, по полям — три
> хвостовых байта выравнивания после `sealed` НЕ пишутся) и `FastTravelState`
> (32 байта сырого битсета). Оба блока — после последнего фиксированного и перед
> списком открытых ящиков, туда же, куда входили v7/v8/v9.
>
> **Имена полей в §4.1 ниже были неверны** (`phaseTick`/`nextTick`, `count` как
> `u32`); настоящие — `phaseMs`/`phaseTotalMs`/`activeMs`, а `count` это `u16`.
> Провод построен по коду, а не по этому наброску.
>
> **Дефект был шире, чем описан здесь, и вторая половина важнее первой.** Сейв
> действительно обнулял часы — но их обнуляла и КАЖДАЯ ПОЕЗДКА НА ЛИФТЕ, через
> `samosbor_new_game` на двух точках прибытия, при том что `samosbor_enter_floor`
> написан ровно для этого случая и не имел ни одного вызова. Разбор и замер
> (старый код — ноль переходов за прогон, новый — `WARNING` и `ACTIVE`) в
> [problems.md](../../problems.md) §48. Путь загрузки F9 теперь ВОССТАНАВЛИВАЕТ
> состояние из сейва: загрузка — это возобновление, а не прибытие.
>
> Проверено на настоящем файле: версия `0a 00 00 00`, живые значения часов в
> блоке, корректная раскладка битсета.

[Спека 06](06_SAMOSBOR_AND_CRISIS.md) описывает `SamosborState` как автомат с
`count`, переносимым через поездку (`samosbor_enter_floor`), кулдауном, зависящим
от глубины, и duty cycle до 93.8 % на \|z\|=50. Ничего из этого не в сейве.

Следствие: **сохранение и загрузка — это способ отменить самосбор.** Игрок в
активной фазе на глубине 50 сохраняется, загружается — и автомат в `Idle` с
нулевым `count`, то есть с самым длинным кулдауном. Центральная кризис-механика
обнуляется штатной кнопкой.

Цена исправления мала: `SamosborState` — это счётчик, фаза, тик перехода и
вариант. Единицы байт.

### 3.2. NPC забывают всё

[Спека 01](01_ALIFE_AND_UTILITY_AI.md) §2.4 нашла шесть видов памяти ИИ, из
которых у пяти нет производителей. Здесь — обратная сторона: **у той памяти,
что производится, нет персистентности.**

Житель, видевший игрока убийцей, после загрузки не помнит. Слух, прошедший по
`rumour`, исчезает. Это делает социальные системы `speech`/`rumour`/`vendor`
существующими **только внутри одного сеанса**, что противоречит их назначению:
репутация, которая не переживает сохранение, — не репутация.

**Но здесь есть настоящий проектный выбор**, и его надо сделать осознанно:
`NpcPool` — таблица на много тысяч голов. Память каждой головы, сохранённая
целиком, может весить больше остального блоба. Варианты:

| Вариант | Цена | Что теряется |
|---|---|---|
| Сохранять всю память | размер блоба ×N | ничего |
| Сохранять только «видел игрока» + репутацию | ~2 байта на голову | детали слухов |
| Не сохранять, признать сеансовой | 0 | вся социальная память |

**Рекомендация: второй.** Репутация игрока — то, ради чего система существует;
детали слухов восстановимы естественным путём. Первый вариант оптимизирует не то
измерение.

---

## 4. Проектируемые структуры

### 4.1. Версия 10 — часы самосбора

```cpp
// save.cpp — SAVSAM блок, по образцу SAVSTAT из версии 9.
// Поле за полем LE, НЕ sizeof — тот же закон, что для RpgStats и StatusSet.
//
// ПОЧЕМУ ОТДЕЛЬНЫМ БЛОКОМ, А НЕ ПОЛЯМИ В ГОЛОВНОЙ СТРУКТУРЕ:
// три предыдущие версии добавлялись именно так, читатель уже умеет
// пропускать незнакомый блок, и это единственная форма, которая
// позволит миграции из §4.3 работать.
void samosbor_write(Archive& ar, const SamosborState& s) {
    ar.u32(s.count);        // перенос через поездку — самое важное поле
    ar.u8 (s.phase);        // Idle/Warning/Active/Aftermath
    ar.u8 (s.variant);      // какой из 7
    ar.u64(s.phaseTick);    // когда фаза началась
    ar.u64(s.nextTick);     // когда следующий
}
```

### 4.2. Репутация — два байта на голову

```cpp
// Компактная память, переживающая загрузку. НЕ вся AiMemory.
struct NpcPersistentMemory {
    std::uint8_t sawPlayerKill : 1;   // видел убийство
    std::uint8_t wasHelped     : 1;   // игрок лечил/кормил
    std::uint8_t wasRobbed     : 1;   // игрок обокрал
    std::uint8_t knowsPlayer   : 1;   // вообще знаком
    std::uint8_t reserved      : 4;
    std::int8_t  attitude;            // -100..+100, свёрнутая репутация
};
static_assert(sizeof(NpcPersistentMemory) == 2);
```

При `NpcPool` в несколько тысяч голов это единицы килобайт — против сотен, если
сохранять память целиком.

### 4.3. Миграция или явный отказ от неё

Сегодня `if (h.version != kSaveVersion)` отвергает всё. Два честных пути, и
выбор — за владельцем:

```cpp
// ВАРИАНТ A: миграция вперёд. Читатель знает, какие блоки появились когда,
// и подставляет умолчания для отсутствующих.
if (h.version > kSaveVersion) return fail(SaveError::BadVersion);  // из будущего — нет
if (h.version < kMinMigratableVersion) return fail(SaveError::TooOld);
// дальше: блоки, добавленные после h.version, получают умолчания

// ВАРИАНТ B: отказ остаётся, но становится ЯВНЫМ решением с тестом.
// Тест утверждает: сейв версии 8 отвергается с BadVersion, а НЕ падает
// и НЕ читает мусор. Это тоже контракт, и он тоже требует пина.
```

**Вариант B дешевле и честнее для проекта в текущей фазе.** Пока формат меняется
раз в несколько недель, миграция — это N путей кода, каждый из которых надо
тестировать. Но отказ обязан быть **протестирован как отказ**, иначе неизвестно,
отвергает он или читает мусор.

---

## 5. Критерии приёмки

### 5.1. Часы самосбора

- [ ] Сохранение в активной фазе на \|z\|=50, загрузка → фаза и `count`
      **те же**. Прямая проверка, число к числу.
- [ ] Сохранение/загрузка **не** сбрасывает кулдаун. Это тест на то, что
      сохранение перестало быть способом отменить кризис.
- [ ] `kSaveVersion` поднят до 10, история версий в `save.h` дополнена строкой.

### 5.2. Память NPC

- [ ] Житель, видевший убийство, помнит это после загрузки.
- [ ] Размер блоба до/после печатается. Прирост укладывается в объявленный
      бюджет, и бюджет назван числом заранее.
- [ ] Решение по объёму памяти (весь / репутация / ничего) записано в `save.h`
      с причиной — иначе следующий правящий переоткроет вопрос.

### 5.3. Версионирование

- [ ] Тест: сейв версии 8 отвергается с `BadVersion`, **не** падает и **не**
      читает мусор. Проверка отказа, а не только успеха.
- [ ] Тест обратной полярности: рост `ContractBook` без подъёма `kSaveVersion`
      **роняет сборку**. Комментарий на `save.cpp:405` уже просит об этом;
      просьба в комментарии — не гейт.
- [ ] Ни одна структура не пишется через `sizeof`. Гейт:
      `rg 'ar\.(bytes|raw)\(.*sizeof' src/game/save.cpp` пуст. Это защита от
      единственной ошибки, которая расходит Windows и Mac молча.

### 5.4. Roundtrip

- [ ] Тест сравнивает **равенство данных** после цикла запись→чтение, а не
      только «загрузка не упала». Второе — это `green gate proving consistency,
      not reachability`, и оно проходит на пустом блобе.
- [ ] Перепекаемые поля (`RoomZones`, `FineNav`, `CoarseGraph`) после загрузки
      **непусты** — иначе перепекание не случилось, а сейв «успешен».

---

## 6. Порядок работ

```
A. Тест на отказ v8 (проверка отказа)   <- 20 минут, пинует текущее поведение
B. Гейт «нет sizeof в сейве»            <- 10 минут, защита Mac-сборки
C. Часы самосбора -> версия 10          <- единицы байт, закрывает отмену кризиса
D. Решение по объёму памяти NPC         <- решение владельца, не техническое
E. NpcPersistentMemory -> версия 11     <- зависит от D
F. Roundtrip на равенство данных        <- независим
G. Тест обратной полярности на рост     <- зависит от A
```

**A и B — полчаса на двоих**, и они пинуют то, что сегодня держится только
комментариями.

**C — лучшее соотношение цены к последствиям во всём документе:** единицы байт
закрывают возможность отменить центральную кризис-механику кнопкой сохранения.

---

## 5а. Карта файла: 64-байтовый заголовок, ноль тегов

**Главная структурная особенность формата, которую надо знать раньше любой
правки: тегов в `run.sav` нет.** Единственные магические слова — по одному на файл
(`GH2S` `save.h:75`, `GH2F` `save.h:367`). Все блоки **позиционные**, длина каждого
либо константа времени компиляции, либо один из четырёх префиксов в заголовке.

**Незнакомый блок пропустить нельзя.** Это осознанное следствие точного равенства
версий: раз читатель отвергает всё, кроме своей версии, самоописание не нужно.
Решение согласованное — но оно означает, что миграция из §4.3 потребует **сначала**
ввести теги, и её цена выше, чем выглядит.

### 5а.1. Заголовок — 64 байта, `kSaveHeaderWire` (`save.h:214`)

| Смещ. | Поле | Тип | Роль |
|---|---|---|---|
| 0 | `magic` = «GH2S» | u32 | `save.cpp:447` |
| 4 | `version` | u32 | точное равенство, `:492` |
| 8 | `tickHz` | u32 | **совещательное**, никогда не отвергается (`save.h:172-181`) |
| 12 / 16 | `itemCount` / `mobKindCount` | u32 | слабая проверка |
| 20 / 24 | `itemFingerprint` / `mobFingerprint` | u32 | **сильная**, FNV-1a по именам (`:345-355`) |
| 28 | `openedCount` | u32 | префикс массива открытых ящиков |
| 32–38 | `ledgerBytes`, `bookBytes`, `needsBytes`, `invBytes` | u16 ×4 | сигнал дрейфа раскладки |
| 40 | `payloadBytes` | u32 | **префикс длины всего после 64-го байта** |
| 44 | `payloadCrc` | u32 | CRC-32 полезной нагрузки |
| 48 / 52 | `questCount` / `questFingerprint` | u32 | слабая / сильная |
| 56 / 60 | `poolBytes` / `macroBytes` | u32 | префиксы двух блобов |

Пара «слабая + сильная проверка» — правильная форма: количество ловит усечение
таблицы, отпечаток ловит **переименование при том же количестве**. Второе без
первого не обнаруживается вовсе.

### 5а.2. Полезная нагрузка — 12 блоков, 978 байт на пустом прогоне

| # | Блок | Байт | Длина откуда |
|---|---|---|---|
| 1 | `RunLedger` | 33 | константа |
| 2 | `ContractBook` (3 контракта × 21 + счётчики) | 79 | константа |
| 3 | `PlayerSnapshot` (`Needs` 37 + инвентарь 256 + hp/floor/клетка) | **308** | константа |
| 4 | `RpgStats` | 12 | константа |
| 5 | `CraftingState` | 93 | непрозрачный блоб, `craft_write` |
| 6 | Боевое: `hasRanged` + `PlayerRanged` + `kills` | 21 | константа |
| 7 | `StatusSet` — **по столбцам**, не по слотам | 42 | константа |
| 8 | Открытые ящики | 5 × N | **префикс, смещ. 28** |
| 9 | Блоб `NpcPool` | N | **префикс, смещ. 56** |
| 10 | Блоб `MacroSim` | N | **префикс, смещ. 60** |
| 11 | `FactionRelations` 6×6 | 36 | константа |
| 12 | `QuestLog` (19 × 14 + счётчики) | 308 | константа |

**Асимметрия порядка, которую надо знать.** Массив открытых ящиков (8) лежит
**до** двух блобов переменной длины, а матрица фракций (11) и журнал заданий (12)
— **после**. Ошибка в `poolBytes` сдвинет фракции и задания, но **не** игрока.
При отладке это первый различающий признак: испорчен игрок или только хвост.

`StatusSet` по столбцам (все `remainMs`, затем все `intensityE3`, затем все `alt`)
— решение против паддинга: столбцы однотипны, структуры нет, выравнивать нечего.

### 5а.3. Снимок этажа — RLE, три независимых потока

**Поправка к постановке:** встроенного снимка версии 4 в `run.sav` больше нет.
`save.h:83-91` фиксирует, что журнал карвов (v3) и снимок (v4) **удалены в v5** и
заменены поэтажными файлами; поля заголовка `carveCount`/`snapBytes` ушли вместе с
ними (`save.h:198-200`).

Механизм жив, переехал в `floor_<N>.sav` (заголовок 16 байт: `GH2F`, версия,
длина, CRC). Это **не** сырые воксели, **не** список операций и **не** дельта, а
полный RLE-снимок: типы клеток, состояние масок (пусто / полно / смешано),
страницы суб-материалов. Смешанные клетки уходят в сырые 64 байта.

| Форма | Размер |
|---|---|
| Сырой эквивалент | 4,2 МБ типы + 134,2 МБ маски = **138,4 МБ** (`save.h:16`) |
| Чистый этаж до страничного RLE | **736 МБ** → после: **125 МБ** (`save.h:280-281`) |
| Худший измеренный случай | **772 МБ**, отвергнут читателем с лимитом 256 МиБ (`save.h:266-268`) |
| Типичный этаж | единицы МБ (`save.h:16`, ~5 МБ на этаже 0) |

Дельта была **рассмотрена и отвергнута** (`save.h:284-289`): она сделала бы
генератор постоянной частью формата. Довод верный — генератор меняется чаще
формата.

### 5а.4. Границы проверены везде, и проверены правильно

Это второй сильный отрицательный результат аудита. Дыр нет, и защита построена на
одном приёме, применённом последовательно:

```cpp
// save.cpp:1081-1083 — и тот же приём на :1100 и :1141
// «Subtract, never add: at + len is uint32 arithmetic and a hostile or
//  corrupt run length wraps it past the guard straight into the write below.»
if (len > kMacroCells - at) return false;
```

**Вычитание вместо сложения** — единственная форма, устойчивая к переполнению
`uint32`. `Reader::skip` (`:146-153`) сделан так же: `if (n_ - at_ < k)`.

Порядок проверок в `save_read` тоже верен: длина ≥ 64 (`:481`) → заголовок
читается **ровно по 64 байтам**, а не по `n` (`:484`) → три беззнаковых счётчика
ограничены **до** участия в арифметике (`:515-517`) → пересчёт длины и точное
равенство с `payloadBytes` (`:518-524`) → `n - 64 < payloadBytes` (`:525-526`,
подтекания нет, так как `n ≥ 64` уже доказано) → `Reader` строится по проверенной
длине (`:540`).

`Reader::u8` (`:94-101`) — единственная точка сужения: за концом ставит
`ok_ = false` и возвращает 0, **не читая**. Все более широкие аксессоры идут через
неё, поэтому ни один визитёр не может прочитать за буфер.

**Асимметрия, задокументированная, а не дефект:** `apply_floor_snapshot` пишет в
живую сетку по мере разбора, поэтому отказ посреди блоба оставляет её частично
перезаписанной (`save.h:467-472`). Вызывающему предписано трактовать `false` как
«генерировать заново».

### 5а.5. Частичная запись уничтожает предыдущий сейв — ЗАКРЫТО 2026-08-12

> **Закрыто** temp+rename в `write_bytes_file`, обе полярности замерены на
> заполненном HFS+ томе: старый код оставляет **0 байт** вместо 4096-байтового
> сейва, новый оставляет его **побайтово прежним**, `*.tmp` не остаётся.
> Замер, первая неудачная его редакция и честная граница (это атомарность, а не
> долговечность — `fsync` нет) — [problems.md](../../problems.md) §47.
>
> **Одно утверждение ниже было неверным** и осталось в тексте как урок: нулевой
> файл НЕ предъявляется как «первый запуск». `save_read` отвергает его как
> `TooShort` на `n < 64` (`save.cpp:481`), и `main.cpp` это документирует прямо;
> `err = None` бывает только когда не открылся сам `fopen`. Дефект был настоящим,
> а названное последствие — нет.
>
> Не сделано из этого пункта: **общий счётчик поколения** в `run.sav` и
> `floor_<N>.sav`. Окно «новый этаж со старым прогоном» между двумя записями
> `save_run_now` остаётся — теперь каждая из них атомарна по отдельности, но пара
> транзакцией не является.

Ниже — исходный разбор.

**Ни временного файла, ни переименования, ни журнала, ни резервного слота, ни
`fflush`/`fsync`.** Весь путь записи — `main.cpp:914-924`:

```cpp
std::FILE* f = std::fopen(path, "wb");   // "wb" УСЕКАЕТ до нуля немедленно
const std::size_t n = std::fwrite(bytes.data(), 1, bytes.size(), f);
```

`"wb"` обнуляет существующий `run.sav` **до** первого записанного байта.

Радиус поражения шире одного файла: `save_run_now` (`main.cpp:2291-2294`) пишет
**сначала** поэтажный файл, **потом** `run.sav`, и между ними нет ни общей версии,
ни метки транзакции. Падение в промежутке даёт новый этаж со старым прогоном.

CRC ловит порванный файл (`BadChecksum`), усечение ловит `TooShort` — но **старый
сейв к этому моменту уже уничтожен**. Хуже всего вырожденный случай: файл нулевой
длины заставляет `read_run` вернуть `false` с `err` = `None`, и приложение
показывает это как **«первый запуск»** (`main.cpp:972-976`). Уничтоженный сейв
предъявляется как отсутствующий.

**Починка стандартна и дешева:** писать в `run.sav.tmp`, затем
`std::filesystem::rename` — на одном томе это атомарная операция. Плюс общий
счётчик поколения в обоих файлах, чтобы рассинхрон был обнаружим.

Это **самый дорогой дефект во всей серии спек по последствиям для игрока**: он
уничтожает прогресс, а не искажает поведение.

---

## 6а. ~~Живой дефект: провод вырос, версия не поднялась~~ — ОТОЗВАНО 2026-08-12

> **ЭТОГО ДЕФЕКТА В ДЕРЕВЕ НЕТ И НИКОГДА НЕ БЫЛО.** Проверено построчно
> 2026-08-12; каждое из четырёх процитированных ниже чисел не совпадает с кодом:
>
> | Цитата §6а | В дереве |
> |---|---|
> | `save.h:219 kNeedsWire = 37` | `kNeedsWire = 33` |
> | `save.cpp:383 static_assert(kNeedsWire == 9 * 4 + 1)` | `== 8 * 4 + 1` (`save.cpp:382`) |
> | `npc_pool.h static_assert(sizeof(Needs) == 40)` | `== 36` (`npc_pool.h:173`) |
> | `save.cpp:236-238 ar.f32(n.hpBank)` | поля `hpBank` в `Needs` не существует |
>
> `grep -rn hpBank src/ tests/` — **ноль попаданий в коде**; символ живёт только в
> тексте этой спеки, спеки 14 и спеки 01, где он помечен как `// НОВОЕ`, то есть
> как ПРЕДЛОЖЕНИЕ. `git log -S"hpBank" -- src/` — **ни одного коммита**;
> `git log -S"kNeedsWire"` — один, тот, что провод и завёл. Провод `Needs` не
> менялся ни разу с момента появления.
>
> Итог: `kSaveVersion = 9` / `kNeedsWire = 33` / `sizeof(Needs) == 36` полностью
> согласованы между собой, и арифметические `static_assert`ы это подтверждают на
> сборке. **Поднимать версию до 10 по этому основанию НЕЛЬЗЯ** — оснований нет.
> (Основание появится от 0.3б/2.10 — `FastTravelState` и часы самосбора в сейве;
> вот там номер обязан вырасти.)
>
> **§6а.1 отозвана вместе с §6а.** `busy_run()` (`suite_saveload.inl:126-136`)
> заполняет все восемь полей `Needs` различимыми ненулевыми значениями
> (`63.25f, 41.5f, 7.125f, 12.75f, 33.375f, 9.0625f, 4.5f, 0.5f`, `seeded = 1`), а
> `memcmp` стоит на `:227` под `static_assert(sizeof(Needs) == 8*4+1+3)`. Тест **не
> вакуумный**.
>
> **Почему это оставлено в файле, а не удалено.** Спека была написана уверенно, с
> номерами строк и цитатами — и целиком про несуществующий код; ровно то, от чего
> предостерегает [problems.md](../../problems.md) §45, и ровно тот способ, каким
> §12a.1 роадмапа держала «мёртвой» строку `Mass` у мобов, живую с 2026-08-03.
> **Требование, которое из этого следует, шире сейва: цитата с номером строки —
> это не проверка. Проверка — это `grep`, выполненный сегодня.**
>
> Что из §6а остаётся В СИЛЕ и не зависит от несуществующего `hpBank`:
> требование §6а.1 к форме (**новое поле провода обязано получить различимое
> ненулевое значение в `busy_run()` той же правкой**) и вывод, что комментарий
> `save.cpp:405` — не гейт. Оба верны и оба стоит выполнять.

Ниже — исходный, ОШИБОЧНЫЙ текст, сохранённый как образец класса ошибки.

**Найдено 2026-08-09, в свежеприземлившейся правке.** `hpBank` добавлен в `Needs`
и **корректно** проведён в сериализацию:

```cpp
// save.cpp:236-238 — поле пишется
ar.f32(n.hpDebt);
ar.f32(n.hpBank);   // <- новое
ar.u8 (n.seeded);
```

```cpp
save.h:219   kNeedsWire = 37;              // было 33
save.cpp:383 static_assert(kNeedsWire == 9 * 4 + 1);   // согласован
npc_pool.h   static_assert(sizeof(Needs) == 40);       // согласован
save.h:110   kSaveVersion = 9u;            // <- НЕ ПОДНЯТА
```

Три пина из четырёх согласованы между собой. Четвёртый — версия формата — нет.

**Что это значит на диске.** Байт заголовка тот же (`9`), длина блока игрока
выросла на 4. Читатель проверяет версию на **точное равенство** (`save.cpp:492`) и
пропускает файл, а затем сверяет `payloadBytes` с пересчитанной длиной
(`:518-524`) и отвергает как `SizeMismatch`.

То есть **старый сейв не читается как мусор — он отвергается**, и это заслуга
пересчёта длины. Защита сработала. Но:

1. Игрок получает `SizeMismatch` вместо `BadVersion`. Сообщение врёт о причине.
2. **Два разных формата носят номер 9.** Сейв, записанный этой сборкой, и сейв,
   записанный вчерашней, неотличимы по заголовку. Любая будущая миграция получит
   неразрешимую развилку.

Второе — настоящая цена. Номер версии существует ровно для того, чтобы формат был
опознаваем; два формата под одним номером делают номер бесполезным именно там, где
он нужен.

`save.cpp:405` предупреждает об этом классе дословно: *«`ContractBook` is
serialized; grow it and bump `kSaveVersion` in the same...»*. Предупреждение
относится к другой структуре, но класс тот же, и оно сбылось на первой же правке
после его написания.

**Починка — одна строка** (`kSaveVersion = 10u`) плюс строка в истории версий.
Приёмка §5.3 требует теста обратной полярности ровно на это: рост провода без
подъёма версии **должен ломать сборку**. Комментарий вместо гейта — вот его цена,
измеренная на практике за один день.

### 6а.1. И один вакуумный тест как следствие

`suite_saveload.inl:228` сравнивает `Needs` целиком через `memcmp` — то есть
**поймал бы** потерянный `hpBank`. Но `busy_run()` его не заполняет, поэтому обе
стороны нули и сравнение проходит **вакуумно**.

Это единственный найденный в наборе тест, проходящий не потому, что код верен.
Заголовок самого файла (`:9-11`) предупреждает ровно об этой ловушке — и попался
в неё, потому что предупреждение адресовано человеку, а не сборке.

**Требование:** поле, добавленное в провод, обязано получить **различимое
ненулевое** значение в `busy_run()` той же правкой. Это дешевле любого гейта и
ловит весь класс.

---

## 7. Порядок полей внутри блока — закон, а не стиль

Поле за полем LE решает вопрос паддинга, но **не** вопрос порядка. Порядок полей
внутри блока — второй, независимый контракт, и он держится только тем, что
писатель и читатель написаны рядом.

```cpp
// ЛОВУШКА. Обе функции компилируются, гейт `нет sizeof` проходит,
// тест «загрузка не упала» проходит. Файл читается как мусор.
void write(Archive& ar, const S& s) { ar.u32(s.a); ar.u32(s.b); }
void read (Archive& ar,       S& s) { ar.u32(s.b); ar.u32(s.a); }  // <- порядок
```

Пока `a` и `b` — оба `u32`, ни один компилятор и ни один существующий тест этого
не заметят. Ловит только roundtrip на **равенство данных** с **различными**
значениями полей: `a=1, b=2`. Тест с `a=0, b=0` — а именно так выглядит
свежесконструированная структура — проходит при перепутанном порядке.

**Отсюда требование к тесту:** заполнять поля **различимыми** значениями, и ни
одно не оставлять нулём. Это дешевле любой схемы самоописания и ловит ровно тот
класс, который переживает все остальные проверки.

---

## 8. Что сейв не может выразить в принципе

Три вещи не «забыты» — они **невыразимы** в текущей форме блоба, и каждая
требует решения раньше кода.

| Что | Почему невыразимо |
|---|---|
| Незавершённое действие | Интент — это результат ежекадрового скоринга, а не хранимое поле. NPC на полпути к кухне после загрузки пересчитает интент с нуля. |
| Полёт снаряда | Пул снарядов не в блобе. Сохранение в момент выстрела теряет выстрел. |
| Незакрытый карв | Очередь предложений карва (`kMaxCarveProposals`) — кадровая, не сохраняемая. |

Первое — **правильно** и должно остаться. Интент, восстановленный скорингом,
всегда согласован с миром; интент, поднятый из файла, может ссылаться на комнату,
которую самосбор уже съел. Это ровно тот случай, когда **не** сохранять — сильнее.

Второе и третье — тоже правильно, но по другой причине: их время жизни короче
кадра ожидания, и игрок не может сохраниться «в момент выстрела» иначе как
скриптом. Записать это решение в `save.h` явно — иначе следующий правящий увидит
пропуск, а не решение.

**А вот четвёртое — настоящая дыра.** Состояние лифта. Если поездка между этажами
это последовательность шагов (см. спеку по лифтам), сохранение **в кабине** либо
невыразимо, либо восстановится в геометрию, которой на новом этаже нет. Проверить
до того, как писать версию 10.

---

## 9. Явные не-цели

- **Не переходить на `sizeof`-дамп ради скорости.** Поле за полем — это то, что
  делает сейв переносимым между двумя хостами владельца. Автор написал это
  трижды и трижды объяснил.
- **Не сохранять `FineNav`/`CoarseGraph`/`RoomZones`.** Они выводимы из
  геометрии; хранить их — дублировать истину и создавать возможность
  расхождения.
- **Не писать миграцию с v2.** Восемь путей кода, каждый со своим тестом, ради
  сейвов, которых ни у кого нет. Если миграция понадобится — только вперёд с
  ближайшей версии.
- **Не сохранять всю `AiMemory` без замера.** Несколько тысяч голов × полная
  память может превысить остальной блоб. Сперва число, потом решение.
