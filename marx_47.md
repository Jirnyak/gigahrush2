# План Агента: marx_47

## Роль
Вы — один из агентов Jules, выполняющих МЕГА-АПДЕЙТ для проекта ГИГАХРУЩ. Ваша задача №47.
Вы действуете полностью автономно. От вас ожидается реализация фичи от начала до конца, включая написание кода, интеграцию с архитектурой и проверку работоспособности.

## ⚠️ ОБЩИЕ ПРАВИЛА ДЛЯ ВСЕХ АГЕНТОВ (AGENTS.md)
1. **Никакого хардкода и костылей.** Системы должны быть расширяемыми и опираться на `src/data/`.
2. **Пятислойная архитектура:** `core/` -> `data/` -> `gen/` -> `systems/` -> `render/`. Никакого геймплея в `render/`.
3. **Iron Law (Оптимизация):** Никаких тяжелых поисков (BFS) в реалтайме во время симуляции.
4. Уважайте мобильную платформу и производительность (без лишних DOM манипуляций).
5. **Максимальная независимость:** Все агенты (Жюли) работают параллельно и абсолютно независимо друг от друга.
6. **Полная автономность:** НЕ задавайте пользователю вопросы и не просите советов. Обязательно принимайте все архитектурные, кодовые и дизайнерские решения самостоятельно!
7. **Обязательный коммит и PR:** Обязательно делайте `git commit` ваших изменений и создавайте Pull Request (или делайте пуш) после завершения работы.

## Текущая задача
**Новые Мобы: Гномы/Карлики (entities/gnome.ts).**

### Контекст задачи
Необходимо добавить нового монстра — Гнома (Карлика).
Лор: Маленький быстрый мутант-гуманоид, результат мутации от Самосбора. Низкорослый, стайный, живёт в вентиляции и коллекторах (Maintenance/Living). Агрессивен, но поодиночке труслив (предпочитает нападать толпой).

### Конкретные файлы и паттерны
- **`src/entities/gnome.ts`** [НОВЫЙ ФАЙЛ]: Создать определение по паттерну существующих мелких мобов (например, `krysnozhka.ts`).
- **`src/core/types.ts`**: Добавить `GNOME` в `MonsterKind`.
- **`src/entities/monster.ts`**: Зарегистрировать гнома в `MonsterRegistry`.
- **`generateSprite()`** (или эквивалент): Создать процедурную генерацию спрайта для гнома. Он должен быть высотой ~30px (в два раза ниже обычного), серо-коричневого цвета.

### Детальная спецификация по Архитектуре и Реализации (Строго обязательно к исполнению)

#### Шаг 1: Обновление типов
В `src/core/types.ts` добавьте нового монстра в енум (минимальное изменение в RED файле).
```ts
// src/core/types.ts
export enum MonsterKind {
  // ...
  KRYSNOZHKA = 'krysnozhka',
  GNOME = 'gnome', // <- Добавлено
  // ...
}
```

#### Шаг 2: Создание файла сущности
Создайте файл `src/entities/gnome.ts` со всеми характеристиками.
```ts
// src/entities/gnome.ts
import { MonsterDef, MonsterKind } from '../core/types';
import { FloorLevel } from '../core/constants';

export const DEF: MonsterDef = {
  kind: MonsterKind.GNOME,
  name: 'Гном-мутант',
  hp: 25, 
  speed: 2.8, // Быстрее человека
  dmg: 8, 
  attackRate: 0.8,
  sprite: 0, // ID будет присвоен при генерации
  aiFlags: ['melee', 'pack_hunter'], // pack_hunter значит, что трусит без стаи
  floors: [FloorLevel.MAINTENANCE, FloorLevel.KVARTIRY, FloorLevel.LIVING],
  counterplay: 'Маленький и быстрый — используйте дробь или бейте в ближнем бою.',
  lootHint: 'Мелкие детали, гайки, провода, иногда редкие камни.',
};
```

#### Шаг 3: Регистрация моба
В `src/entities/monster.ts` добавьте импорт и регистрацию.
```ts
// src/entities/monster.ts
import * as gnome from './gnome';
// ...
registry.set(MonsterKind.GNOME, gnome);
```

#### Шаг 4: Генерация спрайта (Render Layer)
В рендерере/генераторе спрайтов (например `src/render/sprites.ts` или скриптах генерации) пропишите форму гнома.
```ts
// src/render/sprites.ts или процедурный генератор
function generateGnomeSprite() {
  // Псевдокод процедурной отрисовки:
  // - canvas 32x32 (вместо 64x64)
  // - Цвет: #8B7355 (серо-коричневый)
  // - Короткие ножки, горб, красные маленькие глаза
  // return spriteIndex;
}
```

#### Шаг 5: Трусливый ИИ (Pack Hunter)
Убедитесь, что флаг `'pack_hunter'` (если он поддерживается ИИ) заставляет моба убегать от игрока, если в радиусе 10 клеток нет других гномов. Если такого функционала нет, реализуйте его в `src/systems/ai/monster.ts`.

### QA и Тестирование
1. Используйте консоль для спавна одного гнома: `/spawn_monster gnome`.
2. Проверьте визуальное отображение: он должен быть заметно меньше человека (хитбокс и спрайт).
3. Проверьте стайный ИИ: один гном должен убегать от вас, но если заспавнить 4-5 штук, они должны нападать.
4. Убейте гнома, проверьте выпадение лута по `lootHint`.
5. Выполните `npm run typecheck` и `npm run check:full`.

### Требования к PR
- Файл `src/entities/gnome.ts` должен быть полностью самодостаточным модулем данных.
- Никакого изменения общих циклов в `main.ts`.

---
*Ожидается, что вы полностью автономно завершите задачу и оставите проект в компилируемом состоянии.*
