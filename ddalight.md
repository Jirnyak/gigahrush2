# DDA Light — единая система света и теней

> **СВЕРКА 2026-08-23: док заморожен на e12ebcd8, после него прошло ШЕСТЬ
> световых эпиков — числам ниже не верить.** Что изменилось: клетка сетки =
> **256 Б / 63 слота** (не 128/31, `gpu_light_grid.h:77-79`); `kMaxPointLights=512`
> и камерная сортировка **МЕРТВЫ** (корневой кап `kRootLights = 131072`);
> бюджет теней 8/4/2 мёртв (`uShadowRayOverride`); **god rays удалены**
> (решение владельца, 2f4d155d); «растровые пассы — стаб» — стабов нет ни у
> кого (`volumetric_fog.glsl:126`); долг «wire/cloth/particle без сетки»
> закрыт (`dressing_light`, 20042edb). Построено и не описано: запечённая
> видимость «лампа→клетка» (light-visibility-bake.md), патчи дельта-ламп,
> бакеты динамиков, кластеры хвоста (light-cluster.md); same-frame закон
> снят владельцем 2026-08-22 (37e772d1) — свет ДОГОНЯЕТ мир. Философия ниже
> (один закон, DDA по зеркалу) — верна как была.

## ЭТАЛОННОЕ СОСТОЯНИЕ ЯДРА — 2026-08-23, коммит 86860f58 (метка git
## `light-core-clean-2026-08-23`)

Проверено владельцем в игре: **свет правильный, разрывов теней нет.** Это
точка отсчёта — если картинка когда-нибудь снова поедет, сравнивать с ней.

Что стало ядром после чистки этого дня:
- один DDA-луч по субвокселям на каждую лампу списка клетки (та же геометрия,
  что у физики), запечённая видимость «лампа→клетка» даёт кандидатов;
- КЛАСТЕРОВ НЕТ (удалены 86860f58): механизм не работал ни кадра — биннинг
  выбрасывал все ссылки, — а его побочный эффект резал список клетки до 8
  ламп при среднем 10.6 и рвал свет прямоугольниками по клеткам 4 м;
- грязной ветки светосетки НЕТ (37e772d1): свет ДОГОНЯЕТ мир патчами
  (~0.3-1 с), закон «свет тем же кадром» снят владельцем 2026-08-22;
- мёртвого долга dirtyGen НЕТ (d68cea5b);
- бейк: mean 10.6 / packed 10.6 ламп на освещённую клетку (обрезки нет),
  вход на этаж ~1.1 с.

ЦЕНА, ЗАМЕРЕНА ВЛАДЕЛЬЦЕМ на этом состоянии: открытая комната ~20 fps,
стены и взгляд на лампу 30-40. Это честная цена целой картинки: пиксель
маршит ~10.6 ламп вместо 8.

**КВАД-ТЕНИ ОПРОВЕРГНУТЫ ЗАМЕРОМ 2026-08-24 (построены 5490e3dd, вырезаны
a31a1184).** Вариант B разбора 2026-08-23 («один теневой луч на квад 2×2,
8.9 млн лучей → 2.2 млн, 20 → ~7.5 мс») реализован субгрупповым бродкастом
и проверен владельцем на Apple M2 Pro: ноль разницы по fps И по картинке
(механизм был жив — квад-опы в SPIR-V, файл грузился). Причина: SIMD-лок-
степ — соседние пиксели квада и раньше маршили почти одинаковые лучи
ОДНОВРЕМЕННО в одном варпе; освобождение трёх линий из четырёх не сокращает
время варпа (оно равно длине луча лидера). Прогноз «время ∝ числу лучей»
неверен ВНУТРИ варпа. Урок: выигрыш по теням даёт только сокращение
ФРАГМЕНТОВ (полурезный световой пасс), ЛАМП на пиксель (бюджет по вкладу)
или ДЛИНЫ луча — не разделение лучей между соседями по варпу.

Находки аудита 2026-08-23 — ОБЕ ЗАКРЫТЫ 2026-08-24:
1. ~~ранг лампы — чистая геометрия~~ ПОЧИНЕНО (cfbc2e6a): лампа, попавшая в
   список дилатацией (перекрытая), несёт старший бит id (kLampDilatedBit,
   light_vis_bake.h; в шейдер — дефайном GIGA_LAMP_DILATED_BIT) и штраф +1.0
   к скору в биннинге — все видимые ранжируются выше всех перекрытых, теневой
   бюджет пикселя уходит видимым; из списка перекрытая не выпадает (ошибка
   излишком). Пины обеих полярностей + мутация.
2. ~~дельта-путь не сверяет поколение состава~~ ПОЧИНЕНО (f6efe5ff): свап
   полного бейка отдаёт СНАПШОТНЫЙ тег состава (light_table_baked_tag), не
   текущий — лампа, умершая за время полёта бейка, не переработается, пока
   жив список, ссылающийся на её слот. Гоночный пин в suite_rebake.

---

Основа освещения игры (решение владельца 2026-08-17). Один закон, по Кармаку,
но на нашем козыре: **каждый источник освещает каждую поверхность и каждый
шаг дымки одним и тем же кодом, а тень — короткий DDA-луч по вокселному
зеркалу мира**. Ноль shadow map'ов, ноль дополнительных пассов, ноль
запекания: луч читает те же суб-воксельные маски, о которые бьётся физика,
поэтому разрушаемость и свет согласованы по построению — прокарвил дыру,
свет хлынул в тот же кадр.

Некрасиво — можно (угловато, воксельно, как ид-движки). Нечестно — нельзя.

## Законы

1. **НПЦ = игрок.** Света «от камеры» не существует. Бесплатный налобник
   удалён ([main.cpp] collect_scene_lights); фонарик будет ПРЕДМЕТОМ
   инвентаря — конусный `add_light`, тем же, каким получат его NPC.
   **ЗАКРЫТО ПОЛНОСТЬЮ 2026-08-20.** До этого дня закон соблюдался только в
   DDA-системе, а мимо неё жил ФОРВАРДНЫЙ налобник — аналитическая формула
   `camPos.w / (1 + d²/fog.z²)` в трёх шейдерах (wire/cloth/particle), питаемая
   каждый кадр из main.cpp. Убит по решению владельца; обе лейны пушатся нулями.
   Цена: у verlet_pass и particle_pass layout с ОДНИМ дескрипторным сетом, то
   есть световая сетка им недоступна — провода, тенты и неэмиссивные частицы
   теперь видны только по ambient. Это долг, а не решение.
   Любой свет в кадре — это предмет мира: лампочка, прибор, трассер,
   сигнальная граната, пламя, прожектор.
2. **Одна запись источника на всё.** `PointLight` 48 B: `posRadius`,
   `colorIntensity`, `dirCone` (xyz — направление, w — cos полуугла конуса;
   `w <= -1.5` — сентинель «омни»). Точка и прожектор — один структ, один
   цикл; «типов светильников» в рендере нет.
3. **Сетка света — весь тор, привязана к миру.** 64³ клеток по 4 м =
   256 м = kWorldExtent по каждой оси; индекс — `floor(p/4) & 63`, врап на
   битовом И, как `cell_index` вокселей. Камерной коробки нет — класс багов
   «свет за гранью коробки исчезает» невозможен по построению.
4. **Туман и поверхность видят один свет.** `march_volumetric_fog` (гало,
   конусные лучи в дымке) и `surface_light` (ламберт + Блинн-Фонг) читают
   одну сетку, одно затухание `(1-d/r)²`, один конус-фактор. Источник
   обязан выглядеть одинаково в воздухе и на стене.
5. **Тень — `giga_shadow(p, dir, dist)`.** Прототип в volumetric_fog.glsl,
   определение даёт каждый шейдер своим лучшим средством:
   - `raymarch.frag` — вызов `march()` (двухуровневый DDA по зеркалу).
     Смещения единые, у вызывающего: старт `P + N*0.06` (четверть атома —
     не родиться в собственном теле), финиш `d - 0.26` (не врезаться в
     арматуру источника: лампочка висит вплотную к потолку).
   - растровые пассы без зеркала (cube/prop) — пока стаб `1.0`. Это
     ЧЕСТНЫЙ ШОВ, задокументированный в шейдере, а не тихий обход.
6. **Изотропия и тор.** Период врапа ПУШИТСЯ (`pc.torus.x`), никогда не
   литерал; `wrap_nearest` — все три оси. Никаких функций абсолютной
   высоты ([volumetric_fog.glsl], закон высотного тумана).
7. **Мультимедиа — вокруг КАМЕРЫ, не игрока.** Камера — полноценная
   сущность, ей не обязательно быть привязанной к NPC (свободный полёт).
   Сбор светов, каллы, сортировка — всё от camPos. То же предстоит звуку
   (listener = камера) — кросс-системная очередь.
8. **Свет не рождается из звука.** Блок «шум → мерцающий свет на камере»
   удалён: шаги (grounded-гейт) включали фантомный налобник от WASD и
   гасили его в прыжке. Звук из физики, свет из предметов; синестезии в
   сетке не место.
9. **Перелив клетки решается вкладом.** Биннинг держит top-K по `d²/r²`
   (вклад света В КЛЕТКУ), не порядок CPU-сортировки по камере: порядок-
   по-камере при подлёте к кластеру ламп отдавал лимит клетки лампам у
   камеры, и комната ТЕМНЕЛА при приближении к свету.
10. **Мерцание — профиль из таблицы носителя, одна функция на свет и
   плафон.** Колонка `flicker` в props.csv (none/mains/arc/pulse/crt);
   `flicker_factor(профиль, позиция, время)` — чистая функция, реализованная
   ИДЕНТИЧНО в game/flicker.h (интенсивность света, CPU) и shaders/flicker.glsl
   (emissive поверхности, GPU): арматура и её свет пульсируют синхронно по
   построению, без каналов данных. Профиль едет в биты 0..2 PropMesh.flags.
   **Время здесь — СИМ-ВРЕМЯ** (`simTick * kSimDt`, с 2026-08-20, [CANON.md] S15):
   раньше стоял `SDL_GetTicks()`, и это был единственный вход настенных часов в
   картинку — мерцание не замирало на паузе, два прогона давали разный свет, и
   запинить его было нечем. Тот же скаляр уходит на GPU через `torus.w`, поэтому
   переход стоил одной строки и не потребовал правки шейдеров.
   Бывший зоопарк «Case A-D» с ветками по mat_id и порогам baseEmissive умер.
   CRT — не мерцание, а поверхностная анимация экрана (профиль crt).
   Электросеть (щиток → power cut в радиусе 12 м, combat.h) гасит только
   mains-профиль; сама сеть — ПОД ПЕРЕСМОТРОМ владельца (не его задумка;
   кандидат — топология проводов, как трубы), глубже не связываемся.
11. **Экспозиция = 1, камера яркость не крутит.** «Тёмная адаптация» (порт
   форка e6b5e24b) глушила кадр до 5x по СУММЕ ЛАМП В 16 М ОТ КАМЕРЫ — не по
   свету в глазу; это и был стабильный баг «подлетаешь к свету — он тускнеет»
   (лампа за стеной душила так же). Удалена 2026-08-17. Честная адаптация
   глаза, если понадобится, — GPU-редукция реальной яркости КАДРА, осознанной
   системой.

## Данные и бюджеты

| Что | Где | Значение |
|---|---|---|
| Запись света | gpu_light_grid.h / *.glsl / *.comp | 48 B, три vec4, static_assert |
| Сетка | те же | 64³ × 128 B = 32 MB device-local |
| Кап на клетку | GpuGridCell | клетка = 32 слова (128 Б): счётчик + 31 индекс, ОТСОРТИРОВАНЫ по вкладу (лучший первым); перелив вытесняет худшего; потребители читают клетку прямой индексацией, не копией структа (копия = 128 Б × шаги дымки × пиксели = гигабайты) |
| Стейджинг CPU | kStagingLights | 16384 = все источники худшего этажа (блейм несёт 9500 лампочек); перелив считается (overflow_dropped), GIGA_LIGHT_DBG показывает |
| На GPU за кадр | kMaxPointLights | ближайшие к камере 512 после сортировки |
| Бюджет теней/пиксель | surface_light | 8 вблизи / 4 до старта дымки / 2 дальше; список клетки отсортирован — бюджет уходит сильнейшим, тусклый хвост отбрасывается стабильно. Без бюджета плотное поле ламп жгло 31 DDA-луч на пиксель (26 мс кадра) |
| Свет в дымке | march_volumetric_fog | сильнейшие 16 на шаг; фаза через t·sqrt(t), не pow |
| Замер 2026-08-17 | gpu-ms | блейм (9500 ламп, худший этаж) 11.8 мс; падик/этаж 0 ~5 мс |
| CPU-калл сбора | collect_scene_lights | `light_reaches_view`: сфера СВОЕГО радиуса источника касается сферы тумана вокруг камеры — общих констант нет, лампы разные, решает радиус каждой |
| Порог вклада | surface_light | `power*ndotl < 0.004` — не заслуживает теневого луча |
| Биннинг | light_grid.comp, 262144 инвок. × ≤512 светов | конус НЕ режет объём — сфера радиуса консервативна |

## Источники (CPU, collect_scene_lights)

Пересобираются каждый кадр из ECS — динамика бесплатна. **Пропы — из
таблицы**: любой проп, чья строка data/props.csv несёт `light_radius_mm`,
получает PropLight при спавне и светит, кем бы он ни был (лампочка, терминал,
прожектор); спец-случаев в коллекторе нет. Гейты: Interactable.active для
всех; power cut и хрущёвское мерцание — только mains (interact LightBulb).
Дальше тем же путём: мобы → mobs.csv, носимое → items.csv (фонарик игрока/NPC
— конусная строка предмета). Сполохи самосбора и трассеры — события мира, код.
Пламя/огнемёт: та же запись, позиция и интенсивность из физики огня («звук из
физики» — и свет из физики), тени запляшут сами.

`GIGA_LIGHT_DBG=1` — строка раз в ~2 с: сколько пропов-светов живо и куда
делись остальные (inactive/unpowered/culled), сколько в стейджинге, дропнуто,
уехало на GPU. Ей найден баг «целые ярусы блейма не светятся никогда»:
9500 ламп не влезали в старый стейджинг 2048 по порядку создания.

## Проверка (рецепты)

- Прострелы для тумана/дальнего света: этаж 5,
  `--shot x.png --frames 900 --floor 5 --yaw -1.6 --pitch 0.15`.
  Этажи 0/4 — интерьеры < 64 м, дальний план не виден.
- Тень: колонна/проп между источником и стеной, стрейф — тень пляшет.
- Разрушаемость: carve у освещённой комнаты — свет в дыре тем же кадром.
- Перф: `gpu-ms`/`gpu-ms-peak` в stderr; world peak был ~0.12-0.18 мс с
  тенями (2026-08-17, M-серия).
- `--shot` из чистого cwd — сейвы пишутся в `./gigahrush2_save/`.

## Светоматериалы — «рисовать светом» (ПОСТРОЕНО 2026-08-17)

Любой воксель, чей материал несёт `light_radius_mm` в data/materials.csv,
излучает: бейк этажа (game/light_bake.cpp, зовётся из refresh_floor_props)
сканирует сетку, кластеризует соседние светоячейки (BFS по 6-связности С
ВРАПОМ; полоса из 20 ячеек = один эмиттер) и отдаёт статические эмиттеры
коллектору. Центроид — wrap-дельтами от затравки (кластер на шве тора не
разъезжается — запинено suite_lightbake.inl); радиус = табличный +
полудиагональ кластера; цвет = альбедо материала — неон светит как покрашен.
Поверхность светоматериала ещё и светится сама: `albedo * kMatEmissive[id]` в
шейдинге (колонка `emissive_e3`).

Демо-материал: `neon_tube` (id 20, kMatNeonTube) — вывеска, узор, вылепленная
вокселями лампочка: покрась им ячейки в любом модуле — бейк зажжёт с тенями и
гало. Смерть от carve (перебейк кластера) — следующий инкремент, v1 статичен
до перестройки этажа.

## Фонарик — свет из рук (ПОСТРОЕН 2026-08-17)

Экипированный инструмент ([equip.h] слот Tool), чья строка data/items.csv
светит (та же четвёрка колонок light_*/flicker, что у props.csv), даёт конус
по взгляду — ИЗ РУКИ, не из глаза: свет с нулевым параллаксом от камеры не
может показать ни одной тени по построению (заслон прячет путь света и путь
взгляда одновременно) и читается «искусственным кругом». Рука = полплеча
вправо, локоть ниже глаз, чуть вперёд; параллакс возвращает тени и живой
край луча, камера просто рендерит. Фонарик (`flashlight`, id-строка уже была в контенте) — 20 м,
конус 30°; находится в луте (STORAGE/LIVING/PRODUCTION), включается решением
`equip <слот>` (см. `gear`), выключается `unequip tool`. Это предмет, не свет
камеры: его можно выронить, проиграть, отдать — закон «НПЦ = игрок» цел.
Батарея = существующий износ (durability 300 авторская) — расход при горении
подключит экономика. NPC — тот же путь, когда ai_equip_step научится
инструментам. Цвет пока белый: цветовые колонки items.csv — с первым цветным
носимым источником.

## Тени телам и пропсам (ПОСТРОЕНО 2026-08-17)

Теневой сет вокселного зеркала (masks+class, [voxel_mirror.h]) идёт третьим
сетом (set 2) в pipeline layout'ы cube/body (prop делит layout cube-пасса и
получает его бесплатно); `shadow_march.glsl` — тот же двухуровневый DDA, что
у мира, но только занятость, без материалов. Тела и пропсы ПРИНИМАЮТ тени
мира: лампа больше не светит на труп сквозь стену; carve дыры открывает свет
всем потребителям тем же кадром (буфера общие с физикой). Отбрасывать тени
СОБОЙ тела пока не могут — их нет в зеркале; путь туда — капсулы-заглушки или
вокселизация тел (отдельная задача). prop_tex (фото-пропсы) — ещё стаб.

## God rays и carve-инвалидация (ПОСТРОЕНО 2026-08-17)

- **God rays**: марш дымки прогоняет теневой DDA-луч к источнику — дыра
  режет столб света, решётка полосует конус фонаря. Бюджет по смыслу:
  конусные источники всегда; сильнейший омни — только в разреженных клетках
  (<=4 светов), где одинокий столб читается (полный луч на 16 светов стоил
  23 мс). Худший этаж 14.9 мс, обычный ~5.
- **Carve гасит светоматериал**: сфера carve, задевшая светоячейки
  (пре-скан бокса до carve), перепекает эмиттеры этажа — выломал неон, свет
  погас тем же кадром. Одно событие «ячейка изменилась», два честных
  следствия: дыра впускает свет, выломанная лампа его забирает.
- prop_tex.frag УДАЛЁН: мёртвый шейдер (ничем не грузился), последний
  носитель старого налобника. Фототекстуры пропам — вариантом prop.frag
  через -D, как cube_tex.
- Аудио-крэш на каждом выходе починен: audioSys.shutdown() до SDL_Quit
  ([main.cpp] финальный teardown).

## Очередь (по приоритету)

1. Руки/экипировка/батарея — СВОЯ система, дизайн в [loadout.md] (два слота
   ЛКМ/ПКМ, универсальная активация, расход через износ); свет из рук просто
   читает её Equipped.
2. Фонарик NPC (ai_equip_step: инструменты).
3. Огонь/пламя как источник из физики.
4. Flood-поле отражённого света вместо константного ambient (гибрид:
   DDA — прямой свет и резкие тени, поле — мягкий отскок).
5. Тела ОТБРАСЫВАЮТ тени (их нет в зеркале): капсулы-заглушки или
   вокселизация тел.
6. Кандидат на обсуждение: свет тревоги самосбора прикреплён к камере — по
   духу закона №1 ему место в мире, не на камере.

## Известные компромиссы

- Тень бинарная (0/1), край — воксельный. Резкость = эстетика; мягкость,
  если захочется, — джиттер конца луча, не PCF.
- wire/cloth/particle освещаются без сетки (свой мини-свет + vFog) — тонкие
  и эмиссивные, вклад мал; унифицировать по мере надобности.
- Перелив клетки (>23) молчит. Диагностику (счётчик в отладочную портянку)
  добавить, когда появится плотная расстановка.
