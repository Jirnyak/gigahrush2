# PR 25 - Portal/Upload Support Recheck

Date: 2026-05-27.

Time window: 20:27-20:32 UTC / 21:27-21:32 BST / 23:27-23:32 MSK.

Scope: Yandex Games, VK Play developer/browser project, Newgrounds upload bug status and Gamemoor support/account state. No source code, upload, portal submission, developer application, legal attestation or Newgrounds project mutation was made.

## Result

No live URL was created in this pass.

| Surface | Status | Decision |
| --- | --- | --- |
| Yandex Games | Console session is under `tenevik.games@gmail.com`, but the flow is stopped at Yandex mailbox creation. Official docs still make this a separate portal build: SDK required, `LoadingAPI.ready()` required, `index.html` at archive root, 100 MB unzipped archive limit, external-resource links in texts/media forbidden, moderation usually 3-5 working days. | No draft/submission. Create a Yandex-specific SDK build task before upload. |
| VK Play | Logged in as `Tenevik`; Developer Dashboard opens a registration application requiring company/commercial name, tax residence country, game description, contacts and 18+ confirmation. | No developer application/project submission without owner/legal facts. |
| Newgrounds | Public page still redirects to RIP/eulogy. The `tenevik` account is validated, but project `7759223` still returns `You do not have permission to view this page`; the prior `9B` upload bug was not retested. Current local `itch/gigahrush-itch.zip` is `5 194 294` bytes with root `index.html` (`11 302 283` bytes). | Do not publish or cite Newgrounds until a real archive attaches and preview plays. |
| Gamemoor | Account exists/opens; `/developer` still redirects to homepage. Contact form was sent at 20:12 UTC from `Tenevik Games <tenevik.games@gmail.com>` and confirmed `Message sent! We'll get back to you soon.` Gmail recheck found no visible new support reply beyond the account/welcome thread. | Wait for reply; no duplicate support request and no submission until the developer portal/submit URL is confirmed. |

## Next Steps

1. Yandex: make a separate SDK/portal build task, not a same-turn PR post.
2. VK Play: wait for owner/company/legal/tax facts before any application.
3. Newgrounds: retest only when the account can access the project or support gives a clean upload path.
4. Gamemoor: monitor email/support; do not resubmit while awaiting reply.
